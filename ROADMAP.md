# rx-bridge-electron 로드맵

현재 범위는 **신뢰할 수 있는 로컬 Electron UI**의 Main–Renderer 통신이다.

## 운영 규칙

- 항목 ID는 `RD-001`, `RD-002`처럼 고정한다. 기존 항목 사이에 작업을 넣을 때는 번호를 바꾸지 않고 `RD-001a`, `RD-001b`처럼 붙인다.
- `[ ]`는 미완료, `[x]`는 구현과 해당 완료 기준의 검증이 끝난 상태다. 설계 결정만 필요한 항목은 결정과 문서 반영까지 마쳐야 완료로 표시한다.
- 각 RD를 시작할 때 최신 코드와 테스트를 다시 확인한다. 아래의 현황은 로드맵 작성 시점의 정적 검토 결과이며, 미실행 테스트를 통과한 것으로 취급하지 않는다.

## 진행 순서

- [x] **RD-001 — 공개 인터페이스 네이밍 결정.** Renderer의 `api.domain.operation`과 요구사항 예시의 `api.domain.rpc.operation`·`state`·`event` 계층을 비교한다. 종류별 이름 충돌, 자동 완성, Proxy 및 타입 추론 기준으로 하나를 결정하고 문서에 기록한다. `defineDomain`/`composeContracts`, `timeoutMs`, `DEADLINE_EXCEEDED`는 현재 의미가 명확하므로 이름 변경을 전제하지 않는다. `dispose()`는 RD-003의 종료 의미와 이름 충돌 정책을 함께 결정한다. **완료 기준:** 공개 호출 형태가 결정되고, README·타입 예제·데모가 선택한 형태와 일치한다.

- [x] **RD-001a — Renderer 공개 호출을 계층형으로 전환.** RD-001의 평면 결정(ADR 0005)을 뒤집어 `api.<domain path>.rpc|state|event.<operation>`으로 바꾼다. operation 이름의 `/` 중첩을 금지해 manifest 키의 마지막 segment를 operation으로 확정하고, 도메인 경로 segment에서 `rpc`·`state`·`event`를 예약한다. 종류 간 동명 금지는 유지하고, 도메인에 없는 종류는 노출하지 않는다. `$` 접미사와 루트 `dispose` 예약은 그대로 둔다. 와이어 키와 handshake 형식은 바꾸지 않는다. **완료 기준:** 계약 검증·Renderer manifest 파서·`InferBridge`·Proxy가 계층형을 따르고, 새 ADR이 ADR 0005를 대체하며, README·타입 예제·데모·Electron fixture가 계층형과 일치한다.

- [x] **RD-002 — 활성 State의 늦은 로컬 구독자에게 현재값 전달.** 한 Renderer에서 State를 이미 구독 중일 때 새 구독자가 합류하면 현재 세대의 최신값을 즉시 받도록 한다. 마지막 구독 해제 뒤 남은 `stale` 값은 새 세대에 재생하지 않는다. `undefined`도 유효한 현재값으로 취급한다. **완료 기준:** 첫 구독, 늦은 구독, 마지막 해제, 재구독, 동기 전달을 검증하고 Main의 원격 구독은 공유된다.

- [x] **RD-003 — Renderer와 Main 종료 계약 완성.** Renderer 종료 시 진행 중 RPC를 로컬에서 확정·취소하고 모든 스트림을 정리한다. 종료 후 새 호출·구독의 결과를 정의한다. Main 서버 종료는 최종 상태가 되어 재등록과 새 세션을 허용하지 않는다. 명시적 `dispose()`와 `Symbol.dispose`의 관계를 RD-001 결정에 맞춰 정리한다. **완료 기준:** 진행 중 RPC·스트림, 반복 종료, 종료 중 동기 재진입, 종료 후 호출을 검증하며 늦은 응답이 전달되지 않는다.

- [x] **RD-004 — RPC 출력의 최종 경계값 검증.** 출력 스키마가 반환한 변환 결과를 IPC 전송 전에 다시 `BridgeValue`와 payload 한도로 검사한다. 입력 검증 실패는 `INVALID_ARGUMENT`, handler·출력 실패는 안전한 `INTERNAL`이라는 구분을 유지한다. **완료 기준:** 스키마가 변환하여 만든 비허용 값과 과대 값을 Renderer로 보내지 않는 테스트가 있다.

- [x] **RD-005 — 계약과 Main 등록의 일치 및 타입 검사.** 서버 생성 시 계약의 모든 도메인 구현이 정확히 한 번 등록됐는지 확인한다. `implementDomain`의 RPC 입력·출력 타입을 각 descriptor에서 추론하여 잘못된 handler를 가능한 한 컴파일 단계에서 잡는다. **완료 기준:** 누락·중복 도메인과 누락·초과 operation은 시작 시 실패하고, 잘못된 handler 타입은 타입 검사에서 실패한다. Manifest에 광고된 operation은 등록된 구현을 가진다.

- [x] **RD-006 — 세션별 자원 및 메시지 전체 크기 제한.** 현재의 깊이·항목 수·문자열별 크기·Event 큐 제한에 더해 진행 중 RPC 수, 활성·대기 구독 수, 전체 메시지 크기, timeout 상한을 어디에서 적용할지 결정한다. 장시간 문서에서 사용 완료한 ID 보관량도 제한하되 늦은 메시지를 오인하지 않도록 한다. **완료 기준:** 한 세션의 과부하가 다른 세션의 정상 요청을 막지 않으며, 각 한도 초과가 정해진 오류와 정리 동작으로 끝난다.

- [x] **RD-007 — 운영 진단의 최소 집합 정의.** 기존 diagnostics hook을 바탕으로 활성 세션·RPC·구독 수, 큐 사용량·드롭, timeout·검증 실패, 보안 거부의 사유를 관측할 필요를 정한다. 기록에는 원문 payload·자격증명·내부 경로를 넣지 않는다. **완료 기준:** 채택한 지표의 생성·해제·거부 경로를 검증하고, 기본 동작에서 무조건 콘솔에 출력하지 않는다.

- [x] **RD-008 — 다중 Renderer와 장시간 실행 검증.** 위 변경을 합친 뒤 실제 Electron에서 두 창의 세션·권한·업스트림 공유와 느린 소비자 격리를 확인한다. 반복 구독·해제, reload, 동시 RPC, overflow를 포함한다. **완료 기준:** 단위 및 타입 검사와 실제 Electron 실행 결과를 구분해 기록하고, 검증한 런타임·플랫폼·빌드 형태를 명시한다. **결과:** [docs/verification/rd-008.md](docs/verification/rd-008.md)

- [x] **RD-009 — `authorize` 예외 응답 코드 통일과 overflow 종료 시점 문서화.** RPC 경로는 `authorize` 예외를 adapter까지 다시 던져 `INVALID_ARGUMENT "Invalid bridge request."`로 응답하고, stream 경로는 `INTERNAL`로 응답한다. 앱 권한 콜백의 실패는 요청 형식 오류가 아니므로 두 경로를 `INTERNAL "Internal bridge error."`로 통일하고, 요청이 이미 취소됐으면 `CANCELLED`가 우선한다. 와이어 형식과 오류 코드 집합은 바꾸지 않는다. 함께 `error` 정책 overflow의 종료 통지가 대기 값 전달 뒤에 오고 구독 슬롯은 그 종료 뒤 반환된다는 동작을 문서에 명시한다(동작 변경 없음). 출처: `.scratch/authorize-exception-code`, `.scratch/overflow-slot-timing`. **완료 기준:** RPC `authorize` 동기 throw·비동기 reject가 `INTERNAL`, abort 중 예외가 `CANCELLED`, 예외 뒤 RPC 슬롯 반환, adapter 경유 응답도 `INTERNAL`임을 테스트로 검증하고, ADR·architecture·README가 두 경로의 분류와 overflow 종료 순서를 일치하게 기술한다. **결과:** 완료 조건 전부 충족, 편차 없음. 결정은 [ADR 0011](docs/adr/0011-authorize-exception-internal.md). overflow 문서 보정은 완료·upstream 오류까지 같은 순서로 넓혀 기술했다.

### 경량 계약 (명세: [.scratch/lightweight-contract/spec.md](.scratch/lightweight-contract/spec.md))

초기 구현의 사용자 코드량을 raw IPC 수준으로 줄인다. 계약은 타입만으로 두고 도메인 스키마는 operation 단위로 선택·점진 도입한다. 사용자 코드가 필요 없는 구조·크기 검사, 세션 자원 한도, origin/sender 검사는 기본 유지한다. 기존 descriptor API는 호환 계층 없이 대체한다(외부 소비자 없음). **공통 측정 기준:** README hello-world(RPC 1개, State 1개)에서 계약 타입 5줄 이하, 스키마 0줄, zod 의존성 0.

- [x] **RD-010 — 경량 계약 결정 문서화.** 타입만의 계약, `createBridgeServer<AppBridge>(impl, options)`, impl 키 기반 manifest, 선택 `schemas`·`errors` map, Main source의 buffer 옵션을 신규 ADR로 기록한다. ADR 0004는 도메인 스키마를 선택으로 바꾸고 구조·크기 검사는 모든 operation에 유지한다고 개정한다. ADR 0008은 런타임 계약·등록 일치 검사를 컴파일 단계 검사로 대체한다. **완료 기준:** 신규 ADR과 개정·대체 ADR, architecture가 새 계약과 검증 순서를 일치하게 기술한다. **결과:** 신규 [ADR 0012](docs/adr/0012-lightweight-type-contract.md) 작성, [ADR 0004](docs/adr/0004-validated-bounded-payloads.md)·[ADR 0008](docs/adr/0008-contract-registration-match.md) 상단에 개정·대체 표시(ADR 0008은 "결정 변경"이 아니라 전제 자체가 성립하지 않는다고 명시). `docs/architecture.md`는 DELTA-01에서는 미루고 DELTA-10에서 "계약 형태와 등록" 절 신설과 기존 서술(등록 검증, payload 한도, 세션/진단 옵션 시그니처) 전체 갱신으로 완료. 편차 없음.

- [x] **RD-011 — 타입 계약과 impl 기반 서버.** `createBridgeServer<AppBridge>(impl)`가 스키마 없이 RPC·State·Event를 제공한다. manifest는 impl 키에서 만들고, Event buffer는 source 옵션(생략 시 `capacity: 100`, `overflow: "error"`), 허용 에러 코드는 `options.errors` 중첩 map(목록 밖 코드는 안전한 오류)으로 받는다. 이 단계에서는 기존 descriptor API와 공존한다. **완료 기준:** 스키마 없는 RPC·State·Event가 동작하고, impl의 누락·초과 operation과 잘못된 handler·source 타입이 타입 검사에서 실패하며, `parseBridgeValue`·payload 한도·세션 한도가 그대로 적용된다. **결과:** `BridgeApi<B>`/`BridgeImpl<B>`/`SchemasFor<B>`/`ErrorsFor<B>` 타입과 13개 `@ts-expect-error`(DELTA-02), impl 트리 기반 등록 테이블(DELTA-03), `createBridgeServer`의 impl 오버로드(DELTA-04, 신규 19 테스트 GREEN)로 완료. 입력 없는 RPC handler가 실제로는 `(input, context)` 2-인자로 호출되는데 타입은 `(context)` 1-인자를 요구하던 불일치를 DELTA-04a에서 발견 즉시 수정(`(input: undefined, context) => O`). 편차 없음.

- [x] **RD-012 — 선택 스키마 map.** `options.schemas`를 `SchemasFor<AppBridge>` 부분 map으로 받아 생성 시 경로→스키마 테이블로 평탄화하고 dispatcher(입력·출력)와 stream-hub(State·Event 출력)에 적용한다. `satisfies`로 별도 파일에 둘 수 있다. **완료 기준:** 일부 operation에만 스키마를 적용할 수 있고, 경로 오타와 스키마 출력 타입 불일치가 컴파일 에러이며, 입력 스키마 실패는 `INVALID_ARGUMENT`, 출력 스키마 실패는 `INTERNAL`로 끝난다. **결과:** DELTA-05에서 신규 테스트 3개 파일(런타임 + 타입)이 구현 보정 없이 처음부터 GREEN — DELTA-04의 배선이 이미 요청 처리 순서·실패 분류와 정확히 일치했다. input 스키마 결과에는 output과 달리 clone-then-reparse 재검사를 적용하지 않는다는 의도된 비대칭을 확인(DELTA-05 "## 결정" 참고). 편차 없음.

- [x] **RD-013 — descriptor API 제거와 소비자 이전.** `defineDomain`·`rpc`·`state`·`event`·`composeContracts`·`implementDomain`을 제거하고 demo·Electron fixture·README를 새 형태로 옮긴다. demo 스키마는 `main/`으로 이동해 Renderer 번들에서 빠진다. **완료 기준:** 공통 측정 기준을 README에서 달성하고, 전체 검증과 Electron acceptance가 통과한다. **결과:** demo 이전(DELTA-06, Renderer 번들 zod grep 0건 확인) → 패키지 단위 테스트 이전(DELTA-07, 35 files/429 tests, 이전 전후 검증 항목 대조 완료, 대응 없는 항목은 사유 기록) → Electron fixture·multi-window·soak 이전(DELTA-08, RD-008 시나리오 수 10→10 불변, soak 8.69s 편차 없음) → 옛 descriptor API·어댑터·Renderer 타입 추론용 옛 타입 제거(DELTA-09, 저장소 전체 grep 0건, `pnpm verify`/`pnpm lint`/demo `pnpm test` 통과) 순서로 완료. 공통 측정 기준은 DELTA-10에서 README를 hello-world로 다시 써서 달성(계약 타입 4줄, 스키마 0줄, zod 0). **편차:** demo `pnpm test:electron`이 이 샌드박스에서 간헐적으로(6회 중 최대 2회) `Controller window missing`으로 flake — 창 텍스트 렌더링 전에 창 목록을 스캔하는 헤드리스 Electron 타이밍 문제로 RPC/State/Event 로직과 무관하다고 판단(DELTA-06·DELTA-09에서 각각 관찰, 재시도 시 통과). 이번 리팩터로 새로 생겼는지는 별도 확인하지 않았다 — 사전 존재 가능성이 높은 환경 이슈로 후속 과제로만 기록.

- [x] **RD-014 — 배선 코드 축약.** 기존 함수 인자를 선택화해 배선 보일러플레이트를 줄인다(새 API 없음, 기존 명시 호출 호환). `bindElectronBridge`의 `ipcMain`·`namespace`, `attach`의 `role`(기본 `"default"`), `exposeBridgeInMainWorld`의 `contextBridge`·`ipcRenderer`·`namespace`를 선택으로 바꾼다. `namespace` 기본값은 Main·preload 공통 `"default"`, Electron 모듈은 `import * as electron from "electron"`으로 호출 시점에 해석하고 주입값이 우선한다. `createRendererApi<B>(transport?)`는 생략 시 `globalThis.rxBridge`를 읽어 `declare global`을 없앤다. hello-world에서 `pagehide` dispose 등록을 빼고 `dispose`는 SPA teardown 용도로 문서화한다. `allowedOrigins` 필수, 서버·어댑터 분리, 채널·와이어 형식은 유지한다. Proxy 교체(`.scratch/renderer-proxy-frozen-tree`)는 범위 밖. **완료 기준:** README hello-world 배선(import 제외)이 Main 3줄·preload 2줄·Renderer 2줄 이하이고, 기본값·명시 주입·에러 경로를 단위 테스트로 검증하며, 실제 Electron에서 축약형이 동작하고 dispose 없는 창 닫기·reload에도 Main 세션·구독·RPC 슬롯이 회수된다. demo·Electron fixture·README를 축약형으로 이전하고 결정을 ADR 0013에 기록한다. **결과:** ADR 0013 확정(DELTA-01, ADR 0001·0006 상호 참조) → Main `bindElectronBridge`/`attach` 인자 선택화, `import * as electron from "electron"` 호출 시점 해석 + 미주입·미가용 시 `TypeError`(DELTA-02) → preload `exposeBridgeInMainWorld` 인자 선택화, 기본 namespace 채널 Main과 일치 확인(DELTA-03) → Renderer `createRendererApi` transport 선택화, 생략 시 `globalThis.rxBridge`, 라이브러리가 `declare global`(`Window.rxBridge`) 내장(DELTA-04) → Electron acceptance: fixture 축약형 이전(ESM main + sandbox CJS preload + iife renderer 3/3), dispose 없는 창 닫기 회수 신규 시나리오 단독 5회 연속 8/8 통과, 기존 multi-window 8/8 유지(DELTA-05) → demo 축약형 이전, `role`은 인가에 쓰여 유지(main/monitor), `appBridge`→`rxBridge` 테스트 1곳만 수정(DELTA-06) → README hello-world·architecture 갱신(DELTA-07). 줄 수 측정: hello-world를 "도메인 서버/사용 예"와 "배선" 전용 코드 블록으로 나누고, 배선 블록만 import·빈 줄 제외 전체 줄을 집계 — Main 3줄(`bindElectronBridge`+`attach`+`dispose` 등록), preload 1줄(`exposeBridgeInMainWorld()`), Renderer 1줄(`const api = await createRendererApi<AppBridge>()`), 기준(3/2/2) 이내. 전체 검증: `pnpm verify`·`pnpm lint`·`pnpm format:check`·demo `pnpm test` 통과(자세한 근거는 `_works/_completed/20260924-08-wiring-shorthand/DELTA-07.md` 참고). **편차:** DELTA-05에서 발견한 stale `dist/` 함정(`pnpm verify`가 `build`보다 `test`를 먼저 실행해, `src/` 변경 후 재빌드 없이 `*.electron.test.ts`를 돌리면 옛 dist 타입 오류로 실패) — 회피(먼저 `pnpm --filter @cp949/rx-bridge-electron build`)만 기록하고 `verify` 스크립트 순서 변경은 이번 범위 밖. demo `pnpm test:electron`의 `Controller window missing` 간헐 실패(RD-013 등록 편차)는 이번 축약형 이전 중 재현되지 않았다(DELTA-05·DELTA-06 각각 연속 실행 통과).

### Main 수명주기 심화 (출처: 아키텍처 리뷰 `_works/arch-review/01.html` 후보 01·02)

- [x] **RD-015 — 구독 수명주기를 `Subscriptions` 모듈 하나로 모은다.** 지금 구독 1건의 상태가 `DocumentSessions`(watermark·`pendingStreams`·`subscriptions`)와 `StreamHub`(`#consumers`)에 나뉘고, `create-bridge-server.ts`의 `controlStream`이 `beginStream`→`authorize`→`finishStream`→`current`→`subscribe`→`onClose`→`releaseStream` 호출 순서를 손으로 배선한다. `StreamHub`를 `Subscriptions`로 바꿔 subscriptionId 파싱·watermark·slot·`authorize` 대기와 호출·reject·consumer·교차 세션 fan-out·terminal·slot 반환을 모두 소유하게 한다. 세션별 상태는 모듈 안 `WeakMap<DocumentSession, …>`에 두고 세션 안에서는 `subscriptionId`로 식별하며(stream 쪽 `keyOf`·`#id` 삭제), retire는 `session.signal` 하나로 받는다. `DocumentSessions`에서 `beginStream`·`finishStream`·`releaseStream`·`cancelStream`·`subscriptionCount`와 관련 필드를 제거한다. server의 `controlStream`에는 version 검사·세션 해석·`sender-unauthorized`·위임만 남긴다. **동작 변경(이것만):** stream 순서를 ID 형식 → watermark → 등록 조회 → slot → `authorize`로 바꿔 미등록 key는 RPC처럼 항상 `NOT_FOUND`(현재는 `authorize` deny 시 `FORBIDDEN`)이고 `authorize`는 등록된 key만 받는다. 등록 조회 뒤 판정되는 `subscription-limit` 진단에 `key`를 넣는다(ADR 0010 §6 규칙 적용). retire 시 대기 중 `authorize` signal이 RPC signal보다 먼저 abort되는 순서 변화는 수용한다. 그 외 wire 메시지 순서, ADR 0009 §11 slot 반환 시점, 진단 이벤트 종류·순서, `getDiagnosticsSnapshot().subscriptions` 값은 보존한다. RPC 수명주기는 범위 밖(RD-016). **완료 기준:** 동작 변경을 현재 구조에서 test-first로 먼저 넣고, 구조 이동 단계는 `test/` 내용 변경 0(파일 이름 변경만 허용)으로 `pnpm verify`·`pnpm lint`·Electron acceptance가 통과한다. 새 ADR 0014가 등록 조회 순서를 기록하고 ADR 0010 §6·§9에 개정 표시, `docs/architecture.md`·`CONTEXT.md`(구독)가 새 구조와 일치한다. **결과:** 동작 변경을 현재 구조에서 test-first로 먼저 넣음(DELTA-01, 신규 test 5건 + 기존 test 2건 갱신, 413/413) → `StreamHub`를 `Subscriptions`로 이동해 구독 수명주기 전체를 흡수(DELTA-02, `controlStream` 143→34줄, `DocumentSessions` public 메서드 18→13개, test diff는 rename 1건뿐) → `isScopedSource` 중복과 도달 불가 scoped 분기 제거(DELTA-03, 방어적 캐스트 없이 타입으로 증명) → ADR 0014 신규 작성, ADR 0010 §6·§9 개정 표시, `architecture.md`에 stream 판정 순서 서술 신설(DELTA-04) → `xvfb-run -a pnpm verify` 전체 통과(413+24+4 test, Electron acceptance multi-window 8/8), demo `pnpm test`도 별도 재확인(DELTA-05). **편차:** 없음(TRP-001 `Controller window missing` flake는 이번 검증에서 재현되지 않았다). 후속: `FakeTarget.isCurrentMainFrame`의 frameId 미검증 문제를 `.scratch/sender-admission-unification/issues/01-fake-target-frame-id.md`에 등록.

- [x] **RD-016 — RPC 요청 수명주기를 `RpcRequests` 모듈 하나로 모은다.** 지금 RPC 1건의 상태가 `DocumentSessions`(`active`·`runningRpc`·`#globalRunningRpc`)에 있고, `create-bridge-server.ts`의 `dispatchRpc`가 `findRpc`→`tryAcquireRpc`→`beginRpc`→`authorize`→`current` 재검사→`dispatchRegistered`→`finishRpc`→`releaseRpc` 호출 순서와 deadline을 손으로 배선한다. "aborted면 `CANCELLED` 우선" 규칙은 7곳(`rpc-dispatcher.ts:55,83,105,120,133`, `create-bridge-server.ts:246,252`)에 복제됐다. `rpc-dispatcher.ts:55,83,133`도 도달 가능하다 — 직전 aborted 검사와 동기 parse 사이에 await는 없지만, `parseBridgeValue`·input/output schema의 `parse` 자체가 사용자 코드라 동기 단계 안에서도 signal을 abort할 수 있다(`rpc-dispatcher.test.ts:413`이 output schema 경로를 이미 고정한다). `rpc-dispatcher.ts`를 `main/rpc-requests.ts`의 `RpcRequests`로 바꿔 등록 조회·slot·`authorize`·validation pipeline·deadline·`rpc-finished`·cancel을 모두 소유하게 한다. 세션별 상태는 모듈 안 `WeakMap<DocumentSession, …>`에 두고 세션 안에서는 `requestId`로 식별하며(RPC `keyOf` 삭제), retire는 요청마다 `session.signal` abort listener로 받는다(`#retire`의 RPC 취소 루프 삭제). `rpcInFlight`는 retire된 세션의 미종료 handler도 세도록 모듈 안 전역 카운터로 유지한다(ADR 0010 §10). `DocumentSessions`에서 `tryAcquireRpc`·`beginRpc`·`finishRpc`·`releaseRpc`·`cancelRpc`·`rpcInFlightCount`와 관련 필드를 제거한다. server의 `dispatchRpc`·`cancel`에는 version 검사·세션 해석·`sender-unauthorized`·위임만 남긴다. `CANCELLED` 우선 규칙은 guard 하나로 정의해 사용자 코드가 실행되는 단계 경계 5곳(`authorize`, `parseBridgeValue`, input schema, handler, output schema)에 적용한다. 삭제하는 분기는 없다. `authorize` 뒤 `current() !== session` 재검사는 제거하고 signal만 본다(`Subscriptions`와 같다). **관측 가능한 동작 변경 없음**(편차 참고). 보존: 판정 순서, 오류 코드, 진단 이벤트 종류·개수, 같은 세션의 중복 `requestId`가 앞선 요청을 취소하는 동작, deadline 의미(만료 시 abort·`rpc-timed-out`·`DEADLINE_EXCEEDED`, slot과 `rpc-finished`는 handler 종료 시 — ADR 0009 §10), ADR 0011 분류, `getDiagnosticsSnapshot().rpcInFlight` 값. 수용: retire 시 `rpc-cancelled`와 구독 종료 진단이 도착 순서대로 섞인다(현재는 구독 먼저). 범위 밖: version 분기·`recordAdapterRejection`·`FakeTarget` frameId(후보 04), wire key 문법(후보 03), deadline 뒤 Renderer `cancel`이 `rpc-cancelled`를 추가 기록하는 동작(후속 이슈로 등록). **완료 기준:** 현재 구조에서 test를 먼저 보강하고(중복 `requestId` characterization, `rpc-dispatcher.test.ts:157` 제목을 실제 순서에 맞게 수정, 미등록 key는 `authorize` 호출 0회·`NOT_FOUND`), 구조 이동과 guard 통합 단계는 `test/` 내용 변경 0(파일 이름 변경 `rpc-dispatcher.test.ts`→`rpc-requests.test.ts`만 허용)으로 `pnpm verify`·`pnpm lint`·Electron acceptance가 통과한다. test surface는 `createBridgeServer` seam만 쓴다. 새 ADR 0015가 소유 구조·`current()` 재검사 제거와 그 근거(main frame `routingId`는 retire 없이 바뀌지 않는다 — 구독과 RPC 두 경로가 기댄다)·retire 진단 순서 변화를 기록하고, ADR 0014·ADR 0010 §10에 개정 표시, `docs/architecture.md`(RPC 판정 순서)·`CONTEXT.md`(RPC 요청)가 새 구조와 일치한다. **결과:** 현재 구조에서 test를 먼저 보강(DELTA-01, 신규 test 5건 + 기존 test 제목 1건 수정, 414→419) → `rpc-dispatcher.ts`를 `RpcRequests`로 이동해 RPC 수명주기 전체를 흡수(DELTA-02, `dispatchRpc` 131→30줄, `DocumentSessions` public 메서드 13→7개, test diff는 rename 1건뿐) → `CANCELLED` 우선 guard를 하나로 통합하고 `authorize` 뒤 `current()` 재검사 제거(DELTA-03, `cancelledIfAborted` guard 1곳, `"Request cancelled."` 응답 생성 1곳) → ADR 0015 신규 작성, ADR 0014·ADR 0010 개정 표시, `architecture.md`에 RPC 판정 순서 서술 신설(DELTA-04) → `xvfb-run -a pnpm verify` 전체 통과(419 test, Electron acceptance `bridge.electron.test.ts` 3/3·`multi-window.electron.test.ts` 8/8, retire 시 `rpc-cancelled` 3 포함), demo `pnpm test`·`test:electron`도 재확인(DELTA-05). **편차:** Renderer handshake 실패 문구 1건 — DELTA-03이 `create-renderer-api.ts`의 중복 사전 검사를 없애면서 "Unsupported bridge handshake."가 사라졌고, 리뷰에서 `VERSION_MISMATCH`일 때만 이 문구로 복원했다. 올바른 3키 모양에 clientId·manifest만 잘못된 handshake는 이전 "Unsupported"에서 이제 "Malformed"다. code는 계속 `INTERNAL`이고 문구는 계약이 아니다. 리뷰에서 `withEnvelope`가 body의 `protocolVersion`·`clientId`를 envelope 값으로 덮어쓰게 고쳤다(옛 preload 조립 순서와 같다 — 현재 호출자는 이 필드를 넘기지 않아 wire 값 변화 없음). TRP-001 `Controller window missing` flake는 이번 검증에서 발동하지 않았다. 후속: deadline 뒤 Renderer `cancel`이 `rpc-cancelled`를 추가 기록하는 동작을 `.scratch/rpc-deadline-cancel-diagnostic/issues/01-deadline-cancel-diagnostic.md`에 등록.

### wire 문법 단일 소유 (출처: 아키텍처 리뷰 `_works/arch-review/01.html` 후보 03)

- [x] **RD-017 — wire key 문법을 `protocol/operation-key` 하나가 소유한다.** operation key(`category:domain/op`)의 생성·분해, segment 검증(빈 segment·`.`·예약어 `__proto__`·`prototype`·`constructor`·`then`, 도메인 root `dispose`, 도메인 segment의 `rpc`·`state`·`event`), 경로 충돌 검사(leaf/namespace, 카테고리 간 중복)가 Main(`registration.ts`)과 Renderer(`create-renderer-api.ts`)에 따로 구현돼 있다. 지금은 의미가 같지만 일치를 보장하는 코드가 없고(`create-renderer-api.ts:161` 주석이 동일성을 주장할 뿐), 예약어 Set·카테고리 목록 리터럴이 4곳에 복제됐다. 조회도 두 방식이다 — RPC는 `startsWith("rpc:")`·`slice(4)`로 table key를 얻고, `Subscriptions`는 wire key Map을 한 벌 더 만든다. 비공개 모듈 `src/protocol/operation-key.ts`(`formatWireKey`·`parseWireKey`·`checkDomainSegments`·`checkOperationName`·`OperationPathTrie`)를 만들고 throw 대신 verdict를 반환하게 한다. Main은 verdict를 `TypeError`와 현재 상세 메시지로, Renderer는 `RemoteError("INTERNAL")`와 같은 상세 문구로 번역한다. Renderer는 handshake 수신 시 계속 런타임에 독립 거부한다(코드는 공유하고 신뢰는 공유하지 않는다, ADR 0007). `RegistrationTable`은 wire key로 keyed한다. Main의 impl namespace key split과 Renderer API tree 구성은 각자 남는다. **관측 가능한 동작 변경 없음**(Renderer 거부 메시지 문구 제외, code `INTERNAL` 유지). 보존: 거부/허용 판정, Main 에러 타입·메시지, handshake manifest 순서(도메인명 → operation명 정렬 — wire key 문자열 정렬과 다르다), wire 형식·진단 `key`. 범위 밖: `authorize`의 key 형식과 사용자 코드 파싱(demo `composition.ts:23`, 후속 이슈), 코어 공개 export, Renderer Proxy 구조(후보 07). **완료 기준:** 공유 case table로 현재 Main·Renderer 판정 동등성을 먼저 고정하고, 코어를 table-driven test로 직접 검증한 뒤, 호출자 교체 단계는 `test/` 내용 변경 0으로 통과한다. 이어서 이름 규칙 seam test를 대표 건으로 줄인다(Main 14→4 + 허용 2, Renderer 6→2). 예약어·카테고리 리터럴은 `src/` 안 1곳, `startsWith("rpc:")`·`slice(4)` 0건. `pnpm verify`·`pnpm lint`·Electron acceptance·demo test 통과. ADR 0007에 개정 절(소유 위치, 코드 공유·신뢰 비공유, 거부 주체 정정, JS 예약어 명시), ADR 0005:7 거부 주체 정정, `docs/architecture.md`·`CONTEXT.md`(operation key)가 새 구조와 일치한다. **결과:** DELTA-01~05 완료 — 공유 case table로 Main·Renderer 판정 동등성을 먼저 고정(DELTA-01, 33 case)하고 `src/protocol/operation-key.ts` 코어를 table-driven test로 직접 검증(DELTA-02)한 뒤, Main `registration.ts`(DELTA-03)·Renderer `create-renderer-api.ts`(DELTA-04)를 이 코어 호출로 교체(두 단계 모두 `test/` 내용 변경 0 확인)하고 이름 규칙 seam test를 코어 table 한 벌로 축소(DELTA-05, Main 16→6·Renderer 9→5, 삭제 case는 모두 코어 table 대조표로 남김). DELTA-06에서 ADR 0007 개정 절·ADR 0005:7·`docs/architecture.md`·`CONTEXT.md`를 새 소유 구조(`buildRegistrationTableFromImpl`·`createRendererApi`가 각각 코어를 호출)와 맞췄다. DELTA-07(전체 검증)에서 실측 전부 GREEN: 패키지 `xvfb-run -a pnpm verify`(check-types·test·build) exit 0, 34 test files·482 tests passed; 루트 `pnpm lint` 2/2, `pnpm format:check` 전부 통과; Electron acceptance `bridge.electron.test.ts` 3/3·`multi-window.electron.test.ts` 8/8(합계 11/11, 진단 `key` 값 wire key 그대로 유지); demo `pnpm test:unit` 10 files·24 tests, demo `pnpm test:electron` 1 file·4 tests(1회차 통과, TRP-001 간헐 실패 미발동 — 편차 기록 대상 아님); handshake manifest 순서 test GREEN; `startsWith("rpc:")`·`slice(4)` 0건, 예약어/카테고리 리터럴 grep(`"__proto__"|reservedSegments|parseSegments`) `src/protocol/operation-key.ts` 1곳뿐, `protocol/index.ts`의 `operation-key` re-export 0건. soak(`test:soak`)은 위험 표면 불일치 판단으로 미실행. 부수 발견: `src/protocol/messages.ts:200`의 `assertKeys(manifest, ["rpc","state","event"])`에 카테고리 리터럴 1건이 grep 대상 밖에 남아 있었다(동작 영향 없음) — 마무리 단계에서 `OPERATION_CATEGORIES`로 교체해 정리, 재검증(`check-types`·`test`·`lint`·`format:check`) 통과. RD-017 완료. 병합은 이 결과와 별개로 판단한다.

### sender admission 단일 판정 (출처: 아키텍처 리뷰 `_works/arch-review/01.html` 후보 04)

- [x] **RD-018 — sender admission을 사유가 담긴 단일 판정으로 모은다.** 지금 frame·origin·client 검사는 `DocumentSessions.establish`(`document-sessions.ts:60-72`)와 `current`(`:95-107`)에 같은 4조건으로 2벌 있고, adapter handshake(`electron-adapter.ts:191-198`)에 1벌 더 있다(origin은 `targetFor`의 `isAllowedOrigin`과 같은 `options.allowedOrigins`를 직접 본다). 같은 사실이 채널마다 다른 사유가 된다 — subframe은 handshake에서 `frame-not-main`, rpc·subscribe·ack/unsubscribe에서 `sender-unauthorized`, cancel에서는 기록 없음(`create-bridge-server.ts:212`). `sender-unauthorized` 하나가 frame·origin 불일치·미attach·retired clientId·disposed·establish 경합을 모두 덮는다. 숫자 version 불일치는 adapter의 `parse*`가 `VERSION_MISMATCH`로 throw하고 adapter가 `malformed-envelope` + `INVALID_ARGUMENT`로 바꾸므로, server의 version 분기(`create-bridge-server.ts:191,222`)는 `protocolVersion: 2 as 1` cast test에서만 실행된다(ADR 0010의 `version-mismatch`가 운영 경로에서 기록되지 않는다). adapter 판정을 sink에 넘기려고 `recordAdapterRejection` Symbol 통로(`diagnostics.ts:11`, `create-bridge-server.ts:98,171`)가 생겼다. `FakeTarget.isCurrentMainFrame`(`test/main/fake-ipc.ts:27`)은 frameId를 무시해 server seam test가 frame 교체 거부를 관측하지 못한다. **구조:** server가 `unknown` 값을 받아 envelope parse(version 포함)와 admission을 모두 판정한다. 공개 `StreamBridgeServer`의 `handshake(sender, value): HandshakeResponse | RpcResponse`(성공·거부 wire 응답 모두 server가 만든다)·`controlStream(sender, value, send): Promise<void>`와 부모 `BridgeServer`의 `dispatchRpc(sender, value): Promise<RpcResponse>`·`cancel(sender, value): void`로 바꾼다(breaking — npm 배포 이력 없음, ADR 0005/0007과 같은 근거로 호환 별칭 없음). `protocolError` helper는 server로 옮긴다. adapter는 Electron event → `SenderIdentity` 번역, IPC 등록·응답, server throw 대비 fallback try/catch 한 겹만 남긴다. admission은 `DocumentSessions`의 private `#admit(sender)` 하나가 판정하고 `establish`·`current`가 `{ session } | { reason }` verdict를 반환한다. 판정 순서는 parse → disposed → attachment 없음 → `isMainFrame`·`isCurrentMainFrame` → origin → client(retired clientId, establish 경합)다. `recordAdapterRejection` Symbol, adapter handshake의 frame·origin 검사, server의 version 분기를 삭제한다. 공개 export `WireRpcRequest`·`WireCancelRequest`는 유지한다. **동작 변경(이것만):** (1) 사유가 채널과 무관해진다 — frame 불일치(subframe·현재 main frame 아님)는 `frame-not-main`, origin 불일치는 `origin-not-allowed`, `sender-unauthorized`는 미attach·retired clientId·disposed·establish 경합만. `RejectReason` enum은 바꾸지 않는다. (2) cancel 거부를 다른 채널과 같은 verdict로 기록한다. (3) version 불일치는 4채널 모두 `version-mismatch`로 기록하고 RPC wire 응답은 `VERSION_MISMATCH "Unsupported protocol version."`이다(현재 `malformed-envelope` + `INVALID_ARGUMENT`). (4) attach되지 않은 webContents의 disallowed origin handshake는 `origin-not-allowed` → `sender-unauthorized`. (5) 구조 오류 input(Symbol·함수 등)은 server를 직접 호출해도 등록 조회·`authorize` 전에 envelope parse에서 `malformed-envelope` + `INVALID_ARGUMENT "Invalid bridge request."`로 거부된다(IPC 경로는 이미 같다). 보존: wire 응답에 사유를 싣지 않는다(RPC 거부는 사유와 무관하게 `FORBIDDEN "Bridge sender is not authorized."`, handshake 거부 응답 모양 유지), 한 요청에 `rejected` 최대 1회, `session-opened`/`session-closed` 시점, 그 외 진단 이벤트 종류·순서. 범위 밖: 채널 상수·envelope builder·ID의 protocol 이동(후보 05, TRP-002), loopback adapter(후보 06 — 새 시그니처는 그 전제만 만든다), `retiredClientCount` test 전용 인터페이스. `.scratch/sender-admission-unification/issues/01-fake-target-frame-id.md`는 이 항목으로 흡수해 close한다. **완료 기준:** DELTA 순서 — (01) `FakeTarget`에 현재 main frame id(기본 10, `sender()` 기본값과 일치)와 `replaceMainFrame(newFrameId)`(`"main-frame-navigation"` lifecycle을 함께 발생 — ADR 0015 "routingId는 retire 없이 바뀌지 않는다" 전제 유지)를 추가하고 현재 구조에서 교체 뒤 옛 frameId 요청 거부를 characterization test로 고정(현재 `sender-unauthorized`) → (02) `#admit`·verdict·채널 무관 사유 매핑·cancel 기록을 test-first로 넣는다(01 test 기대값은 `frame-not-main`으로 바뀐다, adapter handshake 검사는 유지) → (03) parse를 server로 옮기고 시그니처 변경·Symbol·adapter handshake 검사·version 분기를 삭제하며 `version-mismatch`·wire `VERSION_MISMATCH`를 test-first로 넣는다. 허용하는 test 변경은 F1(구조 오류 input 분류 변경 2건)·F2(handshake 호출부 17+1곳의 인자·반환값 기대)·F3(미attach handshake 사유 1건)·F5(Symbol·중복 방지 test 삭제)와 `2 as 1` cast 제거, version 기대값뿐이고 그 외 `test/` 내용 변경 0 → (04) 새 ADR 0016(admission 판정과 parse 소유), ADR 0010 머리와 §5·§14·§15 개정 표시, `docs/architecture.md`·`CONTEXT.md`를 새 구조와 맞춘다 → (05) 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, Electron acceptance, demo `test:unit`·`test:electron` 통과, `src/`·`test/`에서 `recordAdapterRejection` grep 0건. **결과:** DELTA-01~05 완료 — `FakeTarget`에 `mainFrameId`·`replaceMainFrame`을 추가하고 frame 교체 뒤 옛 frameId 거부를 characterization test로 고정(DELTA-01, 34 files/483→485 tests)한 뒤, `DocumentSessions`의 private `#admit(sender)` 하나가 disposed→attachment 없음→frame→origin 순서로 판정하고 `establish`·`current`가 `Admission` verdict를 반환하도록 모아 채널 무관 사유 매핑과 cancel 거부 기록을 test-first로 넣음(DELTA-02, 34 files/500 tests, `document-sessions.ts`의 `isAllowedOrigin`·`isCurrentMainFrame` 호출 각 1건으로 통합) → envelope parse(version 포함)를 server로 옮기고 신규 `protocol-error.ts`(타입 전용 import만)로 `protocolError` helper를 분리, `recordAdapterRejection` Symbol·adapter의 frame/origin 검사·server의 도달 불가 version 분기를 삭제, `version-mismatch` 4채널 기록과 RPC wire `VERSION_MISMATCH`를 test-first로 넣음(DELTA-03, 34 files/506 tests, F1은 Symbol 대신 depth-4 중첩 객체로 대체, 재번들 preload에 rxjs·createBridgeServer grep 0건으로 TRP-002 미발생 확인) → 새 ADR 0016(상황/결정 4개/대안과 기각 사유 6개/한계/범위 밖/이전, `#admit` 순서와 사유 매핑 표 포함) 작성, ADR 0010 머리와 §5·§7·§14·§15·ADR 0015에 개정 표시, `docs/architecture.md`·`CONTEXT.md`(sender admission 용어 신규)에 반영, `_works/arch-review/01.html` 카드 4 완료 표시, `.scratch/sender-admission-unification/issues/01` close(DELTA-04) → 패키지 `xvfb-run -a pnpm verify`(check-types·test·build) exit 0로 34 files/506 tests 재확인, 루트 `pnpm lint`·`pnpm format:check` 통과, Electron acceptance `bridge.electron.test.ts` 3/3·`multi-window.electron.test.ts` 8/8(합계 11/11, `authorize-denied`·`rpc-limit` 진단 사유 단언 그대로 통과, 정상 요청에서 `frame-not-main`·`origin-not-allowed` 오탐 없음), demo `test:unit` 10 files/24 tests·`test:electron` 1 file/4 tests(1회 통과), grep 완료 기준 전부 재확인(DELTA-05). **편차:** Renderer handshake 실패 문구 1건 — DELTA-03이 `create-renderer-api.ts`의 중복 사전 검사를 없애면서 "Unsupported bridge handshake."가 사라졌고, 리뷰에서 `VERSION_MISMATCH`일 때만 이 문구로 복원했다. 올바른 3키 모양에 clientId·manifest만 잘못된 handshake는 이전 "Unsupported"에서 이제 "Malformed"다. code는 계속 `INTERNAL`이고 문구는 계약이 아니다. 리뷰에서 `withEnvelope`가 body의 `protocolVersion`·`clientId`를 envelope 값으로 덮어쓰게 고쳤다(옛 preload 조립 순서와 같다 — 현재 호출자는 이 필드를 넘기지 않아 wire 값 변화 없음). TRP-001 `Controller window missing` flake는 이번 검증에서 발동하지 않았다. soak(`test:soak`)은 필수 아님으로 미실행. RD-018 완료. 병합은 이 결과와 별개로 판단한다.

### 와이어 형식 단일 소유 (출처: 아키텍처 리뷰 `_works/arch-review/01.html` 후보 05)

- [x] **RD-019 — 와이어 형식 정의를 `src/protocol/`이 소유하고 preload→main 값 import를 없앤다.** 지금 Electron 채널 이름과 기본 namespace가 `src/main/electron-adapter.ts:22-49`에 있어 preload(`expose-bridge.ts:21-24`)가 `../main/electron-adapter.js`를 값으로 import한다 — TRP-002(preload 번들이 server·`rxjs`를 끌어오는 함정)의 원인이고 막는 장치는 사람의 기억과 build 뒤 grep뿐이다. `protocolVersion: 1` literal이 `src/` 10곳(preload 4, server 1, `protocol-error` 1, `rpc-requests` 1, `subscriptions` 3)에 복제됐다. `parse*` 9개가 받는 `limits`는 호출자 5곳(preload, `rpc-client`, `stream-multiplexer`, `create-renderer-api`, server `envelopeLimits`) 모두 같은 MAX_SAFE_INTEGER 상수라 값이 바뀌지 않는 shallow 인자다. opaque ID 문법은 생성(`renderer/ids.ts`)과 파싱(`protocol/opaque-id.ts`)이 다른 파일에 있다. **구조:** (a) `ElectronBridgeChannels`·`DEFAULT_ELECTRON_BRIDGE_NAMESPACE`·`ELECTRON_BRIDGE_CHANNELS`를 `src/protocol/electron-channels.ts`(런타임 import 없는 leaf)로 옮긴다. `./protocol` 공개 export에는 넣지 않고(transport 중립 공개 표면 유지), `/main`은 re-export로 공개 이름을 유지한다(ADR 0013). eslint `@typescript-eslint/no-restricted-imports`(`allowTypeImports`)로 `src/{preload,protocol,renderer}/**`의 `src/main/*` 값 import를 금지하고 TRP-002를 RESOLVED로 바꾼다. (b) `PROTOCOL_VERSION` 상수와 범용 `withEnvelope(clientId, body)`를 `protocol/messages.ts`에 두고 공개한다. 메시지별 builder는 만들지 않는다(메시지 모양은 TS 타입이 강제한다). (c) `parse*` 9개에서 `limits` 인자를 제거하고 protocol 내부 `ENVELOPE_LIMITS` 하나로 대체한다(값 불변, ADR 0016 결정 2 근거 주석을 함께 옮긴다). `parseBridgeValue(value, limits)`는 그대로다. (d) pure `formatOpaqueId(nonce, scope, sequence)`를 `protocol/opaque-id.ts`에 두고, nonce·sequence 상태는 `renderer/ids.ts`에 남긴다(protocol은 pure). `createOpaqueId`의 공개 경로·동작은 불변이다. 공개 시그니처 변경(`parse*`의 `limits` 제거)은 breaking이며 npm 배포 이력이 없으므로 호환 별칭을 두지 않는다. **관측 가능한 동작 변경 없음.** 범위 밖: clientId 형식(`newClientId`) 이동, `test/`·`apps/`의 `protocolVersion: 1` literal, 후보 06(loopback adapter). **완료 기준:** DELTA 순서 — (01) 채널 이동·eslint guard(이동 전 lint RED, 이동 후 GREEN)·TRP-002 RESOLVED, `test/` 변경은 `test/preload/expose-bridge.test.ts` import 경로 1곳뿐 → (02) `PROTOCOL_VERSION`·`withEnvelope`로 `src/` literal 10곳 대체, `test/` 내용 변경 0 → (03) `limits` 인자 제거와 상수 5벌 삭제, `test/` 변경은 `test/protocol/messages.test.ts`의 인자 제거뿐 → (04) `formatOpaqueId`를 round-trip test-first로 추가 → (05) ADR 0013 개정 절, ADR 0016·ADR 0010 §14 개정 표시, `docs/architecture.md`, `_works/arch-review/01.html` 카드 5 → (06) 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, Electron acceptance, demo `test:unit`·`test:electron` 통과. grep 기준: `src/{preload,protocol,renderer}`의 `from "../main` 값 import 0건, `src/`의 `protocolVersion: 1` 0건, `Number.MAX_SAFE_INTEGER`는 `protocol/messages.ts`·`renderer/ids.ts`뿐, preload 번들 chunk의 `rxjs`·`bindElectronBridge`·`createBridgeServer` 0건. 계획: `_works/20260924-13-protocol-wire-ownership/`. **결과:** DELTA-01~05 완료 — `ElectronBridgeChannels`·`DEFAULT_ELECTRON_BRIDGE_NAMESPACE`·`ELECTRON_BRIDGE_CHANNELS`를 `src/protocol/electron-channels.ts`로 옮기고 eslint `no-restricted-imports` guard로 `src/{preload,protocol,renderer}`의 `src/main/*` 값 import를 차단, TRP-002 RESOLVED(DELTA-01) → `PROTOCOL_VERSION`·`withEnvelope(clientId, body)`로 `src/` `protocolVersion: 1` literal 10곳을 대체(DELTA-02) → `parse*` 9개에서 `limits` 인자를 없애고 protocol 내부 `ENVELOPE_LIMITS`로 대체, 호출자 5벌의 MAX_SAFE_INTEGER 상수 삭제(DELTA-03) → pure `formatOpaqueId(nonce, scope, sequence)`를 `protocol/opaque-id.ts`에 추가하고 `renderer/ids.ts`의 `createOpaqueId`가 형식 생성을 위임, round-trip test-first로 확인(DELTA-04) → ADR 0013(채널 정의 위치)·ADR 0016(envelope limits 정의 위치, `protocol-error.ts` 분리 근거)·ADR 0010 §14 개정 표시, `docs/architecture.md`·TRP-002·`docs/traps/INDEX.md`·`_works/arch-review/01.html` 카드 5 갱신(DELTA-05). 전체 검증(DELTA-06): 패키지 `xvfb-run -a pnpm verify`(check-types·test·build) exit 0로 34 files/509 tests, 루트 `pnpm lint`·`pnpm format:check` 통과, preload dist가 참조하는 모든 chunk에서 `rxjs`·`bindElectronBridge`·`createBridgeServer` grep 0건, grep 기준(`../main` 값 import·`protocolVersion: 1`·`Number.MAX_SAFE_INTEGER`·opaque ID 문자열 조립) 전부 재확인 통과, Electron acceptance `bridge.electron.test.ts` 3/3(`:123`행 채널 이름 단언 포함)·`multi-window.electron.test.ts` 8/8(합계 11/11, sandbox preload 로드 성공), demo `test:unit` 10 files/24 tests·`test:electron` 1 file/4 tests(1회 통과), 각 DELTA의 기존 test 변경이 계획 허용 목록 안임을 diff로 확인. **편차:** Renderer handshake 실패 문구 1건 — DELTA-03이 `create-renderer-api.ts`의 중복 사전 검사를 없애면서 "Unsupported bridge handshake."가 사라졌고, 리뷰에서 `VERSION_MISMATCH`일 때만 이 문구로 복원했다. 올바른 3키 모양에 clientId·manifest만 잘못된 handshake는 이전 "Unsupported"에서 이제 "Malformed"다. code는 계속 `INTERNAL`이고 문구는 계약이 아니다. 리뷰에서 `withEnvelope`가 body의 `protocolVersion`·`clientId`를 envelope 값으로 덮어쓰게 고쳤다(옛 preload 조립 순서와 같다 — 현재 호출자는 이 필드를 넘기지 않아 wire 값 변화 없음). TRP-001 `Controller window missing` flake는 이번 검증에서 발동하지 않았다. soak(`test:soak`)은 필수 아님으로 미실행. RD-019 완료. 병합은 이 결과와 별개로 판단한다.

### Transport test adapter (출처: 아키텍처 리뷰 `_works/arch-review/01.html` 후보 06)

- [x] **RD-020 — `BridgeTransport` seam에 in-process loopback adapter를 test 전용 subpath로 제공한다.** 지금 `BridgeTransport`의 실제 adapter는 preload 하나뿐이다. demo test는 envelope·opaque ID·wire key를 손으로 쓴다 — `composition.test.ts`의 `"test:subscription:N"`·`` `rpc:relay/${operation}` ``·manifest literal, `non-hardware-domain.test.ts`의 `"rpc:notes/append"`, `use-remote-state.test.tsx`의 수기 transport·`subscribed`/`batch`/`complete`, `main-monitor.test.tsx`의 manifest literal("`AppBridge`는 타입 계약이라 런타임 manifest를 만들지 못한다"). RD-017·019처럼 wire 형식이 바뀌면 이 test들이 함께 바뀐다. **구조:** 새 subpath `@cp949/rx-bridge-electron/testing`의 `createLoopbackTransport(server, { sender?, clientId?, role? })`가 고정 target으로 `server.attach`를 호출하고 `BridgeTransport & { dispose() }`를 반환한다. 양방향 `structuredClone`, preload와 같은 `withEnvelope`·`parseStreamMessage`, stream 전달과 `control`·`cancel`은 microtask로 미룬다. server throw는 폴백 없이 드러낸다. `dispose`는 detach만 한다. `./testing`은 main을 type으로만 import한다. demo test 4개를 loopback + 실제 server + 타입 붙은 api로 이전하고, wire 형식 자체를 단언하던 것은 package `test/main`으로 옮기거나 중복이면 삭제한다. `FakeTransport`는 유지한다. **완료 기준:** 새 ADR 0017, ADR 0001 교차 참조, 전용 test(clone·microtask 순서·dispose·sender 분리·throw 노출), demo test 4개의 수기 wire 문자열 grep 0건(manifest 공개 목록 test는 별도 확인), `dist/testing`의 electron 의존 0건, 패키지 `pnpm verify`·demo `test:unit`·`test:electron` 통과. **결과:** DELTA-01~05 완료 — `createLoopbackTransport`·`LoopbackTransport`·`LoopbackTransportOptions`를 `src/testing/`에 test-first로 추가(그릴링 결정 3·4·5·6·8·9·11·13·15, tsup entry·`./testing` exports·eslint `no-restricted-imports` guard 대상 추가, 전용 test 10건, DELTA-01, 패키지 34→35 files/512→522 tests) → demo `main-monitor.test.tsx`·`use-remote-state.test.tsx`의 수기 manifest literal·수기 transport·수기 stream envelope를 loopback + `createDemoComposition()`/소형 server + 타입 붙은 api로 대체, "세대 1개" 단언은 upstream 구독 횟수 카운터로 재구성(DELTA-02, demo test 수 10 files/24 tests 불변) → `composition.test.ts`·`non-hardware-domain.test.ts`를 `connectWindow` helper + loopback + 타입 붙은 api로 재작성, manifest 공개 목록 test는 `Object.keys(api...)` 순회로 wire key 문자열 없이 표현 가능함을 확인해 완료 조건 grep 예외(사용자 확인 분기)가 발동하지 않음(DELTA-03, 이동/삭제할 wire 단언 없음 — 패키지 test 수 522 그대로) → ADR 0017 신규 작성(맥락·결정 9개·거부한 대안 7개), ADR 0001·0016 교차 참조, `docs/architecture.md`·패키지 README "Testing" 절 갱신, `_works/arch-review/01.html` 카드 06 완료 표시(DELTA-04) → 전체 검증(DELTA-05): 패키지 `xvfb-run -a pnpm verify`(check-types·test·build) exit 0로 35 files/522 tests(기준 34/512 + loopback 전용 10건, DELTA-02·03은 package test 미추가 — 실측 일치), 루트 `pnpm lint`·`pnpm format:check` exit 0, demo `check-types`·`test:unit`(10 files/24 tests) exit 0, demo `test:electron` 1회 통과(4/4, TRP-001 미발동), Electron acceptance `bridge.electron.test.ts`·`multi-window.electron.test.ts` 2 files/11 tests 통과, `dist/testing/index.js`와 그 유일한 import 대상 `dist/chunk-XK5G3DQ7.js` 양쪽 `electron`·`bindElectronBridge`·`ipcMain` grep 0건, demo test 4개 grep 기준(`protocolVersion`·`"test:subscription:`·wire key·`: BridgeTransport = {` 등) 0건, `main-monitor.test.tsx`의 manifest literal·"런타임 manifest를 만들지 못한다" 주석 grep 결과 삭제 확인, `git diff dev --stat -- packages/rx-bridge-electron/test apps/demo/test`가 신규 파일 1개(`test/testing/loopback-transport.test.ts`) + 기존 4개 demo test 파일 수정만으로 허용 목록과 일치. **편차:** 없음. `FakeTransport`는 계획대로 유지, `test:soak`는 범위 제외로 미실행. TRP-001 `Controller window missing` flake는 이번 검증에서 발동하지 않았다. RD-020 완료. 병합은 이 결과와 별개로 판단한다. **리뷰 교정:** (1) `connect()`가 server의 handshake 거부 응답(`RpcResponse`)을 `HandshakeResponse`로 캐스트해 resolve하던 것을 preload처럼 `parseHandshakeResponse`에서 reject하도록 고치고, 요청·응답 검사도 preload와 같은 `parseRendererRpcRequest`·`parseRendererStreamCommand`·`parseRpcResponse`로 맞췄다(계획 결정 P2의 전제 "preload도 main 응답을 해석하지 않는다"가 사실과 달랐다). (2) 전용 test에 parse 경계 3건(이전 구현에서 RED)·cancel 전달·dispose 후 control/stream 차단을 추가(10→14건, 패키지 35 files/526 tests). (3) demo `use-remote-state`의 "세대 1개" 단언이 rerender 직후 동기로 upstream 구독 횟수를 세어 재구독 회귀에서도 통과하던 것을 macrotask 경계 뒤로 옮겼다(재구독 회귀 주입 시 `expected 2 to be 1` 확인). (4) TRP-005의 "간헐 실패" 서술을 결정적 누락으로 교정, TRP-006 신호를 새 reject 동작에 맞췄다. (5) `docs/traps/INDEX.md` prettier 위반(DELTA-05 이후 커밋에서 유입)을 고쳤다.

### Renderer 진입 모듈 봉인 (출처: 아키텍처 리뷰 `_works/arch-review/01.html` 후보 07)

- [x] **RD-021 — Renderer 공개 인터페이스에서 내부 `RpcClient`를 봉인하고 test를 `createRendererApi` seam으로 옮긴다.** 지금 `@cp949/rx-bridge-electron/renderer`는 내부 클래스 `RpcClient`·`RpcClientOptions`를 공개한다(`src/renderer/index.ts:5`). src 밖 사용처는 `test/renderer/rpc-client.test.ts`의 `new RpcClient` 18건뿐이라 test가 공개 seam 너머를 검증한다. `RpcClientOptions.defaultTimeoutMs`는 `createRendererApi`가 options를 넘기지 않아(`create-renderer-api.ts:309`) 공개 경로로 도달할 수 없다. `HandshakeWithManifest`(`transport.ts:27`)는 `HandshakeResponse`와 같은 모양이고 `fake-transport.ts`만 쓴다. handshake 처리에서 `addPath`의 충돌 throw 2곳(`create-renderer-api.ts:113-115,123-125`)은 도달할 수 없다(추론: 중복은 `OperationPathTrie`가 먼저, leaf/namespace는 예약어 검사가 먼저 거부). `parseHandshake`가 만드는 frozen `manifest` 사본(`:178-193`)은 쓰이지 않는다. 로컬 `RemoteError` helper가 3벌이다(`create-renderer-api` `internal`, `rpc-client` `localError`, `stream-multiplexer` `internal`). 카드의 "handshake 3중 검사"는 RD-017·019 이후 필요한 2겹(preload IPC 경계, custom transport 경계)과 도달 불가 1겹(`addPath`)이다. **구조:** `RpcClient`·`RpcClientOptions`·`HandshakeWithManifest` 공개 export를 제거한다(breaking — npm 배포 이력 없음, ADR 0005/0007과 같은 근거로 호환 별칭 없음). `defaultTimeoutMs` option을 삭제하고 기본값은 내부 상수 30초로 고정한다. `addPath`는 삽입만 하고 trie가 유일한 충돌 판정자가 된다. 미사용 `manifest` 사본을 삭제한다. 로컬 에러 helper를 `remote-error.ts`의 `localError(code, message)` 1벌로 합친다(문구는 호출 지점 유지, 비공개). Renderer RPC race·dispose test는 `createRendererApi(FakeTransport)` 경유로 옮기고 handshake·Proxy test는 `create-renderer-api.test.ts`로, 나머지는 `rpc-calls.test.ts`로 둔다. `createOpaqueId`는 custom transport용 공개 API라 유지한다. **관측 가능한 런타임 동작 변경 없음.** 새 ADR 없음. 범위 밖: Proxy → frozen tree(`.scratch/renderer-proxy-frozen-tree`), `PublicManifest ≡ HandshakeManifest` 통합, `create-renderer-api.ts` 파일 분할, 후보 08. **완료 기준:** DELTA 순서 — (01) test 재배치·seam 이전, src 변경 0, 단언 기대값 변경 0, `test/`의 `RpcClient` grep 0건, 결함 주입으로 이전 test RED 1회 확인 → (02) 공유 충돌 case 5행을 Renderer seam characterization test로 추가(현재 코드에서 trie 문구로 GREEN — RED면 멈추고 보고) 후 `addPath` throw·미사용 `manifest` 삭제 → (03) 공개 runtime export 목록 test(`createRendererApi`·`createOpaqueId`·`RemoteError`)를 RED로 시작, 기본 30초 timeout characterization test 추가, export 제거·`defaultTimeoutMs` 삭제, `dist/renderer/index.d.ts`의 `RpcClient`·`HandshakeWithManifest` 0건 → (04) 에러 helper 통합, `test/` diff 0 → (05) `_works/arch-review/01.html` 카드 07 완료·정정 → (06) 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`·`test:electron`, Electron acceptance 통과, `apps/demo` 변경 0. 계획: `_works/20260924-15-renderer-entry-seal/`. **결과:** DELTA-01~04 완료 — test 재배치·seam 이전: `rpc-client.test.ts`를 `rpc-calls.test.ts`(18 tests)와 `create-renderer-api.test.ts`(20 tests, 합 38 = 이전 32+6)로 나누고 `new RpcClient` 18건을 `createRendererApi(FakeTransport)` 경유로 옮겼다. src 변경 0, 기존 단언 0 diff, 결함 주입(`cancelOnce`의 `if (settled) return;` 가드 제거)으로 RED 1회 확인(DELTA-01, 패키지 35 files/526 tests — baseline과 동일) → handshake 정리: 공유 충돌 case 5행을 characterization test로 추가해 GREEN 확인(trie가 `addPath`보다 먼저 거부한다는 추론 실측 확인, RED 아님) 후 `addPath`의 leaf/namespace·duplicate throw 2곳과 `parseHandshake`의 미사용 `manifest` 사본·`PublicManifest` import를 삭제(DELTA-02, 531 tests = 526+5) → 공개 export 봉인: export 목록 test를 RED로 시작해 `RpcClient`·`RpcClientOptions`·`HandshakeWithManifest`를 제거하고 GREEN 확인, `defaultTimeoutMs` option을 삭제해 기본값을 내부 상수 30초로 고정(29,999ms 미확정·30,000ms `DEADLINE_EXCEEDED` characterization test로 확인), `dist/renderer/index.d.ts` grep 0건(DELTA-03, 533 tests = 531+2, `pnpm build` 통과) → 에러 helper 통합: `create-renderer-api`·`rpc-client`·`stream-multiplexer`의 `internal`/`localError` 3벌을 `remote-error.ts`의 `localError(code, message)` 1벌로 합치고 code·message literal 불변을 diff로 확인(DELTA-04, 533 tests 유지). 문서: ROADMAP 본 항목 갱신, `_works/arch-review/01.html` 카드 07 완료 표시(DELTA-05) → 전체 검증: 패키지 `xvfb-run -a pnpm verify`(35 files/533 tests) exit 0, 루트 `pnpm lint`·`pnpm format:check` exit 0, demo `check-types`·`test:unit`(24 tests)·`xvfb-run -a pnpm test:electron`(4 tests, flake 없음) 통과, Electron acceptance(11 tests) 통과, grep 재확인 전부 0건/일치, `apps/demo` 변경 0 확인(DELTA-06). **편차:** DELTA-01의 결함 주입에서 계획이 제시한 두 대안 중 `cancelOnce`의 `cancellationSent` guard 단독 제거는 RED를 만들지 못했다 — 계획이 함께 제시한 다른 대안(`if (settled) return;` 가드 제거)으로 RED를 확인했다(계획 범위 내). README 항목 번호는 계획의 8이 아니라 10이다(RD-018·019가 8·9를 먼저 채웠다). DELTA-04 파일 3개의 prettier 위반은 DELTA-04 커밋에서 함께 고쳤다(로직·literal 변경 0). **리뷰 교정:** (1) 위 편차의 원인 서술("abort listener가 `{ once: true }`라 중복 트리거 불가")이 틀렸다 — 두 번째 `cancelOnce`는 `settled` 검사에서 먼저 반환하고, `cancellationSent`는 `transport.cancel`이 같은 호출의 abort를 동기로 재진입시킬 때만 두 번째 `cancel` 전송을 막는다. 이 경로를 지키는 test가 없어 guard를 지워도 전체가 통과했다 — `rpc-calls.test.ts`에 재진입 test를 추가했다(guard 제거 시 RED 확인, 패키지 35 files/534 tests). (2) DELTA-02 characterization test의 주석·제목이 `addPath` throw 삭제 전 시점 서술("삭제하면 안 된다", "not addPath")로 남아 있어 현재 불변식(trie가 유일한 충돌 판정자) 서술로 고쳤다. `addPath` 주석에 카테고리 segment를 끼워도 새 충돌이 없는 이유를 적고, `operation-key-cases.ts` 머리 주석에 Renderer 사용처를 추가했다.

### Renderer stream client 층 합치기 (출처: 아키텍처 리뷰 `_works/arch-review/01.html` 후보 08)

- [x] **RD-022 — Renderer stream client의 얕은 층을 local generation 모듈 하나로 합친다.** 지금 State/Event 구독 하나를 이해하려면 파일 4개(`remote-state.ts`·`remote-event.ts`·`local-generation.ts`·`stream-multiplexer.ts`)를 오가야 한다. `RemoteEvent`(15줄)는 `LocalGeneration`을 감싸기만 한다. `let subscribeLocal!` 트릭은 `RemoteEvent`와 `RemoteStateClient`(`remote-state.ts:17-30`)에 2벌 있다. `LocalGenerationPolicy`(`onOpen`·`beforeNext`·`onClose`·`replayLatest`)를 쓰는 곳은 `RemoteStateClient`와 `local-generation.test.ts`뿐이다. 그래서 snapshot 상태기계가 policy 콜백 3개로 흩어져 있다. "dispose 뒤 subscribe" 검사는 `LocalGeneration.subscribe`(`local-generation.ts:48`)와 `StreamMultiplexer.open`(`stream-multiplexer.ts:65-69`) 두 층에 있다. 뒤쪽은 유일한 호출자를 거치면 도달할 수 없다(추론: 앞쪽 검사와 `open` 호출 사이에 사용자 코드가 동기로 실행되지 않는다). `local-generation.test.ts`(1 test)만 공개 seam 너머를 검증한다. 같은 시나리오는 공개 test가 state+complete(`remote-state.test.ts:451`)와 event+error(`remote-event.test.ts:212`)로 덮는다. 공개 test가 없는 조합은 event+complete 하나다. 카드의 Speculative 근거 "변경 압력 낮다"는 틀렸다. 첫 commit 이후 commit 5건이 모두 이 파일들을 건드렸다. **구조:** `local-generation.ts`가 `createRemoteState(multiplexer, key)`·`createRemoteEvent(multiplexer, key)` 두 factory만 export한다. `LocalGeneration`은 kind(`"state" | "event"`)를 받는다. snapshot 전이(`uninitialized`→`connecting`→`current`→`stale`)와 늦은 구독자 replay는 kind가 `"state"`일 때만 동작한다. `RemoteStateClient`는 같은 파일의 비공개 껍데기(`snapshot` getter)다. generation을 `super()` 전에 지역 변수로 만들어 `subscribeLocal!` 트릭을 없앤다. `LocalGenerationPolicy`·`remote-event.ts`·`remote-state.ts`·`local-generation.test.ts`를 삭제한다. `StreamMultiplexer.open`의 disposed 분기를 삭제하고, dispose 검사는 `LocalGeneration` 1곳만 남긴다(fa8e7ff 불변식). `renderer/index.ts`는 `RemoteState`·`RemoteStateSnapshot` 타입을 `../contract/index.js`에서 re-export한다. **관측 가능한 런타임 동작 변경 없음. 공개 export 변경 없음. 새 ADR 없음.** ADR 0003 전달 의미는 공개 test가 지킨다. 범위 밖: Proxy → frozen tree(`.scratch/renderer-proxy-frozen-tree`), `StreamMultiplexer` 내부(sequence·ack·session 대조), `create-renderer-api.ts` 파일 분할, ADR 0017:77의 test 파일 목록(결정 당시 기록이므로 유지). **완료 기준:** DELTA 순서로 진행한다.
  - (01) event+complete 재구독 test를 공개 seam에 추가한다(GREEN, 결함 주입으로 RED 1회 확인). 그다음 `local-generation.test.ts`를 삭제한다. src 변경 0.
  - (02) `open`의 disposed 분기를 삭제한다. 결함 주입으로 `LocalGeneration`의 검사를 지웠을 때 `renderer-dispose.test.ts`가 RED가 되는지 확인한다. test diff 0.
  - (03) factory 2개로 합친다. test diff 0. `src/`의 `LocalGenerationPolicy`·`\bRemoteEvent\b`·`subscribeLocal` grep 0건.
  - (04) 카드 08 완료 표시와 근거 정정, ROADMAP 결과를 기록한다.
  - (05) 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`·`test:electron`, Electron acceptance가 통과한다. `apps/demo` 변경 0.

  계획: `_works/20260924-16-stream-client-collapse/`. **결과:** DELTA-01~05 완료 — (01) event+complete 재구독 공개 seam test를 `remote-event.test.ts`에 추가해 GREEN 확인(패키지 35 files/536 tests) 후 `local-generation.test.ts` 삭제(34 files/535 tests, baseline 35/535에서 파일 −1·test +1−1), 결함 주입(`#finish`에서 `this.#generation = undefined`를 terminal 통지 뒤로 이동)으로 RED 3건 확인 — `remote-event.test.ts`의 `opens a fresh generation when an error callback subscribes again`·`opens a fresh generation when a complete callback subscribes again`(신규), `remote-state.test.ts`의 `opens a fresh generation when a terminal callback subscribes again` → (02) `StreamMultiplexer.open`의 disposed 분기(구 `:65-69`) 삭제, `createDisposedError` import 제거, test diff 0(34/535 유지), 결함 주입(`LocalGeneration.subscribe`의 disposed 검사 제거)으로 RED 4건 확인 — `renderer-dispose.test.ts`의 `:187`·`:247`·`:311`·`:340`(계획 예상 3건에 `:187` 추가 발견) → (03) `local-generation.ts`를 `createRemoteState`·`createRemoteEvent` factory 2개로 다시 쓰고 `LocalGenerationPolicy`·`subscribeLocal!` 트릭 2벌·`RemoteEvent`·`remote-state.ts`를 삭제, `create-renderer-api.ts`·`index.ts` 갱신, test diff 0(34/535 유지), `pnpm build` 성공·`dist/renderer/index.d.ts` 타입 export 유지, 결함 주입(`#finish`의 snapshot 비활성화를 terminal 통지 뒤로 이동)으로 RED 1건 확인 — `remote-state.test.ts`의 `opens a fresh generation when a terminal callback subscribes again`(`:451`) → (04) `_works/arch-review/01.html` 카드 08 완료 표시·근거 정정 → (05) 전체 검증 — 패키지 `pnpm build` 성공 후 `xvfb-run -a pnpm verify`(check-types·test·build) exit 0, 34 files/535 tests(DELTA-03과 일치). 루트 `pnpm lint`·`pnpm format:check` exit 0. demo `pnpm check-types` exit 0, `pnpm test:unit` 10 files/24 tests 통과, `xvfb-run -a pnpm test:electron` 1 file/4 tests 1회 통과(flake 없음, 재실행 불필요). Electron acceptance `xvfb-run -a pnpm vitest run test/electron/bridge.electron.test.ts test/electron/multi-window/multi-window.electron.test.ts` 2 files/11 tests 통과. 파일 부재 3건(`remote-event.ts`·`remote-state.ts`·`local-generation.test.ts`) 확인, grep 0건(`LocalGenerationPolicy`·`subscribeLocal`·`\bRemoteEvent\b`·`beforeNext`·`replayLatest`·test의 `LocalGeneration`·`StreamMultiplexer`), `.disposed` 읽기 `local-generation.ts:54` 1곳뿐(`stream-multiplexer.ts`는 getter 정의와 `#disposed` 내부 필드만 남음), `local-generation.ts` export `createRemoteState`·`createRemoteEvent` 2개뿐, `dist/renderer/index.d.ts`에 `RemoteState`·`RemoteStateSnapshot` re-export 유지, 공개 runtime export 목록 test(`create-renderer-api.test.ts` "public runtime export surface is sealed to createRendererApi, createOpaqueId, RemoteError") 무변경 통과(535 test에 포함). `git diff dev --stat -- packages/rx-bridge-electron/test apps/demo` → `local-generation.test.ts` 삭제(72줄) + `remote-event.test.ts` 추가(29줄) 2 files changed뿐, `apps/demo` 변경 0(DELTA-01 허용 목록과 일치, dev 기준 c167cf4). **편차:** 없음(각 DELTA 계획대로 진행. checklist.md의 grep 패턴 오탈자 `RemoteEvent\b` → `\bRemoteEvent\b` 수정은 검증 스크립트 오탈자 교정이며 완료 조건 완화가 아니다 — DELTA-03 "## 결정" 참고).

### 잔재 정리 (출처: 아키텍처 리뷰 `_works/arch-review/01.html` 잔재 목록)

- [x] **RD-023 — 아키텍처 리뷰 잔재 목록 중 삭제·이름 정리로 끝나는 항목을 치운다.** 잔재 목록 11건 중 2건(`isScopedSource` RD-015, `HandshakeWithManifest` RD-021)은 이미 처리됐고 9건이 남았다: `registration.ts`의 `RegistrationEntry` union(사용처 0)·entry `path` tuple(읽는 곳 0)·DELTA/descriptor 이력 주석, `sources.ts`의 같은 이력 주석, 제거된 `InferBridge` 이름을 딴 `contract/infer.ts`, `PublicManifest`(`contract/manifest.ts`) ≡ `HandshakeManifest`(`protocol/messages.ts`), test만 부르는 `BoundedQueue.drain()`·`DocumentSessions.retiredClientCount()`, test fake 복제본(`FakeIpcMain` 2벌, WebContents fake 3벌), `CONTEXT.md`가 `BridgeApi<B>`를 Renderer 공개 타입이라 부르는 것(소비자는 `RendererApi<B>`). **구조:** 미사용 타입·필드를 삭제하고 이력 주석을 현재 동작 서술로 바꾼다. `infer.ts`를 `remote-state.ts`로 바꾼다. `manifest.ts`를 삭제하고 `contract`가 `HandshakeManifest`를 `PublicManifest`로 re-export한다(wire 형식 정의는 protocol 소유, RD-019). test 전용 method 2개를 삭제하고 test는 `shift` 반복·`establish` 판정으로 같은 불변식을 관찰한다. fake는 `test/main/fake-ipc.ts` 1벌로 모은다. **관측 가능한 런타임 동작 변경 없음. 공개 export 이름 변경 없음. 새 ADR 없음.** 범위 밖(보류): `contract/bridge-types.ts`의 `main/sources`·`main/types` type import(type-only라 런타임 0이고 eslint guard가 허용한다 — 해소는 `BridgeImpl`이 참조하는 타입의 소유 위치를 정하는 설계 결정), `authorize` key 형식(`.scratch/authorize-key-shape`, 공개 계약 변경이라 ADR 필요). **완료 기준:** 패키지 `xvfb-run -a pnpm verify`(test 수 baseline 34 files/535 tests 유지), 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`·`test:electron`, Electron acceptance 통과, 삭제 대상 grep 0건, `dist/contract/index.d.ts`의 `PublicManifest`·`RemoteState`·`RemoteStateSnapshot` export 유지, `apps/` 변경 0.

  계획: `_works/20260924-17-leftover-cleanup/`. **결과:** DELTA-01~06 완료 — (01) `RegistrationEntry`·`path` 정의 3곳·대입 3곳 삭제, `registration.ts`·`sources.ts` 이력 주석을 현재 서술로 교체(test diff 0, 34/535) → (02) `infer.ts` → `remote-state.ts`, `manifest.ts` 삭제·`HandshakeManifest as PublicManifest` re-export, `manifestFromTable` 반환 타입 `HandshakeManifest`(test diff 0, `dist/contract/index.d.ts` export 3종 확인) → (03) `drain()`·`retiredClientCount()` 삭제, retired id 보존 test 5건을 `establish` 판정으로 재작성(개수 불변). 결함 주입 2건 — `destroyed` 집합 삭제 제거·eviction `>`→`>=` — 각각 2건 RED 확인 후 복구. 격리 test 초안은 `destroyed`가 현재 세션을 retire하며 c1을 밀어내 주입 (a)에서 GREEN으로 남았고, 관찰 id를 c3로 바꿨다 → (04) `FakeIpcMain`·`FakeWebContents(id=1, routingId=10, url="app://local")`를 `fake-ipc.ts`로 모으고 `UrlWebContents`·`FakeContents` 삭제(단언 변경 0) → (05) `CONTEXT.md`에 `RendererApi<B>` 서술 추가, 01.html 잔재 목록 표시 → (06) 패키지 `xvfb-run -a pnpm verify` exit 0(34 files/535 tests), 루트 `pnpm lint`·`pnpm format:check` exit 0, demo `check-types` exit 0·`test:unit` 10 files/24 tests·`xvfb-run -a pnpm test:electron` 4/4(TRP-001 미발동), Electron acceptance 2 files/11 tests, grep·파일 부재 확인, `apps/` diff 0. **편차:** 없음. `test:soak`는 범위 제외로 미실행.

- [x] **RD-024 — `authorize`의 두 번째 인자를 wire key 문자열에서 구조화 객체 `BridgeOperation`으로 바꾼다.** 지금 `Authorize = (context, operationId: string)`은 wire key(`category:domain/op`)를 받고 형식은 공개 문서에 없다. 사용자 코드가 문자열을 직접 자른다 — demo `apps/demo/src/main/composition.ts:20-23` `key.startsWith("state:") || key.startsWith("event:")`, README 예시 `!operation.startsWith("rpc:")`. 호출 지점(`rpc-requests.ts`·`subscriptions.ts`)은 등록 조회 뒤라 등록 entry(카테고리·도메인·operation)를 이미 갖고 있다. **구조:** `Authorize = (context, operation: BridgeOperation)`로 교체한다(breaking — 외부 사용 이력 없음, 호환 인자·이전 안내 없음). `BridgeOperation { key, category: OperationCategory, domain: readonly string[], operation }`을 등록 시 entry마다 한 번 만들어 동결하고 RPC·stream 호출 지점이 그대로 넘긴다. `BridgeOperation`·`OperationCategory`는 `./main`에서 type export한다. 진단 이벤트 `key`는 문자열 그대로, `protocol/operation-key.ts`는 비공개 유지. 결정은 [ADR 0018](docs/adr/0018-authorize-structured-operation.md). 출처: `.scratch/authorize-key-shape`. **완료 기준:** RPC·state·event `authorize`가 동결된 `BridgeOperation`(중첩 도메인 포함)을 받는다는 test, 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`·`test:electron`, Electron acceptance 통과, demo·README의 `startsWith("rpc:"|"state:"|"event:")` grep 0건, `dist/main/index.d.ts`의 `BridgeOperation`·`OperationCategory` export, CONTEXT·architecture·README가 새 시그니처를 기술한다.

  계획: `_works/20260925-01-authorize-operation-shape/`. **결과:** DELTA-01~05 완료 — (01) ADR 0018, RD-024 등록, scratch 이슈 승격 → (02) `BridgeOperation`·`OperationCategory` 타입, 등록 entry의 동결 `bridgeOperation`, RPC·stream 호출 지점 교체, `./main` type export. 신규 `test/main/authorize-operation.test.ts` 5건(RPC·state·event·중첩 도메인·동결) 구현 전 5/5 RED → GREEN → (03) demo `category !== "rpc"`, multi-window `operation.key`, README 예시 교체. build 후에야 `TS2345`·`TS2339` 3건이 드러났다 → (04) CONTEXT·architecture·README 서술 → (05) 패키지 `xvfb-run -a pnpm verify` exit 0(35 files/542 tests, baseline 537 + 5), build 후 `check-types` exit 0, 루트 `pnpm lint`·`pnpm format:check` exit 0, demo `check-types` exit 0·`test:unit` 10 files/24 tests·`xvfb-run -a pnpm test:electron` 4/4(TRP-001 미발동), Electron acceptance 2 files/11 tests, `dist/main/index.d.ts` export 확인, grep 0건. TRP-004에 "`check-types`도 stale `dist/`로 통과한다"를 보정했다. **편차:** 없음. `test:soak`는 범위 제외로 미실행.

- [x] **RD-025 — navigation 세션 retire를 main-frame 문서 교체(commit) 시점으로 옮긴다.** 지금 Electron adapter는 main-frame `did-start-navigation`마다 세션을 retire한다(`electron-adapter.ts:78-90`, `isInPlace` 무시). Electron 44.4.5 실험에서 이 이벤트는 문서가 그대로 살아 있는 이동 — `history.pushState`·`location.hash`(`isSameDocument: true`), HTTP 204, 다운로드, `will-navigate` `preventDefault()`(이벤트가 `will-navigate`보다 먼저 온다) — 에서도 발생했고, main frame `routingId`는 실제 문서 교체에서만 바뀌었다. retire된 `clientId`는 같은 문서에서 재사용할 수 없으므로(`document-sessions.ts:95`) SPA가 pushState 한 번으로 bridge 전체를 잃는다(코드 추론 — 이 RD의 재현 test가 확인한다). **구조:** retire 시점을 main frame이 새 문서로 commit된 이벤트로 옮긴다. 구체 이벤트는 오류 페이지·reload·back/forward·`loadURL`·새 문서 IPC 순서 실험으로 고른다. 진단 사유 `main-frame-navigation`은 유지한다. ADR 0015의 근거 가설("`routingId`는 `did-start-navigation` 없이 바뀌지 않는다")을 선택 이벤트 기준으로 개정한다. 결정은 ADR 0019. 스트림 종료 통지(RD-026)보다 먼저 병합한다. **완료 기준:** pushState·hash·`will-navigate` 차단 뒤 같은 문서의 RPC·State가 계속 동작하는 Electron acceptance(수정 전 RED), 선택 이벤트 없이 `routingId`가 바뀐 실험 case 0건, 패키지 build 후 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`·`test:electron`, Electron acceptance 통과, ADR 0019·ADR 0015 개정·architecture·CONTEXT 반영.

  계획: `_works/20260925-02-navigation-commit-retire/`. **결과:** DELTA-01~05 완료 — (01) 살아 있는 문서 이동(pushState·hash·`will-navigate` 차단) 뒤 bridge가 멈추는 결함을 Electron acceptance로 재현(RED) → (02) commit 신호 실험, 일반 case는 `did-navigate` 단독으로 충분했으나 ERR_CONNECTION_REFUSED case에서 0건이 안 되어 `did-navigate`+`did-fail-load`(routingId 일치) 조합으로 확정(사용자 승인) → (03) `electron-adapter.ts`의 retire 감지를 `did-start-navigation`에서 조합 이벤트로 교체(GREEN), 단위 test·fake(`fake-ipc.ts`)를 commit 이벤트 기준으로 갱신 → (04) ADR 0019 신규, ADR 0015 근거 가설 개정, architecture·CONTEXT 서술 갱신 → (05) 패키지 build 후 `xvfb-run -a pnpm verify` exit 0(35 files/552 tests), 루트 `pnpm lint`·`pnpm format:check` exit 0, demo `check-types` exit 0·`test:unit` 10 files/24 tests exit 0·`xvfb-run -a pnpm test:electron` 4/4 1회 통과(TRP-001 미발동, 재실행 없음), Electron acceptance(`bridge.electron.test.ts`+`multi-window.electron.test.ts`) 2 files/14 tests, `did-start-navigation` grep 0건. **편차:** 204·다운로드 case는 fixture가 `file://`라 acceptance에 미포함(DELTA-02 실험 로그로 대체, checklist 허용 편차대로). `test:soak`는 범위 제외로 미실행.

- [x] **RD-026 — Main이 살아 있는 문서의 세션을 끝낼 때 스트림 종료를 Renderer에 통지한다.** 지금 세션이 retire되면 RPC는 `CANCELLED`(진행 중)·`FORBIDDEN`(이후)을 받지만, 스트림은 활성 구독(`#close`)·`authorize` 대기 구독·retire 뒤 새 subscribe(admission 거부는 진단만) 모두 Renderer에 아무것도 보내지 않아 조용히 멈춘다. 문서가 살아 있는 retire는 detach·`server.dispose()`·bind `dispose()`다. 프로토콜에는 `error`·`complete` 종료 메시지가 이미 있고 Renderer도 처리한다 — 새 메시지 종류·오류 코드·protocol version 변경은 없다. **구조:** `DocumentSessions`가 retire 사유를 `session.signal.reason`으로 싣는다. detach·dispose retire면 활성·`authorize` 대기 구독에 쌓인 값을 버리고 `error CANCELLED`를 즉시 보낸다. subscribe admission 거부(`sender-unauthorized`·`frame-not-main`·`origin-not-allowed`)는 `error FORBIDDEN "Bridge sender is not authorized."`로 응답한다(RPC와 같은 코드). navigation(commit 뒤)·`render-process-gone`·`destroyed`·새 `clientId` retire는 보내지 않는다. 전송 실패는 삼킨다. API 전체 끊김 신호는 범위 밖. 결정은 ADR 0020, ADR 0006 "범위 밖" 절 개정. 출처: `.scratch/shutdown-renderer-notify`. **선행:** RD-025 병합. **완료 기준:** 원인별 전송·비전송 단위 test, 살아 있는 창 detach·dispose 뒤 Renderer 구독자의 `CANCELLED`·`RemoteState` `stale`·새 subscribe `FORBIDDEN` Electron acceptance, 패키지 build 후 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`·`test:electron`, Electron acceptance 통과, ADR 0020·ADR 0006 개정·architecture·README 반영.

  계획: `_works/20260925-03-stream-terminal-notify/`. **결과:** DELTA-01~07 완료(subagent 협업, DELTA-02/03은 `subscriptions.ts`를 순차로 공유하는 한 agent가, DELTA-04는 `create-bridge-server.ts`를 다른 agent가 병렬로, DELTA-06 문서는 구현과 병렬로) — (01) `DocumentSessions`가 `AbortController.abort(reason)`으로 `session.signal.reason`에 lifecycle 3종 + `detach`·`dispose`·`replaced`를 싣고, `notifiesRenderer(signal)`(detach·dispose만 true) 헬퍼 추가, 신규 6 test → (02) `Subscriptions.onSessionAbort`가 notifiesRenderer면 쌓인 값을 버리고 `error CANCELLED "Bridge session ended."`를 즉시 전송, 신규 test → (03) `authorize` 대기 entry의 `onAbort`도 같은 조건이면 `subscribed`→`error CANCELLED`(`#reject`와 별도 메서드 `#pendingCancelled`로 분리, 가드 의미 충돌 방지), 신규 test → (04) `controlStream`의 subscribe admission 거부가 `subscribed`→`error FORBIDDEN`(RPC와 공유하는 `SENDER_UNAUTHORIZED_MESSAGE` 상수)로 응답, 신규 test·기존 "거부 0건" assertion을 새 계약으로 갱신(계획에 명시된 정상 범위, 멈추는 지점 미발동) → (05) multi-window probe에 `detachWindow`·`disposeServer` 제어 추가, Electron acceptance 신규 2건(detach 시 CANCELLED·`RemoteState` stale·다른 창 무영향·새 subscribe/RPC FORBIDDEN, dispose 시 전체 창 CANCELLED) → (06) ADR 0020 신규, ADR 0006·0016 개정 표시, architecture·README·scratch 갱신 → (07) 패키지 build 후 `xvfb-run -a pnpm verify` exit 0(35 files/574 tests, baseline 552 + 22), 루트 `pnpm lint`·`pnpm format:check` exit 0, demo `check-types` exit 0·`test:unit` 10 files/24 tests·`xvfb-run -a pnpm test:electron` 4/4(TRP-001 미발동), Electron acceptance 2 files/16 tests. **편차:** 없음. `test:soak`는 범위 제외로 미실행.

### Renderer API 동결 객체 트리 (출처: [.scratch/renderer-proxy-frozen-tree/spec.md](.scratch/renderer-proxy-frozen-tree/spec.md))

- [x] **RD-027 — `createRendererApi()`가 Proxy 대신 동결 객체 트리를 반환한다.** 지금 Renderer API는 manifest tree를 감싼 `Proxy`(`create-renderer-api.ts`의 `createProxy`)다. 호출 형태·타입 추론은 요구사항 55절을 충족하지만, DevTools·`console.log`에 빈 target(`Proxy {}`)만 보이고, `Symbol.dispose in api`가 `false`이며(`has` trap이 문자열 `dispose`만 본다), `getOwnPropertyDescriptor`가 `value` 없는 descriptor를 돌려준다. **구조:** handshake manifest로 `Object.create(null)` 노드를 재귀로 만들고 leaf(RPC 함수·`RemoteState`·Event `Observable`)를 즉시 생성해 enumerable data property로 둔 뒤 `Object.freeze`한다. 루트 `dispose`·`Symbol.dispose`는 non-enumerable로 같은 함수. 호출 형태·`RendererApi<B>`·manifest 판정·wire는 바꾸지 않는다. 결정은 ADR 0021. **완료 기준:** 보존 동작 characterization test가 교체 전후 GREEN, 동결·descriptor·`Symbol.dispose in api` test가 교체 전 RED·후 GREEN, 기존 renderer test 변경 0, `src/renderer`의 `new Proxy` 0건, 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`·`test:electron` 통과, ADR 0021·README·ADR 0007 개정 반영.

  계획: `_works/20260925-04-renderer-frozen-tree/`. **결과:** DELTA-01~04 완료 — (01) Proxy 구현에서 보존 test 4건(참조 동일성·thenable 아님·쓰기 `TypeError`·없는 경로 `undefined`) GREEN, 새 동작 test 3건(동결·null prototype, `Symbol.dispose in api`, data descriptor) RED 확인 → (02) `createProxy`를 `buildApiNode`로 교체, 기존 test diff 0, `src/renderer`의 `new Proxy` 0건 → (03) ADR 0021 신규, README·ADR 0007 개정, scratch 종료 → (04) 패키지 `xvfb-run -a pnpm verify` exit 0(35 files/584 tests, Electron acceptance 포함), 루트 `pnpm lint`·`pnpm format:check` exit 0, demo `check-types` exit 0·`test:unit` 24/24·`xvfb-run -a pnpm test:electron` 4/4(1회). **편차:** 없음(TRP-001 미발동). `test:soak`는 범위 제외로 미실행.

### Renderer 진단 훅 (출처: [.scratch/renderer-diagnostics/issues/01-renderer-diagnostics.md](.scratch/renderer-diagnostics/issues/01-renderer-diagnostics.md))

- [x] **RD-028 — Renderer가 진단 이벤트로 RPC 확정 원인·구독 종료 원인·메시지 폐기·전송 실패를 관측 가능하게 한다.** 지금 Main만 `DiagnosticsSink`가 있고([ADR 0010](docs/adr/0010-operational-diagnostics.md)), Renderer의 `RpcClient`·`StreamMultiplexer`는 로컬 deadline·abort·dispose·transport 실패·응답 불일치·스트림 메시지 폐기·`cancel`/`control` 전송 실패를 조용히 처리한다. **구조:** `createRendererApi<B>(options?)`로 시그니처를 바꿔 `{ transport?, diagnostics? }`를 받는다(위치 인자 `transport` 제거). `RendererDiagnosticsSink.record(event)`에 닫힌 유니온 `RendererDiagnostic` 6종(`rpc-settled`·`subscription-opened`·`subscription-closed`·`handshake-failed`·`message-dropped`·`transport-failed`)을 기록한다. snapshot 없음. 기록 금지는 ADR 0010을 따르되 원격 에러 `code`는 싣는다. sink 예외 격리·기본 무출력. 결정은 ADR 0022. **완료 기준:** RPC 호출 1건당 `rpc-settled` 정확히 1회(원인 8종 각각 test), `subscription-opened`/`closed` 쌍 일치(원인 5종 각각 test), `message-dropped` 3종·`transport-failed` 2종·`handshake-failed` 4종 test, sink throw 격리 test, 기존 동작(reject 값·구독 결과) 불변, 호출 지점 전부 옵션 객체로 이전, 패키지 `xvfb-run -a pnpm verify`·루트 `pnpm lint`·`pnpm format:check`·demo `check-types`·`test:unit`·`test:electron` 통과, README·architecture·ADR 0013 개정 반영. **결과:** 완료 조건 전부 충족, 편차 없음. `createRendererApi<X>(transport)` 위치 인자 호출 grep 0건, 패키지 `test/renderer/renderer-diagnostics.test.ts`(36 test)로 원인 8종·5종·3종·2종·4종과 sink 격리·무출력·순서·필드 제한을 검증. 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`·`xvfb-run -a pnpm test:electron` 모두 통과. README·`docs/architecture.md`·ADR 0010·0013 반영, `.scratch/renderer-diagnostics/issues/01-renderer-diagnostics.md` closed.

### Event source 등록 시 검증·정규화 (출처: 아키텍처 리뷰 `_works/arch-review/02.html` 후보 01)

- [x] **RD-029 — Event source의 모양·buffer 규칙을 registration 한 곳이 소유하고, 등록 시 `broadcast`/`scoped`로 정규화한다.** 지금 registration은 event source의 `mode`만 판별한다. `buffer.capacity` 검증은 helper(`broadcastEvent`/`scopedEvent`)와 `BoundedQueue` 생성자에만 있고, `overflow`는 어디서도 검증하지 않는다(오타가 조용히 `drop-oldest`로 동작). source interface는 구조적이라 helper 없이 직접 작성한 source가 타입을 통과한다. capacity 0인 직접 작성 source를 구독하면 `Subscriptions.#start`가 `try` 밖에서 `BoundedQueue` 생성에 실패한다. 그 결과 terminal 없이 `controlStream`이 reject하고(adapter 두 개 모두 `void`라 unhandled rejection), 구독 slot이 세션 retire까지 샌다(ADR 0009 위반). **구조:**
  - 먼저 `BoundedQueue` 생성을 `try` 안으로 옮겨 누수를 막는다.
  - registration이 모양, broadcast `source` 타입, scoped `factory` 타입, `capacity`, `overflow`를 검증하고, `createBridgeServer` 시점에 경로를 포함한 `TypeError`를 던진다.
  - helper는 검증 없는 순수 생성자가 된다(`currentValueSource` 제외).
  - event entry를 `broadcast`(공유 upstream, plain `Observable` 포함)와 `scoped`(구독별 factory)로 정규화하고, `Subscriptions`에서 `isScopedSource`·`#broadcastSource`를 제거한다.
  - `controlStream`에 "reject하지 않는다" 최종 방어 catch를 둔다.
  - 새 ADR은 만들지 않는다(ADR 0012 보정 주석).

  **완료 기준:**
  - 누수 회귀 test와 scoped 전달(값·error·complete) test가 있다.
  - 등록 검증 test(capacity·overflow·factory·source 각각)가 있다.
  - 올바른 직접 작성 source가 helper로 만든 source와 같게 동작한다.
  - 기존 단언 변경은 helper capacity test 1건(의도적 전환)뿐이다.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`이 통과한다.
  - architecture·ADR 0012·README·`CONTEXT.md`에 반영한다.

  계획: `_works/20260925-06-event-source-normalize/`. **결과:** 완료 조건 전부 충족, 편차 없음. DELTA-01(slot 누수 한 줄 수정, RED→GREEN) → DELTA-02(scoped 전달 test 4건, coverage 0 안전망) → DELTA-03(registration이 모양·capacity·overflow·factory/source 타입을 검증하는 test 9건, helper는 순수 생성자로 전환) → DELTA-04(event entry를 `delivery: {mode:"broadcast"|"scoped"}`로 등록 시점 정규화, `Subscriptions`의 `isScopedSource`·`#broadcastSource` 제거, 동작 변화 없음) → DELTA-05(`controlStream` 최종 방어 catch와 "reject하지 않는다" 계약 주석) → DELTA-06(architecture·ADR 0012 개정 note·`impl-types.ts`·README·`CONTEXT.md` 반영) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest) 36 files/637 tests 통과, 루트 `pnpm lint`·`pnpm format:check` 통과, demo `check-types`·`test:unit` 10 files/24 tests 통과. `grep -n 'isScopedSource\|#broadcastSource' src/main/subscriptions.ts`·`grep -rn 'assertEventBuffer' src` 모두 0건. 기존 test 단언 변경은 `create-bridge-server-impl.test.ts`의 helper capacity test 1건(의도적 전환)뿐. demo `test:electron`은 제외 범위(패키지 verify의 Electron acceptance로 대체)라 별도 실행하지 않음.

### Renderer API 인스턴스 종료 상태 단일 소유 (출처: 아키텍처 리뷰 `_works/arch-review/02.html` 후보 02)

- [x] **RD-030 — Renderer API 인스턴스의 종료 상태를 수명 객체 하나가 소유하고, 부작용 전에 종료를 확정한다.** 지금 "종료됐다"는 사실이 `RpcClient.#disposed`와 `StreamMultiplexer.#disposed` 두 곳에 나뉘어 있고, 루트 `dispose`는 `rpcClient.dispose()` → `streams[Symbol.dispose]()` 순으로 두 module을 차례로 닫는다. RPC 정리 단계에서 `rpc-settled(disposed)` sink가 동기로 호출되는데, 이때 stream 플래그는 아직 `false`다. 그래서 sink 안에서 `subscribe()`를 부르면 subscribe control이 전송되고 `subscription-opened`가 기록된 뒤, 이어지는 stream 정리가 그 구독을 `complete`로 닫는다. 이 동작은 ADR 0006 "종료 뒤 subscribe는 control 없이 동기 오류"와 ADR 0022 결정 10 "sink 재진입 시 내부 상태는 이미 일관된다"를 어긴다. ADR 0006 §순서의 "사용자 코드가 동기로 실행되는 지점은 `complete()` 하나뿐"이라는 전제는 ADR 0022가 sink를 추가하면서 깨졌다. **구조:**
  - 새 파일 `src/renderer/api-lifetime.ts`의 수명 객체가 `disposed`와 종료 절차를 소유한다. 절차는 멱등 검사 → ① 플래그 설정 → ② RPC 확정 → ③ stream 정리 순서다. 단계는 범용 콜백 목록이 아니라 고정 슬롯 2개(RPC, stream)로 받는다.
  - `RpcClient`·`StreamMultiplexer`·`LocalGeneration`은 수명 객체의 `disposed`를 읽기만 한다. `RpcClient.dispose()`와 `StreamMultiplexer[Symbol.dispose]`는 자체 guard가 없는 내부 단계 메서드로 바꾸고, 두 클래스의 `#disposed`는 삭제한다.
  - 루트 `dispose`는 수명 객체의 `dispose()` 한 줄이 된다.
  - subscribe 차단 지점은 `LocalGeneration.subscribe` 한 곳을 유지한다.
  - 공개 계약·wire·진단 종류는 바꾸지 않는다. 새 ADR은 만들지 않고 ADR 0006에 개정 note를 단다.

  **범위 밖(보류):**
  - envelope 대조 중복 2곳(`rpc-client.ts`·`stream-multiplexer.ts`)과 생성자 인자 전달 2벌: 수명과 무관한 관심사다.
  - generation terminal 경로 통합과 `StreamMultiplexer.open`의 전제 주석·guard 정리: 리뷰 02 후보 03이며, RD-030 뒤 별도 RD로 진행한다.

  **완료 기준:**
  - 재진입 매트릭스 test 9건(재진입 지점 `rpc-settled` sink·`subscription-closed` sink·구독자 `complete` 콜백 × 동작 `subscribe`·RPC 호출·`dispose()`)이 있다. 수정 전 RED case와 수정 후 전부 GREEN을 기록한다.
  - 기존 renderer test의 단언 변경 0건.
  - `src/renderer`에서 `#disposed` grep 0건, `StreamMultiplexer`의 `Symbol.dispose` 0건.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`이 통과한다.
  - ADR 0006 개정 note, `docs/architecture.md` Renderer dispose 서술, 리뷰 02.html 카드 완료 표시를 반영한다.

  계획: `_works/20260925-07-renderer-api-lifetime/`. **결과:** 완료 조건 전부 충족, 편차 없음. DELTA-01(재진입 매트릭스 test 9건 추가, RED 2건 확보: `rpc-settled` 지점의 `subscribe`·`dispose` 재진입) → DELTA-02(`src/renderer/api-lifetime.ts` 신규, `ApiLifetime`이 종료 플래그·절차를 소유하고 `RpcClient`·`StreamMultiplexer`·`LocalGeneration`이 이를 읽게 전환, 매트릭스 9/9 GREEN) → DELTA-03(ADR 0006에 RD-030 개정 note, ADR 0022 결정 10에 교차 참조, `docs/architecture.md` Renderer dispose 서술 갱신) → DELTA-04(전체 검증) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest, Electron acceptance 포함) 36 files/648 tests 통과(리뷰에서 RPC 확정 중 sink 재진입 구독 해제 test 1건 추가), 루트 `pnpm lint`·`pnpm format:check` 통과, demo `check-types`·`test:unit` 10 files/24 tests 통과. `grep -rn '#disposed' src/renderer`·`grep -n 'Symbol.dispose' src/renderer/stream-multiplexer.ts` 모두 0건. 기존 test 단언 변경 0건(`git diff dev -- packages/rx-bridge-electron/test` 삭제·변경 줄 0). 범위 밖으로 둔 envelope 대조 중복 2곳과 생성자 인자 전달, 리뷰 후보 03(generation terminal 경로 통합)은 계획대로 이 작업에 포함하지 않았다.

### 원격 구독 generation terminal 경로 단일화 (출처: 아키텍처 리뷰 `_works/arch-review/02.html` 후보 03)

- [x] **RD-031 — `StreamMultiplexer`의 generation terminal 처리를 한 경로로 모으고, dispose 뒤 acknowledge 전송을 막는다.** 지금 terminal 지점 5곳(open의 subscribe 실패, `close`, `closeAll`, `#dispatch`의 `error`·`complete`)이 map 삭제·`subscription-closed` 진단·unsubscribe 전송·handler 통지·guard를 각자 반복한다. `transport-failed(control)` 기록도 3곳에 복제돼 있다. batch의 acknowledge 전송(`#dispatch`)에는 종료 검사가 없다. 그래서 `next` 콜백 안에서 `api.dispose()`를 부르면 control이 `["subscribe","unsubscribe","acknowledge"]` 순서로 나간다. 이는 ADR 0006의 "종료 뒤 추가 전송 없음"과 어긋난다. 반면 로컬 마지막 구독자 해제 뒤의 acknowledge는 기존 test(`remote-event.test.ts` "acknowledges an accepted batch after a synchronous last-subscriber unsubscribe")가 계약으로 고정한 동작이라 유지한다. **구조:**
  - `#terminate(subscriptionId, generation, cause)` 한 곳이 1회 보장(map identity 검사) → 삭제 → `subscription-closed` 진단 → cause에 따른 unsubscribe → cause에 따른 handler 통지를 이 순서로 수행한다. cause → 동작은 표 하나로 둔다.
  - `#sendControl(message)` 한 곳이 전송 throw를 삼키고 `transport-failed(control)`를 기록한다. subscribe 전송 실패만 open에서 직접 처리해 `#terminate(transport-failed)`로 넘긴다(ADR 0022 결정 8 중복 방지).
  - acknowledge 전송 앞에 `lifetime.disposed` 검사를 둔다. 억제한 acknowledge는 진단으로 기록하지 않는다.
  - `open`의 호출 전제(종료 검사는 `LocalGeneration.subscribe`)는 유지하고 주석만 새 구조에 맞춘다. `LocalGeneration`은 바꾸지 않는다. `StreamGenerationHandlers`에 "terminal 통지 최대 1회, terminate 뒤 handler 호출 없음" 계약을 주석으로 둔다.
  - 공개 API·wire·진단 종류는 바꾸지 않는다. 새 ADR은 만들지 않는다.

  **범위 밖(보류):**
  - generation 레코드 2벌(`Generation`·`StreamGeneration`) 병합: 01 리뷰 카드 08(multiplexer = wire 순서·ack 내부 seam)을 다시 여는 일이다.
  - `LocalGeneration`의 closed·identity 검사 축소.
  - envelope 대조 중복과 생성자 인자 전달(RD-030 범위 밖 항목 그대로).

  **완료 기준:**
  - `next` 콜백 안의 `api.dispose()` 시나리오 test가 수정 전 RED, 수정 후 GREEN이다(control `["subscribe","unsubscribe"]`).
  - 기존 test 단언 변경 0건.
  - `stream-multiplexer.ts`에서 `type: "subscription-closed"` 기록 1곳, `channel: "control"` 기록 1곳.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`이 통과한다.
  - ADR 0006 개정 note, ADR 0022 결정 8 교차 참조, `docs/architecture.md` Renderer dispose 서술, 리뷰 02.html 카드 03 완료 표시를 반영한다.

  계획: `_works/20260925-08-stream-generation-terminal/`. **결과:** 완료 조건 전부 충족, 편차 없음. DELTA-01(`next` 콜백 안 dispose 시나리오 RED 확보: control `["subscribe","unsubscribe","acknowledge"]`) → DELTA-02(acknowledge 전송 앞 `lifetime.disposed` 검사 추가로 GREEN 전환) → DELTA-03(`#terminate`·`#sendControl` 도입, terminal 5곳 이관, 관측 가능한 순서·진단 종류·control 전송 변화 0) → DELTA-04(ADR 0006 개정 note, ADR 0022 결정 8 교차 참조, `docs/architecture.md` 반영) → DELTA-05(전체 검증) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest, Electron acceptance 포함) 36 files/650 tests 통과(DELTA-01 test 1건, 리뷰 test 1건 추가로 648→650), 루트 `pnpm lint`·`pnpm format:check` 통과, demo `check-types`·`test:unit` 10 files/24 tests 통과. `stream-multiplexer.ts`에서 `grep -c 'type: "subscription-closed"'`·`grep -c 'channel: "control"'` 각 1건, `git diff dev -- src/renderer/local-generation.ts` 빈 출력. 기존 test 단언 변경 0건(`git diff dev -- packages/rx-bridge-electron/test` 삭제·변경 줄 0). 범위 밖으로 둔 generation 레코드 2벌 병합, `LocalGeneration`의 closed·identity 검사 축소, envelope 대조 중복과 생성자 인자 전달은 계획대로 이 작업에 포함하지 않았다.

### 구독 종료 통지 판정 한 곳으로 (출처: 아키텍처 리뷰 `_works/arch-review/02.html` 후보 04)

- [x] **RD-032 — Main이 구독 종료를 Renderer에 알릴지·무엇을 보낼지 판정을 `Subscriptions` 한 곳의 표로 모으고, 거부 응답 전송 도중 retire의 누락 terminal을 막는다.** 지금 시작 전 구독의 `subscribed`(0)+`error`(1) 프레임 조립이 2벌이다(`sendSubscribeFailure`, `Subscriptions.#reject`). 통지 여부 규칙은 2 file 4곳에 서로 다르게 흩어져 있다: server admission 거부(항상 전송), `#reject`(session aborted면 사유와 무관하게 침묵, 두 프레임 사이 재검사), pending `onAbort`(`notifiesRenderer`일 때만), consumer `onSessionAbort`(`notifiesRenderer`일 때만, ack 우회). `notifiesRenderer`는 stream 전용 규칙인데 `document-sessions.ts`에 있다. 그래서 거부 응답의 `subscribed`(0) 전송 중 동기로 detach·dispose retire가 끼면 `error`가 생략되어 Renderer 구독에 terminal이 오지 않는다. 이는 ADR 0020 결정 5("detach·dispose는 통지")와 어긋난다. 실제 adapter 두 개는 stream 메시지를 비동기로 전달하므로 커스텀 `send`에서만 도달한다. **구조:**
  - `Subscriptions` 안 private 판정 경로 하나가 `cause`(`admission` / `rejected(error)` / `retired`)와 `session.signal`만 보고 보낼 error(또는 없음)를 정한다. 규칙: `admission`은 항상 `FORBIDDEN "Bridge sender is not authorized."`; session이 살아 있으면 `cause`의 error; aborted이고 사유가 `detach`·`dispose`면 `CANCELLED "Bridge session ended."`; 그 외 사유면 침묵. retire가 앞선 거부를 이긴다(ADR 0020 결정 3과 같은 규칙).
  - 프레임 방출은 두 모양이다: 미시작 구독(`subscribed`(0) 뒤 판정을 다시 해 `error`(1)), 시작된 구독(`error`(`++sequence`), ack 우회). 호출 경로가 모양을 고른다.
  - server는 admission 거부 진단(`rejected` + sender 사유)을 유지하고, wire 응답은 `subscriptions.rejectAdmission(command, send)`에 위임한다. `sendSubscribeFailure`·`#reject`·`notifiesRenderer`를 제거한다. `subscriptions.ts:278`의 `"Internal bridge error."` 리터럴을 `internalError` 상수로 바꾼다.
  - `#start`의 도달하지 않는 `session.signal.aborted` 검사는 그대로 둔다(후보 06 관심사). wire 모양·오류 코드·진단 종류는 바꾸지 않는다. 새 ADR은 만들지 않는다.

  **범위 밖(보류):**
  - 리뷰 후보 05(RPC·stream 공유 authorize 단계): 이 RD 뒤 별도 RD. `rpc-requests.ts` 등 다른 파일의 `"Internal bridge error."` 리터럴도 05 범위다.
  - 리뷰 후보 06(consumer 전달 창 내부 seam): 활성 구독 통지의 `++sequence` ack 우회는 이 RD에서 위치만 유지한다.

  **완료 기준:**
  - 거부 응답 `subscribed` 전송 callback 안 동기 detach 시나리오 test가 수정 전 RED, 수정 후 GREEN이다(`subscribed`(0)+`error`(1) `CANCELLED`). 같은 조건의 비통지 사유(`destroyed`) test는 침묵을 고정한다.
  - 기존 test 단언 변경 0건.
  - `grep -rn "sendSubscribeFailure\|notifiesRenderer" packages/rx-bridge-electron/src` 0건.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`이 통과한다.
  - ADR 0020 개정 note, `docs/architecture.md` 세션 정리 서술, 리뷰 02.html 카드 04 완료 표시를 반영한다.

  계획: `_works/20260925-09-stream-end-notice/`. **결과:** 완료 조건 전부 충족, 승인된 편차 1건. DELTA-01(거부 응답 `subscribed` 전송 도중 동기 detach test RED 확보 — `expected ['subscribed'] to deeply equal ['subscribed', 'error']`, 비통지 `destroyed` test는 수정 전부터 GREEN) → DELTA-02(`EndCause`·`endNotice`·`#endUnstarted` 판정 표 도입, `rejectAdmission` 위임, `sendSubscribeFailure`·`#reject`·`notifiesRenderer` 제거, `SENDER_UNAUTHORIZED_MESSAGE`를 `document-sessions.ts`로 이동, `internalError` 상수 정리로 GREEN 전환) → DELTA-03(ADR 0020 결정 2에 거부 전송 도중 retire 규칙 추가, `docs/architecture.md` "문서 세션과 정리" 절 갱신) → DELTA-04(전체 검증) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest, Electron acceptance 포함) 36 files/652 tests 통과, 루트 `pnpm lint`·`pnpm format:check` 통과, demo `check-types`·`test:unit` 10 files/24 tests 통과. `grep -rn "sendSubscribeFailure|notifiesRenderer|#reject" packages/rx-bridge-electron/src` 0건, `grep -c '"Internal bridge error."' subscriptions.ts` 1건(상수 정의만), `grep -c 'withEnvelope(' subscriptions.ts` 5→3, `create-bridge-server.ts` stream 프레임 조립 0건. **편차:** DELTA-02 진행 중 `notifiesRenderer` 삭제 계획이 `test/main/session-registry.test.ts`의 `describe("Main retire reason on session.signal")`(6 test, `notifiesRenderer` 직접 import해 unit 계약 고정)와 충돌하는 것을 발견 — 사용자 확인 후 `notifiesRenderer` 삭제를 유지하고 해당 describe를 `createBridgeServer` seam 기준(`describe("Main retire reason drives stream terminal notify")`)으로 재작성해 같은 계약(반환값 대신 실제 stream 메시지로 검증)을 고정했다. "기존 test 단언 변경 0건" 조건은 이 파일 1건에 한해 예외로 적용했다(근거: `_works/_completed/20260925-09-stream-end-notice/DELTA-02.md` "## 결정"). 범위 밖으로 둔 리뷰 후보 05(RPC·stream 공유 authorize 단계)·06(consumer 전달 창 내부 seam)은 계획대로 이 작업에 포함하지 않았다. **리뷰 후속:** 계획의 "`#reject` 진입 guard는 도달하지 않는다" 전제가 틀렸다 — `diagnostics.record`가 `rejected` 진단을 받는 중 동기로 detach·dispose하면 도달하고, 실제 adapter에서도 열린다. 수정 전에는 아무것도 보내지 않았고(`subscribed`도 없음) 지금은 결정 9 표대로 `subscribed`(0)+`CANCELLED`를 보낸다. `subscriptions.test.ts`에 test 1건을 추가해 고정하고(수정 전 코드에서 `expected [] to deeply equal ['subscribed', 'error']`), ADR 0020 개정 note·`docs/architecture.md`에 이 창을 적었다. 재작성한 `session-registry.test.ts` 침묵 test 3건은 retire가 일어나지 않아도 통과했다(lifecycle retire 제거 mutation에서 6/6 통과) — `getDiagnosticsSnapshot().subscriptions` 단언을 더했다. 패키지 `verify` 36 files/653 tests.

### RPC·stream 공유 authorize 단계 (출처: 아키텍처 리뷰 `_works/arch-review/02.html` 후보 05)

- [x] **RD-033 — RPC와 stream에 복제된 authorize 구간을 공유 판정 단계 하나로 모으고, 두 경로는 판정을 응답·프레임으로 번역만 하게 한다.** 지금 `RpcRequests.dispatch`와 `Subscriptions.subscribe`가 같은 구간을 각자 반복한다: context 조립, `authorize` 호출("생략 시 전부 허용" 포함), 예외 → `INTERNAL`(진단 없음), abort 시 취소 우선, `authorize-denied` 진단 → `FORBIDDEN`. `BridgeContext` 조립은 scoped factory까지 3곳, `"Internal bridge error."` 정의는 5곳(상수 2, 리터럴 3)이다. **구조:**
  - 신규 `src/main/authorization.ts`는 `bridgeContext(...)`와 순수 함수 `authorizeOperation(authorize, diagnostics, context, operation)`을 둔다. 판정은 `allowed` / `rejected(error)` / `cancelled` 중 하나다.
  - 판정 규칙: authorize가 settle한 뒤 signal이 aborted면 `cancelled`. 아니면 throw는 `INTERNAL`(진단 없음), deny는 `authorize-denied` 진단 뒤 `FORBIDDEN`이다.
  - `authorize`를 생략하면 판정을 동기로 돌려줘 같은 tick 진행을 보존한다.
  - 번역: RPC는 `cancelled`를 `cancelledIfAborted`로, `rejected`를 응답으로 바꾸고, 판정 뒤 abort를 다시 검사하지 않는다. stream은 `#finishPending`(slot 반환)을 거쳐 `rejected`를 `#endUnstarted`로 넘긴다.
  - stream 거부 진단은 이제 slot 반환 전에 기록된다(RPC와 같은 순서). wire 출력은 같다. sink 안에서 읽는 snapshot의 `subscriptions`만 1 더 크다.
  - `INTERNAL` payload는 `error-serializer.ts`의 export 하나로 모은다.
  - 생성자 시그니처·`create-bridge-server.ts` 배선·wire 모양·오류 코드·진단 종류는 바꾸지 않는다. 새 ADR은 만들지 않는다.

  **범위 밖(보류):**
  - 등록 조회·slot·한도 거부 공유와 `unknown-operation` 문구 차이: slot·취소 의미가 달라 합치면 shallow해진다. 문구는 wire에서 관찰된다.
  - 두 module 생성자 5-인자 동일성.
  - 리뷰 후보 06·07·08.

  **완료 기준:**
  - refactor 전에 characterization test 7건을 추가하고 GREEN을 확인한다. 대상:
    - stream·RPC `authorize-denied` sink 재진입 detach
    - RPC cancel 뒤 늦은 deny
    - stream unsubscribe 뒤 늦은 deny
    - stream detach 뒤 늦은 throw
    - authorize 생략 시 RPC·stream 같은 tick 진행

    이 test들은 refactor 뒤에도 GREEN이다.

  - 기존 test 단언 변경 0건이다.
  - `packages/rx-bridge-electron/src`의 `"Internal bridge error."`·`"Bridge operation is forbidden."`·`reason: "authorize-denied"`가 각 1건, `src/main`의 `windowRole:`이 1건이다.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`이 통과한다.
  - ADR 0011 개정 note, `docs/architecture.md` 판정 순서 서술, 리뷰 02.html 카드 05 완료 표시를 반영한다.

  계획: `_works/20260925-10-authorize-step/`. **결과:** 완료 조건 전부 충족, 편차 없음. DELTA-01(characterization test 7건 추가 — 수정 전 코드에서 36 files/653→660 tests GREEN 확인) → DELTA-02(`error-serializer.ts`의 `internalError`를 동결 상수로 export, `subscriptions.ts`·`rpc-requests.ts` 리터럴 통합) → DELTA-03(`authorization.ts` 도입, RPC 경로가 `authorizeOperation`의 판정을 응답으로 번역만 하도록 전환) → DELTA-04(stream 경로 전환, authorize 거부 진단을 slot 반환 전으로 옮겨 RPC와 순서를 맞춤) → DELTA-05(문서 반영·전체 검증) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest, Electron acceptance 포함) 36 files/660 tests 통과(기준 653+7), 루트 `pnpm lint`·`pnpm format:check` 통과, demo `check-types`·`test:unit` 10 files/24 tests 통과. grep: `"Internal bridge error."` 1건(`error-serializer.ts`), `"Bridge operation is forbidden."` 1건(`authorization.ts`), `reason: "authorize-denied"` 1건(`authorization.ts`), `windowRole:` 1건(`authorization.ts`), `#authorize(` 0건(`rpc-requests.ts`·`subscriptions.ts`). `git diff dev -- create-bridge-server.ts` 빈 출력, 생성자 시그니처 불변. `git diff dev -- test` 실제 삭제 줄은 `diagnostics-rejections.test.ts`의 `setup()` 반환값에 `detach` 필드를 더한 2줄뿐(단언 불변), 나머지는 추가. ADR 0011 개정 note·`docs/architecture.md`(:112·:114 부근)에 반영. 리뷰 `02.html` 카드 05 완료 표시. **발견:** DELTA-01 ②(RPC sink 재진입) test의 진단 순서는 계획 시점 추정과 달랐다 — `detach()`가 session을 abort시켜 `#begin`의 `onSessionAbort` 리스너가 동기로 `#cancelActive`를 불러 `rpc-cancelled`가 끼어든다(실측: `session-opened`→`rejected(authorize-denied)`→`session-closed`→`rpc-cancelled`→`rpc-finished`). 실측값으로 test를 고정했다(DELTA-01 "## 결과"). **리뷰:** `authorize`가 있으면 판정이 microtask 한 단계 늦게 도착한다(macrotask 경계와의 순서는 같다) — ADR 0011 note에 추가. sink 안 snapshot `subscriptions` +1 주장을 test로 고정(RD-033 이전 코드 RED `[0]`, 이후 GREEN `[1]`).

- [x] **RD-034 — consumer 1건의 전달 창을 `Subscriptions` 안 내부 module `DeliveryWindow`로 분리한다.** 지금 `subscriptions.ts`(680줄)의 `Consumer`는 19필드이고, 그중 창 상태 6개(`pendingState`·`hasPendingState`·`pendingEvents`·`inFlight`·`terminal`·`sequence`)가 admission·fan-out·수명 필드와 섞여 있다. 창 불변식("ack 전 추가 전송 없음")의 예외인 선점 종료(ADR 0020 결정 3)가 창 밖 `onSessionAbort`의 `++consumer.sequence`에 있다. State/Event 정책 차이는 값이 들어올 때마다 kind 분기(`#start` queue 생성·`#next`·`#flush` 2곳)로 다시 판별된다. 출처: 아키텍처 리뷰 `_works/arch-review/02.html` 카드 06, 2026-09-25 그릴링. **구조:**
  - 신규 `src/main/delivery-window.ts`의 `DeliveryWindow`는 rxjs·session에 의존하지 않는다. 입력은 `open()`·`accept(value)`·`ack(sequence)`·`end(terminal)`·`preempt(error)`·`close()`이고, 출력은 envelope 없는 보낼 메시지(반환값)다.
  - 창이 sequence(`subscribed` 0 포함)·`inFlight`·기록된 terminal·닫힘 플래그를 단독 소유한다. `Consumer.closed`는 삭제하고 `Subscriptions`는 창 판정을 읽는다.
  - State/Event 차이는 생성 시 주입하는 buffer 정책 하나로 표현한다. State는 최신값 1칸 덮어쓰기, Event는 `BoundedQueue`이고 `error` 정책 overflow는 `STREAM_OVERFLOW` terminal이다. 창 안 kind 분기는 0곳이다.
  - `stream-dropped`·`stream-queue` 진단은 창 생성 시 주입한 callback(`onDropped`·`onQueueDepth`)을 발생 지점에서 동기 호출한다. 창은 callback 뒤마다 자기 상태를 다시 확인한다. 효과 목록 반환형은 sink 재진입 시 선점 `error` sequence를 바꾸므로 쓰지 않는다.
  - 선점 종료는 `preempt(error)`다. 쌓인 값·기록된 terminal을 버리고 다음 sequence로 `error`를 반환한다. `onSessionAbort`는 반환 메시지를 보내고 `#close`만 한다.
  - `Subscriptions`에 남는 것: admission·`authorize` 대기, fan-out(kind 분기 `#start` 시작 경로 선택·늦게 합류한 State의 `getValue()` 2곳 유지), `parseOutput`·`validation-failed`, envelope 조립·`send`·send 예외 시 close, 수명 정리(listener 해제·slot·`subscription-closed`·controller·source 분리). `#endUnstarted`의 `subscribed`(0)+`error`(1)는 창 생성 전 경로라 그대로 둔다.
  - **동작 변경(이것만):** 진단 sink가 `#flush`의 shift 뒤 `stream-queue` 진단을 받는 중 동기로 detach·dispose하면, 지금은 선점 `error`(1) 뒤에 `batch`(2)를 보낸다(`#flush`가 진단 뒤 `closed`를 다시 확인하지 않는다, ADR 0020 결정 3 위반). 현재 구조에서 test-first로 먼저 고쳐 `subscribed`(0)·`error`(1)로 끝나게 한다.
  - 그 외 wire 메시지 순서, sequence 번호, 진단 종류·순서, `getDiagnosticsSnapshot()` 값을 보존한다. `stream-dropped` 도중 retire 시 `subscription-closed` 뒤에 `stream-queue`가 1건 더 기록되는 현재 순서도 보존한다. 생성자 시그니처·`create-bridge-server.ts` 배선을 바꾸지 않는다. 새 ADR·ADR 개정 note는 만들지 않는다.
  - test 표면: RD-015 그릴링 결정 5("구독 모듈 직접 test 없음")의 예외로, `BoundedQueue`와 같은 등급의 순수 module인 `DeliveryWindow`만 `test/main/delivery-window.test.ts`에서 직접 test한다. `Subscriptions`는 계속 `createBridgeServer` seam으로만 test한다.

  **범위 밖(보류):** 리뷰 02 카드 07·08. fan-out 쪽 kind 분기 2곳. close 뒤 `stream-queue` 꼬리 제거. 위 1건 외에 동작 변경이 필요한 결함이 나오면 이 RD에 섞지 않고 별도 RD로 등록한다.

  **완료 기준:**
  - refactor 전에 `createBridgeServer` seam characterization test 4건을 추가하고 GREEN을 확인한다. 대상:
    - 진단 sink가 `stream-dropped`를 받는 중 동기 detach
    - 진단 sink가 `stream-queue`를 받는 중 동기 detach
    - 동기 `send` 안 `acknowledge` 재진입
    - 동기 `send` 안 `unsubscribe` 재진입

    이 test들은 refactor 뒤에도 GREEN이다.

  - 위 동작 변경은 RED test 1건(shift 뒤 `stream-queue` 도중 detach → wire `subscribed`·`error`만)을 수정 전 코드에서 확인한 뒤 고친다.
  - `delivery-window.test.ts`가 창 불변식을 표 형식으로 고정한다: ack 게이트, State 최신값 교체, Event overflow 3정책, 대기 값 drain 뒤 terminal, `preempt`의 대기 값·기록된 terminal 폐기, `close` 뒤 무출력, 진단 callback 안 `preempt`·`close` 재진입.
  - 기존 test 단언 변경 0건이다.
  - `src/main/subscriptions.ts`의 `consumer.sequence`·`inFlight`·`pendingState`·`hasPendingState`·`pendingEvents`·`closed: ` 0건. `src/main/delivery-window.ts`의 `rxjs`·`./subscriptions`·`./document-sessions` import 0건, `kind ===` 0건.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`이 통과한다.
  - `docs/architecture.md` 구독 서술 1문장, `CONTEXT.md` "구독" 항목 1문장(전달 창은 `DeliveryWindow`가 소유), 리뷰 02.html 카드 06 완료 표시를 반영한다.

  계획: `_works/20260925-11-delivery-window/`. **결과:** 완료 조건 전부 충족, 편차 없음. DELTA-01(characterization test 4건 추가 — 수정 전 코드에서 36 files/661→665 tests GREEN, probe 실측 P1~P4 재현 확인) → DELTA-02(결정 12 결함을 현재 구조에서 test-first로 수정 — 수정 전 RED `batch`(sequence 2) 관측, `#flush`의 Event 분기에 shift 뒤 `stream-queue` 진단 기록 직후 `consumer.closed` guard 추가, 37 files/682 tests GREEN) → DELTA-03(`src/main/delivery-window.ts` 신규: `DeliveryWindow`·`DeliveryBuffer` 인터페이스로 State 최신값 1칸/Event `BoundedQueue` 두 buffer 정책을 감싸고 `createStateDeliveryWindow`/`createEventDeliveryWindow` factory로 공개, `test/main/delivery-window.test.ts` 16건 직접 test 추가 — 37 files/677 tests GREEN) → DELTA-04(`Subscriptions`를 창으로 배선: `Consumer`의 창 필드 6개와 `closed` 삭제, `#start`·`onSessionAbort`·`control`·`#next`·`#terminate`·`#send`·`#close`·`queuedEventsCount`·`dispose`를 창 호출로 전환, `#flush` 삭제 — 37 files/682 tests GREEN, `subscriptions.ts` 680→620줄) → DELTA-05(문서 반영·전체 검증) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest, Electron acceptance 포함) 37 files/682 tests 통과(기준 661+DELTA-01 4+DELTA-02 1+DELTA-03 16), 루트 `pnpm lint`·`pnpm format:check` 통과, demo `check-types`·`test:unit` 10 files/24 tests 통과. grep: `consumer\.sequence|inFlight|pendingState|hasPendingState|pendingEvents|closed: `(`subscriptions.ts`) 0건, `from "rxjs"|\./subscriptions|\./document-sessions|kind ===`(`delivery-window.ts`) 0건. `subscriptions.ts`의 `kind ===`/`.kind` 5건 중 2건은 `EndCause` 판정(DELTA-04 이전부터 존재, 무관)이고 registration 관련은 정확히 3건(버퍼 정책 선택 1 + fan-out 시작 경로 선택·늦은 State 합류 2). `git diff dev -- create-bridge-server.ts` 빈 출력, 생성자 시그니처 불변. `git diff dev -- test` 2파일 543줄 모두 추가 줄(삭제 0). `docs/architecture.md`(:112 부근)·`CONTEXT.md`(구독 항목)에 1문장씩 반영, 리뷰 `02.html` 카드 06 완료 표시. **발견:** DELTA-02 결함의 실제 원인은 `#flush`가 shift 뒤 `stream-queue` 진단 기록 중 동기 detach로 `closed`가 true가 돼도 재확인 없이 계속 진행해 `batch`를 보내던 것 — guard 4줄(주석 포함) 추가로 해결, `Subscriptions`·창 경계 밖 다른 변경은 없다. 결정 13(`stream-dropped` 도중 retire 시 `subscription-closed` 뒤 `stream-queue` 1건 추가 기록)의 순서는 계획대로 보존했고, 후속 정리 여지를 `pending-issues/01.md`에 기록했다. **리뷰:** DELTA-04가 `#next`의 옛 `closed || terminal !== undefined` guard를 지우고 창 `accept`에 맡겼는데, 창은 닫힘·종결만 보고 "terminal 기록됨(ack 대기)"을 보지 않았다. 공유 fan-out 스냅샷 안에서 앞 consumer의 동기 `send`가 source를 complete하거나 뒤 consumer를 닫으면 결과가 달라졌다(결정 12 밖 동작 변경 2건): ① terminal 기록 뒤 도착한 값이 queue에 들어가 ack 뒤 `batch`로 나간다(이전 `complete2` → `batch2, complete3`) ② 닫힌 consumer에 `validation-failed`가 1건 더 기록된다. DELTA-04 기록의 "같은 tick에 동일 consumer가 두 번 `#next`를 받는 경로가 없어 도달 불가" 판단은 스냅샷 재진입을 놓쳤다. 창에 `accepting`(닫힘·종결·terminal 기록 아님)을 두고 `accept`와 `#next`의 `parseOutput` 앞에서 확인하도록 고쳤다. 공개 seam test 2건(RD-034 이전 코드 GREEN, 수정 전 RED)과 창 직접 test 2건을 추가했다 — 37 files/686 tests 통과. 소스 주석의 작업 문서 참조(`C1`~`C6`, checklist 결정 번호, `"## 결정" 참고`)를 자체 설명으로 바꿨다.

- [x] **RD-035 — Renderer test의 wire 조립·구독 ID 조회 helper를 `FakeTransport` 한 파일로 모은다.** 지금 `test/renderer/` test 6개 파일에 같은 helper가 약 15벌 복제돼 있다: stream 메시지 조립(`message`/`streamMessage`, `StreamMessageBody` 타입) ×4, manifest만 다른 handshake factory(`eventTransport`·`stateTransport`·`streamTransport`·`bridgeTransport`) ×4, 글자까지 같은 RPC `success` ×3, 구독 ID 추출 3방식(`subscriptions(transport)[n]`·`firstSubscriptionId`·`subscriptionIdFor(transport, key)`) ×4. 출처: 아키텍처 리뷰 `_works/arch-review/02.html` 카드 07, 2026-09-25 그릴링. 카드 원안(key 단위로 sequence·id를 자동 추적하는 "가짜 Main")은 기각했다 — remote-event test가 검증하는 sequence 규칙(중복·감소·`subscribed` 전 batch)을 test 본문에서 감추고, 이상 상황용 raw 우회로가 필요해 wire 지식이 두 곳에 남는다. 카드 근거("ADR 0017은 wire 변경이 test를 한꺼번에 깨뜨린 이력을 기록한다")도 사실과 다르다: 그 이력은 demo test 대상이고 loopback으로 해결됐으며, RD-017·019가 `test/renderer/`를 바꾼 커밋은 3건, 각 1파일이다. **구조:**
  - `FakeTransport`는 shallow 호출 기록기로 유지한다(ADR 0017 결정 불변). sequence·subscriptionId는 계속 test가 인자로 쓴다.
  - `test/renderer/fake-transport.ts`에 모은다. wire 조립은 자유 함수 export(`streamMessage(subscriptionId, body)`, `rpcSuccess(requestId, result?)`, `rpcError(requestId, error)`, `StreamMessageBody` 타입. `rpcError`는 정상 RPC error 응답 2곳을 raw literal에서 빼기 위해 둔다), 기록 조회는 `FakeTransport` method(`subscribeCommands()`, `subscriptionIdFor(key, nth = 0)`, 조회 실패 시 throw)다.
  - handshake factory 4벌은 생성자 옵션 `new FakeTransport({ manifest })`로 바꾼다. manifest는 기본값과 병합하지 않고 통째로 교체하며, 지정하지 않은 종류는 `[]`다. 옵션이 없으면 지금 기본 handshake를 유지한다. `create-renderer-api.test.ts`에서 정상 manifest를 넣고 API tree 모양을 보는 인라인 handshake 4곳도 이 옵션으로 옮긴다. handshake 자체를 검증하는 인라인(deferred·잘못된 값·reject)은 그대로 둔다.
  - 구독 ID 조회는 key 기준 `subscriptionIdFor(key, nth)` 하나로 통일한다. 같은 key 재구독은 `nth = 1`이다.
  - builder는 `protocolVersion: 1`·`clientId: "client-1"`을 고정하고 envelope override 인자를 두지 않는다. envelope 이상(`protocolVersion: 2`, 다른 `clientId`)과 handshake 검증 입력은 raw literal로 남긴다. sequence·subscriptionId·requestId 이상은 builder 인자로 표현한다. 로컬 `message(..., clientId)` 셋째 인자로 쓰던 `remote-event.test.ts`의 `"old-client"` 지점은 raw literal로 바꾼다.

  **범위 밖(보류):** key 단위 가짜 Main. `src/` 변경. `test/renderer/` 밖 test. ADR 0017·`docs/architecture.md`·`CONTEXT.md` 변경. 리뷰 02 카드 08.

  **완료 기준:**
  - test 제목·단언(matcher·기대값) 변경 0건이다. 관측 대상 식이 로컬 helper에서 `FakeTransport` method로 바뀌는 것만 허용한다(`remote-event.test.ts`의 `expect(subscriptions(transport))` 3곳).
  - test 수가 기준(RD-034 뒤 37 files/686 tests, renderer 6 files/136 tests)과 같다. `vitest list test/renderer` 출력이 `dev`와 같다.
  - `test/renderer/*.test.ts`에서 로컬 helper 정의(`message`·`streamMessage`·`success`·`subscriptions`·`firstSubscriptionId`·`subscriptionIdFor`·`subscribeCommands`·`eventTransport`·`stateTransport`·`streamTransport`·`bridgeTransport`, `StreamMessageBody` 타입) 0건이다.
  - `test/renderer/*.test.ts`의 `protocolVersion: 1` 잔존분이 handshake 검증 입력과 envelope 이상 test뿐이다(계획 시점 예상 10건).
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`가 통과한다.
  - 리뷰 `02.html` 카드 07의 근거를 교정하고, 완료(범위 B)와 기각한 원안 1줄을 표시한다.

  계획: `_works/20260925-12-renderer-fake-helpers/`. **결과:** 완료 조건 전부 충족, 편차 1건(prettier 재정렬, 허용). DELTA-01(`test/renderer/fake-transport.ts`에 생성자 옵션 `{ manifest }`, 자유 함수 `streamMessage`·`rpcSuccess`·`rpcError`, `FakeTransport.subscribeCommands()`·`subscriptionIdFor(key, nth)` 추가, 기존 test 불변 — 6 files/136 tests GREEN, check-types 통과) → DELTA-02(6개 파일에서 로컬 helper 약 15벌 삭제, 새 helper 호출로 이전, 파일 단위 커밋 — rpc-calls 21·create-renderer-api 33·remote-state 17·remote-event 7·renderer-dispose 19·renderer-diagnostics 39 = 136 tests 전부 GREEN) → DELTA-03(전체 검증·문서 반영) 순으로 진행. 검증 수치: `npx vitest list test/renderer` 출력이 DELTA-01 시작 전 기준(`baseline-renderer-list.txt`)과 `diff` 빈 출력. 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest, Electron acceptance 포함) 37 files/686 tests 통과(RD-034 기준 686 그대로, 이 RD는 test 수 증감 없음). 루트 `pnpm lint` 통과, `pnpm format:check`는 1차 실행에서 DELTA-02가 이전한 4개 파일(`remote-event`·`remote-state`·`renderer-diagnostics`·`renderer-dispose`)의 줄바꿈 불일치를 지적해 `prettier --write`로 재정렬(값·순서 변경 없음) 후 통과. demo `check-types`·`test:unit` 10 files/24 tests 통과. grep: 로컬 helper 정의(`message`·`streamMessage`·`success`·`subscriptions`·`firstSubscriptionId`·`subscriptionIdFor`·`subscribeCommands`·`eventTransport`·`stateTransport`·`streamTransport`·`bridgeTransport`, `StreamMessageBody` 타입) `test/renderer/*.test.ts` 0건. `protocolVersion: 1` 잔존 10건, 계획 시점 예상과 정확히 일치 — `create-renderer-api.test.ts` 6건(deferred resolve 1·잘못된 manifest `test.each` 4·malformed 1), `renderer-diagnostics.test.ts` 3건(handshake 실패 2·다른 clientId stream 1), `remote-event.test.ts` 1건(`"old-client"`, C3). 단언 변경: `git diff dev -U0 -- test/renderer/*.test.ts`에서 `expect(...)` 등 관련 줄 3건뿐이며 전부 `expect(subscriptions(transport))` → `expect(transport.subscribeCommands())`(C4 허용, 관측 대상 식만 변경, matcher·기대값 불변). `git diff dev --stat -- packages/rx-bridge-electron/src` 빈 출력. 리뷰 `02.html` 카드 07 근거 교정·완료(범위 B) 표시, 기각한 원안(key 단위 가짜 Main) 1줄 반영. **발견:** 없음 — `pending-issues/`·`pending-traps/` 모두 비어 있다. **리뷰:** DELTA-02가 handshake factory를 지우면서 manifest literal을 test마다 인라인해 같은 manifest가 43곳에 복제됐다(test 파일 순증 143줄). 파일 상수 manifest(`CONNECTION_MANIFEST`·`FAULT_MANIFEST`·`STREAM_MANIFEST`·`BRIDGE_MANIFEST`)를 생성자 옵션으로 넘기도록 고쳤다. remote-state에 남은 인라인 subscribe 필터 7곳(완료 조건 grep은 helper 정의만 봐서 놓침)을 `transport.subscribeCommands()`로 바꿨다 — `expect` 3곳은 C4와 같은 관측 식 교체, matcher·기대값 불변. `fake-transport.ts`의 기본 handshake와 옵션 분기의 envelope 중복을 생성자 1회 조립으로, 옵션 타입을 `Partial<HandshakeManifest>`로 정리했다. 순변화 -180줄, `xvfb-run -a pnpm verify` 37 files/686 tests, `vitest list` 기준 diff 빈 출력, lint·format 통과.

### upstream 연결 module과 구독 frame sequence 단일화 (출처: 아키텍처 리뷰 `_works/arch-review/03.html` 후보 01·03)

- [x] **RD-036 — upstream 연결(key별 공유·scoped 개별)을 `Subscriptions` 안 내부 module `Upstreams`로 분리하고, 시작 전 거부 frame의 sequence도 `DeliveryWindow`가 매기게 한다.** 지금 `subscriptions.ts`(623줄, src 최대)는 admission·`authorize` 대기·consumer 수명·종료 통지 판정과 함께 key별 upstream 공유까지 소유한다. 공유 상태가 `Consumer`에 필드 3개(`shared`·`own`·`sourceDetached`)로 새고, readonly 필드를 캐스트로 대입한다(`(consumer as { shared?: SharedSource }).shared = shared`). fan-out 결함 test(`subscriptions.test.ts`의 옛 consumer evict·같은 fan-out 재진입 3건)는 server 전체와 ack 왕복으로만 재현된다. 별개로 `DeliveryWindow`는 "sequence를 모두 창이 매긴다"고 하지만 `#endUnstarted`는 `subscribed`(0)·`error`(1)를 리터럴로 조립하고, `withEnvelope(clientId, { subscriptionId, … })` 조립이 3곳에 있다. 출처: 아키텍처 리뷰 `_works/arch-review/03.html` 후보 01·03, 2026-09-25 그릴링. **구조:**
  - 신규 `src/main/upstreams.ts`의 `Upstreams`가 State·broadcast Event의 key별 공유, scoped Event의 구독별 upstream, 늦게 합류한 State의 `getValue()` 현재값, scoped factory 결과의 Observable 검사, 동기 완료, 마지막 해제 시 upstream 정리, 옛 연결 해제가 같은 key의 새 공유를 지우지 않는 identity 검사를 소유한다.
  - interface는 연결 1개와 해제 1개다. 연결은 호출자 토큰(consumer 객체)·registration·완성된 `BridgeContext`·observer 모양 sink를 받는다. 해제는 같은 토큰으로 하고, 연결 요청이 아직 반환되지 않은 동기 재진입 중에도 불러도 되며 멱등이다. 집계·dispose·조회는 없다.
  - upstream `error`는 원래 값을 sink에 넘기고 `Subscriptions`가 `internalError`로 바꾼다(지금의 `serializeError(error, [], limits)`는 선언 목록이 비어 항상 `internalError`다). scoped factory 예외·non-Observable 반환과 늦은 합류 `getValue()` 예외는 연결 요청이 동기로 throw하고, `Subscriptions`의 기존 `try/catch`가 `INTERNAL` terminal로 번역한다. throw한 연결의 토큰은 연결되지 않은 상태로 남는다.
  - fan-out 도중 해제된 토큰에는 다음 값·terminal을 전달하지 않는다(`Upstreams` 불변식). `Subscriptions#next`의 `window.accepting` 검사도 유지한다.
  - `Upstreams`는 `DocumentSession`·`authorization.ts`·`DeliveryWindow`·진단·envelope를 모른다. `Consumer`의 `shared`·`own`·`sourceDetached`, `SharedSource`, `#shared`·`#startShared`·`#observer`·`#detachSource`를 삭제한다.
  - 시작 전 거부(`#endUnstarted`)도 buffer 없는 전용 창(`createRejectionDeliveryWindow`, 이름은 예시)을 열어 `open()`→`preempt(error)`로 sequence 0·1을 받는다. `endNotice` 전후 2회 평가는 `Subscriptions`에 남는다. envelope 조립은 순수 함수 1개로 모으고, 전송 실패 처리(활성 consumer는 close, 시작 전 거부는 무시)는 각 호출부에 남는다.
  - 관측 가능한 동작을 모두 보존한다: wire 메시지 순서·sequence·코드·문구, 진단 종류·순서, `getDiagnosticsSnapshot()` 값, slot 반환 시점, upstream 구독·해제 횟수와 시점. 생성자 시그니처·`create-bridge-server.ts` 배선·공개 API를 바꾸지 않는다. 새 ADR·ADR 개정 note는 만들지 않는다.
  - test 표면: RD-015 그릴링 결정 5("구독 모듈 직접 test 없음")의 예외로 `Upstreams`를 `test/main/upstreams.test.ts`에서 직접 test한다(RD-034 `DeliveryWindow`와 같은 근거). 기존 server-level fan-out test는 삭제하지 않는다.

  **범위 밖(보류):** 리뷰 03 후보 02(retire 통지 typed interface)·04(payload limits 해석)·05(`LocalGeneration` kind 분기). 리뷰 03 잔재 중 이동 범위 밖 항목(`BridgeServer`·`CommonServerOptions`·`buildBridgeServer`·`TransportErrorCode`·registration manifest 재조립·`RemoteError` 변환 2벌). 동작 변경이 필요한 결함이 나오면 이 RD에 섞지 않고 별도 RD로 등록한다.

  **완료 기준:**
  - refactor 전에 `createBridgeServer` seam characterization test 4건을 추가하고 수정 전 코드에서 GREEN을 확인한다. 대상:
    - 늦게 합류한 State의 `getValue()` throw → 합류자만 `subscribed`·`error INTERNAL`, 기존 consumer·upstream 유지
    - scoped factory throw·non-Observable 반환 → `subscribed`·`error INTERNAL`, slot 반환
    - 첫 State `batch` 송신이 throw(upstream 동기 subscribe 도중 close) → upstream 구독 해제
    - broadcast upstream `error` → 두 consumer 모두 `error INTERNAL`, upstream 해제

    이 test들은 refactor 뒤에도 GREEN이다.

  - `upstreams.test.ts`가 공유·마지막 해제·늦은 합류·scoped 분리·factory 예외·factory 실행 중 해제·동기 방출 중 해제·fan-out 중 뒤 토큰 해제·error/complete 전파·옛 토큰 evict 방지·해제 멱등을 고정한다.
  - `delivery-window.test.ts`에 시작 전 거부 창(`open`→`preempt` = 0·1, 이후 무출력) test를 추가한다.
  - 기존 test 단언 변경 0건이다(`git diff dev -- packages/rx-bridge-electron/test`는 추가 줄만).
  - `src/main/subscriptions.ts`의 `SharedSource`·`#shared`·`#startShared`·`#observer`·`#detachSource`·`sourceDetached`·`serializeError`·`from "rxjs"`·`sequence: 0`·`sequence: 1` 0건, `withEnvelope(` 1건. `src/main/upstreams.ts`의 `./subscriptions`·`./document-sessions`·`./authorization`·`./delivery-window`·`./diagnostics`·`withEnvelope`·`internalError` 0건.
  - `git diff dev -- packages/rx-bridge-electron/src/main/create-bridge-server.ts` 빈 출력.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`이 통과한다.
  - `CONTEXT.md` "구독"·"Event 전달 방식" 항목, `docs/architecture.md` 구독·Event 전달 방식 서술에 `Upstreams`를 반영하고, 리뷰 03.html 카드 01·03 완료를 표시한다.

  계획: `_works/20260925-13-upstreams/`. **결과:** 완료 조건 전부 충족, 편차 없음. DELTA-01(`createBridgeServer` seam characterization test 5 case(G1 1·G2 `test.each` 2·G3 1·G5 1) 추가, 수정 전 코드에서 전부 GREEN — 37 files/691 tests) → DELTA-02(신규 `src/main/upstreams.ts`의 `Upstreams`와 `test/main/upstreams.test.ts` 21건 직접 test 추가, 아직 배선하지 않음 — 38 files/712 tests) → DELTA-03(`Subscriptions`를 `Upstreams`로 배선, `Consumer`의 `shared`·`own`·`sourceDetached`와 `SharedSource`·`#shared`·`#startShared`·`#observer`·`#detachSource`·`serializeError` 삭제, `subscriptions.ts` 623→539줄 — 신규 test 없음, 712 tests 그대로) → DELTA-04(시작 전 거부도 buffer 없는 전용 창 `createRejectionDeliveryWindow`를 열어 sequence 0·1을 받게 하고, envelope 조립을 module 수준 순수 함수 `streamFrame` 1곳으로 통합 — 신규 test 5, 38 files/717 tests) → DELTA-05(문서 반영·전체 검증) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest, Electron acceptance 포함) 38 files/717 tests 통과(기준 686+DELTA-01 5+DELTA-02 21+DELTA-03 0+DELTA-04 5), 루트 `pnpm lint`·`pnpm format:check` 통과, demo `check-types`·`test:unit` 10 files/24 tests 통과. grep: `subscriptions.ts`의 `SharedSource|#shared|#startShared|#observer|#detachSource|sourceDetached|serializeError|from "rxjs"|sequence: 0|sequence: 1` 0건, `withEnvelope(` 1건. `upstreams.ts`의 `./subscriptions|./document-sessions|./authorization|./delivery-window|./diagnostics|withEnvelope|internalError` 0건(DELTA-02에서 doc comment 산문 중 `internalError` 문자열 1건이 걸려 문구를 바꿔 0건으로 만들었다 — 실제 import·참조 아님). `git diff dev -- create-bridge-server.ts` 빈 출력, 생성자 시그니처 불변. `git diff dev --numstat -- test` 삭제 열 전부 0. `CONTEXT.md`에서 `startShared|SharedSource|detachSource` 0건(DELTA-05). `docs/architecture.md`(구독·Event 전달 방식)·`CONTEXT.md`(구독·Event 전달 방식)에 반영, 리뷰 `03.html` 카드 01·03 완료 표시(로컬만, `_works/`는 gitignore 대상). **발견:** DELTA-02 최초 test 작성 시 `tsc` 오류 3건(공유 test.each 유니언 캐스트, terminal 콜백 인자 수, `SinkCall` narrowing) — test 코드만 수정, 프로덕션 코드 영향 없음. **리뷰(2026-09-25):** `Upstreams.connect`의 throw 정리가 갈래별 부분 삭제(`#tokens.delete`, `members.delete`)라서, 늦은 합류 `getValue()`가 기존 member를 동기로 모두 끊은 뒤 throw하면 member 0인 공유 upstream과 map entry가 남았다(옛 `#detachSource`는 정리했다). `connect`가 예외 시 `disconnect(token)` 하나로 정리하도록 바꾸고 `upstreams.test.ts`에 회귀 test 1건을 추가했다(RED 확인 후 GREEN). 함께 `TokenEntry`의 중복 `key` 필드를 지우고, 추적되지 않는 계획 문서를 가리키는 주석(`C1`·`C3`·`C4`·`C9`·`C10`·`DELTA-02`)을 자체 설명으로 바꾸고, DELTA-05에서 지워진 `### 패키지 README 사용자 가이드 재구성 (출처: [.scratch/readme-restructure/issues/01-readme-user-guide-restructure.md](.scratch/readme-restructure/issues/01-readme-user-guide-restructure.md))

- [x] **RD-047 — 패키지 README를 사용 흐름 순서의 사용자 가이드로 재구성한다.** 지금 README는 580줄에 목차가 없고, 코드 블록 밖 300자 초과 줄이 35개다. 사용자 가이드와 계약 명세가 한 파일에 섞였고 RD마다 절을 덧붙여 Renderer 사용법이 "런타임 동작"의 하위 절에 있다. 오류 코드 설명은 4곳에 흩어졌다. 절 순서를 소개·진입점 → 설치 → 시작하기 → Renderer에서 쓰기 → 프레임워크 연동 → Main 구현 → 배선 → 한도·진단 → 테스트 → 범위 밖으로 바꾸고, 문단당 규칙 1~2개로 쓴다. 모든 경우의 동작은 `docs/design/` 링크로 넘긴다(RD-046에서 이관처로 결정). 코드 변경은 없다.
  - 목차, 진입점 표 `/testing` 행, 오류 코드 표 1개(`docs/design/08-payload-and-errors.md` 기준, RPC·stream 적용 열), 종료 원인 표 1개.
  - 내부 용어(`generation`·`retire`·`manifest`·`handshake`·`wire`·`slot`·`envelope`)는 쓰지 않거나 첫 등장에서 정의한다.
  - README에서 빼는 규칙은 `docs/design/`에 있는지 확인하고, 없으면 옮긴 뒤 뺀다.
  - README 옛 절 이름을 참조하는 문서(ADR 0024, `docs/design/07-renderer-streams.md` 등)를 고친다.

  **범위 밖:** architecture.md 축약, 코드·demo 변경.

  **완료 기준:** 제안 구조 순서와 목차가 있다. 코드 블록·표 밖 300자 초과 줄 0건. README 규칙 인벤토리의 규칙마다 새 README 또는 이관처 위치가 있고 누락 0건. README의 모든 `ts`/`tsx` 블록이 타입 검사를 통과한다(TanStack 예제는 `@tanstack/react-query` 5.103.2). 상대 링크가 모두 존재한다. 옛 절 이름 참조 0건. 루트 `pnpm format:check` 통과.

  계획: `_works/20260926-04-readme-restructure/`. **결과:** 완료 조건 충족, 편차 없음. 목차와 제안 구조 10개 절, 진입점 표 `/testing` 행, 오류 코드 표 1개(9개 코드, RPC·구독 적용 열, 재시도 열), 구독 종료 원인 표 1개. 코드 블록·표 밖 300자(문자 수) 초과 줄 20→0건(이슈의 35건은 byte 기준). 내부 용어는 대체어로 바꿨다(남은 것은 ADR 파일명 링크와 진단 이벤트 이름 `handshake-failed`뿐). README 규칙 인벤토리 123건: 유지 102건, 이관 21건(이관처 `docs/design/` 절 확인, MISSING 0). 예제 31블록을 README 추출 harness로 `tsc` 6.0.3 타입 검사 통과(오류 주입 검출 확인). 상대 링크·anchor 깨짐 0건. 옛 절 이름 참조는 ADR 0024·`docs/design/07-renderer-streams.md` 2곳을 고쳤다. 독립 검토 2건(정보 손실 7건, 코드 대조 11건)을 모두 반영했다. 그중 5건은 옛 README부터 있던 오류다: 이름 규칙 누락, "structured clone 가능" 표현, Main 진단 필드, `broadcastEvent` 공유 범위 주석, loopback `clientId` 설명. 그 밖에 값 규칙을 어긴 RPC 입력은 preload가 먼저 거부해 `INTERNAL "RPC transport failed."`가 되고, TanStack 기본 key hash는 `bigint`에서 throw한다. 같은 검토에서 설계 문서 03·04·06의 loopback·`replaced` 서술 오류를 코드 기준으로 고쳤다. 후속 이슈: [bind `dispose()` 뒤 새 구독 무응답](.scratch/bind-dispose-subscribe/issues/01-bind-dispose-subscribe-no-response.md).

## 현재 범위 밖의 확장` 제목을 복원했다.

### 렌더러 문서 세션 retire interface (출처: 아키텍처 리뷰 `_works/arch-review/03.html` 후보 02)

- [x] **RD-037 — `DocumentSession`의 retire 통지를 raw `AbortSignal` 대신 세션 interface(`retireReason`·`onRetire`)로 노출하고, 이미 retire된 세션으로 들어온 구독도 ADR 0020 통지 표를 따르게 한다.** 지금 `DocumentSession`의 interface는 `{ target, clientId, signal }`이다. 호출자는 규약 4개를 안다: once 등록, 등록 직후 aborted 재검사, 정상 종료 시 remove, `signal.reason`(any) 문자열 비교. 이 규약이 `RpcRequests` 4곳과 `Subscriptions` 15곳에 흩어져 있다. `RetireReason`은 `abort(reason)`에서만 타입을 갖고, `endNotice`는 `"detach"`·`"dispose"` 문자열로 비교한다. 등록 3곳은 이미-retire 경우를 각자 다르게 처리한다. RPC는 즉시 cancel하고, 구독 pending은 무통지 return하고, consumer는 무통지 close한다. grilling 중 실측으로 pending·consumer 두 분기가 공개 seam에서 도달함을 확인했다. 진단 sink가 `session-opened`·`subscription-opened`를 받는 중 동기로 detach·dispose하면 frame을 하나도 보내지 않는다. 이는 ADR 0020(detach·dispose → `subscribed`(0) 뒤 `CANCELLED "Bridge session ended."`) 위반이다. 출처: 아키텍처 리뷰 `_works/arch-review/03.html` 후보 02, 2026-09-25 그릴링. **구조:**
  - fix를 먼저 한다(동작 변경). pending·consumer의 이미-retire도 detach·dispose면 `subscribed`(0) → `CANCELLED`, 그 외 사유는 무출력이다. consumer의 retire 처리는 open 송신 여부로 갈린다. open 전이면 시작 전 거부 창(`#endUnstarted`)으로 통지하고, open 뒤면 지금처럼 `preempt`한다. consumer 등록 위치(open 송신 앞)는 유지한다. open 뒤로 옮기면 동기 `send` 중 `server.dispose()` 통지를 잃는다.
  - interface: `retireReason: RetireReason | undefined` getter와 `onRetire(listener: () => void): () => void`. 이미 retire된 세션이면 listener를 즉시 동기 호출하고 no-op 해제 함수를 돌려준다. listener는 인자가 없고 사유는 getter로만 읽는다. `endNotice`의 사유 입력은 `RetireReason` 타입이 된다.
  - implementation: 호출자에게 보이는 interface와 module 내부 구현 class를 분리한다. class가 `AbortController`를 쥐고 `#controllers` WeakMap을 삭제한다. `retire`는 비공개다. 내부 `AbortController`를 유지해 listener 예외 전파와 등록 순서 실행을 보존한다. `onRetire`는 호출마다 독립 등록이다.
  - `RpcRequests`·`Subscriptions`에서 `session.signal`·`addEventListener`·`removeEventListener`를 없애고, interface에서 `signal`을 제거한다. `create-bridge-server.ts`·`authorization.ts`·공개 API·wire·진단 종류는 바꾸지 않는다.
  - test 표면: RD-015 그릴링 결정 5의 예외로 `test/main/document-sessions.test.ts`에서 retire interface를 직접 test한다(RD-034·RD-036과 같은 근거).

  **범위 밖(보류):** slot 회계(RPC `running`, 구독 pending+consumers). 리뷰 03 후보 04(payload limits 해석)·05(`LocalGeneration` kind 분기)와 잔재 목록. Renderer 쪽 무기한 대기 가설 검증.

  **완료 기준:**
  - fix test(pending·consumer 창 × detach·dispose)가 수정 전 RED, 수정 후 GREEN이다. RPC 이미-retire(`CANCELLED "Request cancelled."`)와 무통지 사유(navigation) characterization은 전 과정에서 GREEN이다.
  - `document-sessions.test.ts`가 사유별 `retireReason`, listener 1회·사유 가시, 해제·멱등, 이미-retire 즉시 동기 호출, 등록 순서, 같은 함수 2회 등록, retire 뒤 `current()` 거부를 고정한다.
  - 구조 이동 단계에서 기존 test 단언 변경은 0건이다.
  - `subscriptions.ts`·`rpc-requests.ts`의 `session.signal`·`sessionSignal`·`addEventListener`·`removeEventListener` 0건이다. `document-sessions.ts`의 `#controllers`·`readonly signal` 0건이다. `git diff dev -- create-bridge-server.ts authorization.ts` 빈 출력이다.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`이 통과한다.
  - 새 ADR 0023을 쓰고, ADR 0015·0020에 개정 주석(ADR 0020에는 fix 개정 포함)을 달고, `docs/architecture.md` "문서 세션과 정리"를 반영하고, 리뷰 03.html 카드 02 완료를 표시한다.

  계획: `_works/20260925-14-session-retire-interface/`. **결과:** 완료 조건 전부 충족, 편차 없음. DELTA-01(현재 raw `session.signal` 구조 그대로 pending·consumer의 이미-retire 무통지를 fix — pending은 `entry.onAbort()` 직접 호출, consumer는 `opened` 플래그로 open 전/후 분기 — 하고 RPC 이미-retire·무통지 사유(navigation) characterization 추가, fix test 4 case가 수정 전 RED·수정 후 GREEN, ADR 0020 결정 2에 RD-037 개정 주석 — 38 files/726 tests) → DELTA-02(`document-sessions.ts`에 `DocumentSession` interface·구현 class(`SessionImpl`)·`retireReason`·`onRetire` 추가, `signal`은 병존 유지, `#controllers` WeakMap 삭제, 신규 `document-sessions.test.ts` 15 case 직접 test — 39 files/741 tests) → DELTA-03(`RpcRequests`·`Subscriptions`를 `onRetire`·`retireReason`으로 이전하고 interface에서 `signal` 제거, `endNotice`·`#endUnstarted` 사유 인자를 `RetireReason` 타입으로 교체 — 신규 test 없음, 741 tests 그대로) → DELTA-04(ADR 0023 신규 작성, ADR 0015·0020 개정 주석, `docs/architecture.md` 반영, 전체 검증, 리뷰 카드 완료 표시) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest, Electron acceptance 포함) 39 files/741 tests 통과(기준 718+DELTA-01 8+DELTA-02 15+DELTA-03 0), 편차 없이 1회 통과(TRP-001·TRP-004 미사용). 루트 `pnpm lint`·`pnpm format:check` 통과(`format:check`는 신규 ADR 0023 파일 1건 미스포맷 발견 → `prettier --write` 후 재통과). demo `check-types`·`test:unit` 10 files/24 tests 통과. grep: `subscriptions.ts`·`rpc-requests.ts`의 `session\.signal|sessionSignal|addEventListener|removeEventListener` 0건, `document-sessions.ts`의 `#controllers|readonly signal` 0건, repo 전체 `session\.signal` 0건. `git diff dev --numstat -- test`: `document-sessions.test.ts` +221/-0, `rpc-requests.test.ts` +45/-0, `subscriptions.test.ts` +109/-0, 삭제 열 전부 0. `git diff dev -- create-bridge-server.ts authorization.ts` 빈 출력. **발견:** grilling 실측대로 구독 pending(`:264`)·consumer(`:431`) 이미-retire 분기가 ADR 0020을 위반했다(무통지) — DELTA-01에서 수정. RPC(`:256`)는 이미 ADR 0015대로 동작해 characterization만 추가했다. `docs/architecture.md` "문서 세션과 정리"에 retire interface 문장(ADR 0023 링크)과 DELTA-01 fix 내용을 반영했고, 리뷰 `03.html` 카드 02를 01·03과 같은 형식으로 완료 표시했다(로컬만, `_works/`는 gitignore 대상). **리뷰(2026-09-25):** `Subscriptions.#start`는 `onRetire` 등록 직후 창이 닫혀 있으면 handle을 저장하지 않고 return했다. `subscription-opened` 진단 sink가 동기 unsubscribe로 창을 먼저 닫은 경우에는 살아 있는 세션에 등록된 listener가 남았다. 그래서 나중 detach·dispose에서 해지된 구독으로 `subscribed`(0)·`CANCELLED`가 나갔다(변경 전 raw `signal` 구조에서는 unsubscribe 직후 stray `subscribed`(0) 1건). 창이 닫혀 있으면 방금 받은 handle을 해제하도록 고치고 `subscriptions.test.ts`에 회귀 test 1건을 추가했다(RED 확인 후 GREEN). ADR 0023에서 추적되지 않는 계획 문서 참조(`C2`)를 지웠다. 즉시 호출 경로의 listener 예외가 dispatch 격리 없이 호출자에게 전파된다는 점을 ADR 0023 결정 2와 `onRetire` doc에 적었다. 패키지 `xvfb-run -a pnpm verify` 39 files/742 tests, 루트 `pnpm lint`·`pnpm format:check` 통과.

### payload limits 해석 module (출처: 아키텍처 리뷰 `_works/arch-review/03.html` 후보 04)

- [x] **RD-038 — 서버 옵션 `payloadLimits`의 기본값·부분 병합·검증·동결을 `resource-limits.ts`와 같은 모양의 module `payload-limits.ts`로 옮기고, `maxTotalBytes: undefined`가 16 MiB 한도를 조용히 끄는 구멍을 막는다.** `create-bridge-server.ts`(291줄)에서 배선이 아닌 로직은 payload limits 해석(`defaultLimits`·`PAYLOAD_LIMIT_KEYS`·`assertPartialPayloadLimits`·spread 병합, `:40-98`·`:131-135`)뿐이다. resource limits는 같은 일을 전용 module(`resolveResourceLimits`, 동결한 `DEFAULT_RESOURCE_LIMITS`를 `/main`에서 공개)이 한다. payload 기본값은 비공개다. `assertPartialPayloadLimits`는 `maxDepth`·`maxEntries`·`maxStringBytes`의 명시적 `undefined`를 거부하지만 `maxTotalBytes`의 `undefined`는 통과시키고, spread 병합이 기본값 16,777,216을 지운다. 실측(loopback): `{ maxStringBytes: 100_000_000, maxTotalBytes: undefined }`면 18,000,000 byte 문자열 RPC 입력이 성공하고, `maxTotalBytes`를 생략하면 같은 입력이 `INVALID_ARGUMENT`다. TS 사용자는 `exactOptionalPropertyTypes` 때문에 이 입력을 쓸 수 없고 JS·캐스트 사용자만 도달한다. "`undefined` = 전체 크기 무제한"은 architecture.md·ADR 0004·0009·0012·README 어디에도 기록이 없다. `PayloadLimits.maxTotalBytes?`(`./protocol` 공개)가 선택 필드인 것은 envelope parse 단계의 `ENVELOPE_LIMITS`(`protocol/messages.ts:128`)가 전체 크기 무제한을 필드 생략으로 표현하기 때문이다(ADR 0016 결정 2). 출처: 아키텍처 리뷰 `_works/arch-review/03.html` 후보 04, 2026-09-25 그릴링. **구조:**
  - 동작 변경(이것만): 서버 옵션 `payloadLimits.maxTotalBytes`에 명시적 `undefined`를 넣으면 `createBridgeServer` 생성 시점에 `TypeError`다. 문구는 다른 필드와 같은 `Payload limit 'maxTotalBytes' must be a non-negative safe integer.`다. 무제한이 필요하면 `Number.MAX_SAFE_INTEGER`를 쓴다(`Infinity` 허용은 `parseBridgeValue`의 공개 규칙까지 바뀌어 기각).
  - 신규 `src/main/payload-limits.ts`: `resolvePayloadLimits(options?: Partial<PayloadLimits>): Required<PayloadLimits>`와 동결한 `DEFAULT_PAYLOAD_LIMITS: Required<PayloadLimits>`. `resource-limits.ts`와 같은 모양(옵션 생략 시 기본값 객체 그대로 반환, 알 수 없는 키 거부, 필드별 검증, 결과 동결)이다. 두 한도는 한 파일로 합치지 않는다 — 검증 규칙(0 이상 vs 1 이상, `Infinity` 허용 여부)과 소비자가 다르다.
  - `DEFAULT_PAYLOAD_LIMITS`를 `/main`에서 export한다(추가만 한다). `create-bridge-server.ts`에서 `defaultLimits`·`PAYLOAD_LIMIT_KEYS`·`assertPartialPayloadLimits`·spread 병합을 삭제한다. 한도를 받는 쪽(`Subscriptions`·`RpcRequests`·`output-boundary`·`error-serializer`)의 매개변수 타입은 `PayloadLimits` 그대로다.
  - 유지: 값 0 허용(payload에서 0은 의미 있는 값), `parseBridgeValue`의 호출마다 한도 재검증(공개 함수의 자기 방어), `PayloadLimits.maxTotalBytes`의 선택성(`ENVELOPE_LIMITS`), 검증 순서(payload limits 검증 → 등록 테이블 생성 → resource limits 해석).
  - test 표면: `test/main/payload-limits.test.ts`에서 해석 규칙을 직접 test한다(`resource-limits.test.ts`와 같은 근거). 기존 server-level test(`create-bridge-server-impl.test.ts` `describe("createBridgeServer(impl, options): payloadLimits")`)는 배선 검증으로 남긴다.

  **범위 밖(보류):** 리뷰 03 후보 05(`LocalGeneration` kind 분기)와 잔재 목록(`BridgeServer`·`CommonServerOptions`·`buildBridgeServer`·`TransportErrorCode`·registration manifest 재조립·`RemoteError` 변환 2벌). payload·resource 한도 규칙 통일(0 허용, `Infinity`). `parseBridgeValue`의 재검증 생략 경로.

  **완료 기준:**
  - `{ maxTotalBytes: undefined }` 거부 test를 현재 구조의 server seam에 먼저 넣고 수정 전 RED, 수정 후 GREEN을 확인한다.
  - `payload-limits.test.ts`가 기본값 반환(동일 객체), 부분 병합, 결과 동결, 알 수 없는 키, 필드별 거부(`undefined` 포함), 값 0 허용을 고정한다.
  - 구조 이동 단계에서 기존 test 단언 변경은 0건이다.
  - `create-bridge-server.ts`의 `defaultLimits|PAYLOAD_LIMIT_KEYS|assertPartialPayloadLimits` 0건이다.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`이 통과한다.
  - ADR 0009 결정 5에 개정 주석을 달고, `docs/architecture.md` "Payload 및 제한"과 README payload 절에 `undefined` 거부·`DEFAULT_PAYLOAD_LIMITS`를 반영하고, 리뷰 03.html 카드 04 완료를 표시한다. 신규 ADR은 쓰지 않는다.

  계획: `_works/20260925-15-payload-limits-resolution/`. **결과:** 완료 조건 전부 충족, 편차 없음(TRP-001·TRP-004 미사용). DELTA-01(현재 구조 그대로 server seam(`create-bridge-server-impl.test.ts`)에 `{ maxTotalBytes: undefined }` 거부 `test.each` 4행 추가 — `maxTotalBytes` 행이 수정 전 RED·나머지 3행 GREEN, `assertPartialPayloadLimits`의 `maxTotalBytes` 전용 통과 블록을 제거해 GREEN 전환, 기존 test 단언 무변경 — 39 files/746 tests) → DELTA-02(신규 `src/main/payload-limits.ts`에 `resolvePayloadLimits`·동결한 `DEFAULT_PAYLOAD_LIMITS`를 `resource-limits.ts`와 같은 모양(undefined 시 기본값 객체 그대로 반환, 알 수 없는 키 우선 검사, `KEYS` 순회 + `Object.hasOwn`, 결과 동결)으로 추출, `/main`에서 export, `create-bridge-server.ts`에서 `defaultLimits`·`PAYLOAD_LIMIT_KEYS`·`assertPartialPayloadLimits` 삭제, 신규 `payload-limits.test.ts` 직접 test 36건 — 40 files/782 tests) → DELTA-03(ADR 0009 결정 5에 `_(개정: RD-038 — …)_` 주석, `docs/architecture.md` "Payload 및 제한"·README payload 절에 `undefined` 거부와 `DEFAULT_PAYLOAD_LIMITS` 반영, 신규 test 없음, 782 tests 그대로) → DELTA-04(전체 검증, ROADMAP·리뷰 카드 완료 표시) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest, Electron acceptance 포함) 40 files/782 tests 통과(기준 742+DELTA-01 4+DELTA-02 36), 24.4초, 1회 통과. 루트 `pnpm lint`·`pnpm format:check` 통과. demo `check-types`·`test:unit` 10 files/24 tests 통과. grep: `create-bridge-server.ts`의 `defaultLimits|PAYLOAD_LIMIT_KEYS|assertPartialPayloadLimits` 0건, `index.ts`의 `DEFAULT_PAYLOAD_LIMITS` 1건. `git diff dev -- src/protocol` 빈 출력. `git diff dev --stat -- src/main`은 `create-bridge-server.ts`(-53)·`index.ts`(+1)·`payload-limits.ts`(신규 +37) 3개뿐. `git diff dev --numstat -- test`: `create-bridge-server-impl.test.ts` 18/0, `payload-limits.test.ts` 114/0, 삭제 열 전부 0. **발견:** DELTA-01·02·03 모두 `pnpm format:check` 1차 실행에서 신규/개정 코드 블록의 줄바꿈이 prettier 규칙과 달라 실패 → `prettier --write`로 자동 정리 후 재통과(내용 변경 없음, 편차 아님). 그 외 계획과의 차이 없음.

### 리뷰 03 잔재 정리 (출처: 아키텍처 리뷰 `_works/arch-review/03.html` 잔재 섹션)

- [x] **RD-039 — 리뷰 03 잔재 5건을 동작 변경 없이 정리한다: registration 중복 필드, `RemoteError` 변환 2벌, `CommonServerOptions`, `TransportErrorCode` 누락·미사용, `BridgeServer` 부모 interface.** 2026-09-25 재확인 기준(`dev` @ `e7e1ea5`): 등록 항목의 `domainName`·`operation`은 `bridgeOperation`과 겹치고 manifest 목록(`manifestCategoryList`)만 읽는다. `RpcErrorPayload → RemoteError` 변환이 `rpc-client.ts`·`stream-multiplexer.ts`에 2벌이다. `CommonServerOptions`·`buildBridgeServer`는 호출자가 1개이고, "테이블 만드는 방법과 분리" 주석의 두 번째 방법이 없다. `TransportErrorCode`는 `./protocol`에서 공개되지만 사용처가 0곳이고, wire로 나가는 `STREAM_OVERFLOW`가 빠져 있다. 라이브러리 오류 code는 `string` 인자로 약 50곳에서 만들어져 컴파일러가 검사하지 못한다. `BridgeServer`는 `StreamBridgeServer`의 부모로만 쓰이고 부모 단독 호출자가 0이다. **구조:**
  - manifest: 등록 항목의 `domainName`·`operation`을 삭제하고 목록은 `bridgeOperation`(`domain`·`operation`·`key`)으로 만든다. 순서(도메인명 → operation명, wire key 정렬과 다름)는 유지한다.
  - `remote-error.ts`의 `remoteErrorFromPayload`가 원격 오류 payload 변환을 전담한다(비공개).
  - `CommonServerOptions`를 삭제하고 필드를 `ImplServerOptions`에 직접 둔다. `buildBridgeServer`는 해석된 값(payload·resource limits, `authorize`, `diagnostics`)만 받는다. 생성 시점 검증 순서(payload limits → 등록 → resource limits)는 유지한다.
  - `TransportErrorCode`를 `protocol/error-code.ts`로 옮기고 `STREAM_OVERFLOW`를 추가한다. `localError`·`protocolError`·`BridgeProtocolError`·`rpc-requests`의 `error()`를 이 타입으로 좁히고, 객체 리터럴은 비공개 `LibraryErrorPayload`로 검사한다. `RemoteError.code`·`RpcErrorPayload.code`는 선언 오류를 싣기 위해 `string`으로 둔다.
  - `BridgeServer`를 `StreamBridgeServer`에 병합·삭제한다. 이름은 `StreamBridgeServer`로 유지한다.

  **범위 밖:** `TransportErrorCode`·`StreamBridgeServer` 개명, 과거 ADR·ROADMAP 본문의 이름 수정, 리뷰 03 후보 05(`LocalGeneration` kind 분기, 채택 안 함).

  **완료 기준:**
  - 런타임 동작 불변. 기존 test 단언 변경 0건(`upstreams.test.ts` fixture 필드 6줄 삭제만 허용).
  - stream 원격 오류 `details` 전달과 생성 시점 검증 순서를 characterization test로 먼저 고정한다.
  - 목록에 없는 code를 helper·리터럴에 넣으면 컴파일 오류임을 probe로 확인한다.
  - `grep -rnw BridgeServer`(패키지 src·test·README, `docs/architecture.md`) 0건, `CommonServerOptions` 0건.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`이 통과한다.
  - ADR 0016에 개정 주석을 단다. 신규 ADR은 쓰지 않는다.

  계획: `_works/20260925-16-residue-cleanup/`. **결과:** 완료 조건 전부 충족, 편차 없음. DELTA-01(등록 항목의 `domainName`·`operation` 삭제, `manifestCategoryList`를 `bridgeOperation.domain`·`operation`·`key` 기반으로 교체, `upstreams.test.ts` fixture 6줄 삭제 — 40 files/784 tests 불변) → DELTA-02(`remote-error.ts`에 `remoteErrorFromPayload` 도입해 `rpc-client.ts`·`stream-multiplexer.ts`의 중복 변환 제거, stream 원격 오류 `details` 전달 characterization 1건이 변경 전 GREEN 확인 후 구현 — 785 tests) → DELTA-03(`CommonServerOptions` 삭제, `ImplServerOptions`에 필드 직접 선언, `buildBridgeServer`는 해석된 `ResolvedServerConfig`만 받음, 등록 오류 우선 검증 순서 characterization 1건이 변경 전 GREEN 확인 후 구현 — 786 tests) → DELTA-04(`TransportErrorCode`를 `protocol/error-code.ts`로 이동해 `STREAM_OVERFLOW` 추가, `BridgeProtocolError`·`localError`·`protocolError`·`rpc-requests`의 `error()`를 이 타입으로 좁힘, 이름 있는 상수 5곳·인라인 리터럴 6곳을 비공개 `LibraryErrorPayload`로 컴파일 타임 검사, probe로 4개 오용 경로 모두 컴파일 오류 확인 후 삭제, `pnpm build` 뒤 preload 번들 `rxjs` 오염 0(TRP-002) — 786 tests 불변) → DELTA-05(`BridgeServer`를 `StreamBridgeServer`에 병합·삭제, `/main` export 제거, README `:413`·ADR 0016 개정 주석 — 786 tests 불변) → DELTA-06(전체 검증, 이 항목·리뷰 카드 완료 표시) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest, Electron acceptance 포함) 40 files/786 tests 통과(기준 784+DELTA-02 1+DELTA-03 1), 1회 통과. 루트 `pnpm lint`·`pnpm format:check` 통과. demo `check-types`·`test:unit` 10 files/24 tests 통과. grep: `registration.ts`의 `readonly (domainName|operation): string` 0건, `src/renderer`의 `new RemoteError(` 3건(모두 `remote-error.ts`), `CommonServerOptions` 0건, `TransportErrorCode =` 정의 1건(`protocol/error-code.ts`), `BridgeServer`(패키지 src·test·README, `docs/architecture.md`) 0건. `git diff dev --numstat -- test`: `create-bridge-server-impl.test.ts` +9/0, `remote-event.test.ts` +32/0, `upstreams.test.ts` 0/-6(허용 편차), 그 외 삭제 열 0. **발견:** DELTA-05에서 `pnpm format:check` 1차 실행이 `create-bridge-server.ts`·`stream-multiplexer.ts`(DELTA-02에서 개정된 줄) 2파일 줄바꿈 실패 → `prettier --write`로 정리 후 재통과(내용 변경 없음). 그 외 계획과의 차이 없음.

### 닫힌 구독의 `stream-queue` 꼬리 제거 (출처: `.scratch/delivery-window-dropped-diagnostic-order/issues/01-stream-dropped-tail-after-subscription-closed.md`, RD-034 보류 항목)

- [x] **RD-040 — `stream-dropped` 진단 도중 구독이 닫히면 뒤이은 `stream-queue` 진단을 기록하지 않는다.** 2026-09-25 재확인 기준(`dev` @ `0b097bd`): `DeliveryWindow.accept()`(`src/main/delivery-window.ts:233-249`)는 push 뒤 `onDropped`(dropped > 0) → `onQueueDepth`(push 뒤 depth)를 모두 부른 다음에야 `#closed`·`#concluded`를 확인한다. 진단 sink가 `stream-dropped`를 받는 중 동기로 detach·retire·unsubscribe하면 `subscription-closed` 뒤에 같은 구독의 `stream-queue`가 1건 더 기록된다(`..., stream-dropped(count 1), session-closed, subscription-closed, stream-queue(depth 2)`). retire 경로에서 이 depth는 `preempt`가 buffer를 비우기 전 값이라 이미 사실과 다르다. overflow 정책 3종(`drop-oldest`·`drop-newest`·`error`)이 모두 이 경로를 탄다. shift 뒤 경로(`#flush`)는 callback 직후 상태를 이미 재확인한다(RD-034 결정 12). 이 순서는 RD-034 checklist 결정 13이 보존 대상으로 고정했고, `test/main/subscriptions.test.ts`의 "detach from the diagnostics sink during stream-dropped ends the stream with CANCELLED"와 `test/main/delivery-window.test.ts`의 "onDropped 안에서 preempt해도 onQueueDepth는 불리고 accept는 무출력이다"가 단언한다. **구조:**
  - `accept()`에서 `onDropped` 호출 직후 창이 종결·닫힘이면 `onQueueDepth`를 부르지 않고 무출력(`{ message: undefined, overflowed: false }`)으로 반환한다. 판정은 창 안에 둔다. shift 뒤 guard와 같은 규칙이다.
  - 정상 경로 순서(`stream-dropped` → `stream-queue`)와 빈도(push·shift마다, ADR 0010 결정 13)는 그대로다. wire 메시지·sequence·`getDiagnosticsSnapshot()` 값은 바뀌지 않는다.
  - 결정 6(효과 목록 반환형 미사용)은 유지한다.
  - **동작 변경(이것만):** 위 재진입 경로에서 `stream-queue` 1건이 사라진다. 결과적으로 한 구독에 대한 진단은 `subscription-closed` 뒤에 나오지 않는다.

  **범위 밖:** `Subscriptions` 쪽 진단 callback 필터링, 진단 이벤트 종류·필드 변경, `stream-queue` 빈도 조정, RPC 진단 순서, Renderer 진단.

  **완료 기준:**
  - 수정 전 코드에서 RED를 확인한 뒤 고친다. 대상: seam test 3종(retire × `drop-newest`·`error`, unsubscribe × `drop-oldest`)과 기존 단언 2건.
  - 기존 test 단언 변경은 위 2건으로 한정한다.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`이 통과한다.
  - ADR 0010 결정 13에 개정 주석을 단다. `docs/architecture.md` 운영 진단 절에 1문장을 추가한다. 신규 ADR은 쓰지 않는다. `.scratch` 이슈를 closed로 바꾼다.

  계획: `_works/20260925-17-dropped-queue-tail/`. **결과:** 완료 조건 전부 충족, 편차 없음(TRP-001·TRP-004 미사용). DELTA-01(`DeliveryWindow.accept()`에서 `onDropped` 호출 직후 `#closed`·`#concluded` 재확인 guard 추가 — RED 먼저 확인: 수정 전 `npx vitest run test/main/subscriptions.test.ts test/main/delivery-window.test.ts` 2 files/93 tests 중 6 failed(seam 기존 1 + seam 신규 retire 2 case + seam 신규 unsubscribe 1 + 창 기존 1 + 창 신규 1), wire 단언은 4곳 모두 수정 전 GREEN(진단 꼬리만 RED) → guard 추가 후 GREEN 전환, doc 주석(`:226-232`) 갱신 — 40 files/790 tests) → DELTA-02(ADR 0010 결정 13에 `_(개정: RD-040 — …)_` 주석, `docs/architecture.md` "운영 진단" 절 1문장 추가, `.scratch` 이슈 `Status: closed (RD-040)`) → DELTA-03(전체 검증, ROADMAP 완료 표시) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest, Electron acceptance 포함) 40 files/790 tests 통과(기준 786+신규 4), 1회 통과. 루트 `pnpm lint`·`pnpm format:check` 통과. demo `check-types`·`test:unit` 10 files/24 tests 통과. grep: `delivery-window.ts`의 `현재 진단 순서 보존` 0건. `git diff dev --stat -- src`는 `delivery-window.ts`(+9/-4) 1파일뿐. `git diff dev -- test`의 삭제 줄(파일 헤더 제외) 4줄 전부 계획한 두 test(`delivery-window.test.ts` 제목·events 단언, `subscriptions.test.ts`의 `.slice(-4)`→`.slice(-3)`·`stream-queue` expect)에 속한다. `subscriptions.test.ts:1226`·`:1371`의 재진입 test는 라인만 `:1337`·`:1482`로 이동, 내용 무변경. **발견:** 계획과의 차이 없음.

### 세션 slot 회계 단일 소유 (출처: 아키텍처 리뷰 `_works/arch-review/04.html` 후보 01)

- [x] **RD-041 — 세션 소유 자원 1건의 slot 회계와 retire 연동을 `RpcRequests`·`Subscriptions` 안 내부 module `SessionSlots`로 모은다.** 동작을 보존하는 refactor이며 버그 수정이 아니다. 2026-09-25 확인 기준(`dev` @ `f981884`): `RpcRequests`는 세션별 `running` 카운터와 전역 `#inFlight`를 따로 증감한다(`src/main/rpc-requests.ts:98-104`, `:235-247`). `Subscriptions`는 `pending.size + consumers.size`로 한도를 판정하고 `#liveStates` 순회로 집계한다(`src/main/subscriptions.ts:171-176`, `:243-245`). retire handle 규약(no-op 초기값 `releaseRetire`, 등록 직후 identity 재확인, `window.closed` 뒤 해제)은 등록 3곳(RPC entry, 구독 pending, 구독 consumer)이 각자 지킨다. 리뷰 02 slot 누수와 ADR 0023 리뷰 수정(닫힌 consumer에 남은 listener의 stray `CANCELLED`)이 이 틈에서 나왔다. **구조:**
  - 신규 `src/main/session-slots.ts`의 `SessionSlots`(세션당 한도를 받는 class)와 `SlotLease`. `acquire(session)`은 한도를 넘으면 `undefined`다. `count()`는 release 전 lease 수이며 retire된 세션도 센다(ADR 0010 §10). lease는 `onRetire(listener)`·`offRetire()`·`release()`·`released`를 가진다. 이미 retire된 세션이면 `onRetire`가 즉시 동기 호출한다(ADR 0023 결정 2 유지). release된 lease의 `onRetire`는 등록하지 않는다(세션이 이미 retire면 즉시 1회 호출, 살아 있으면 no-op — DELTA-02a 개정). 이중 등록은 throw한다. `offRetire()`는 slot을 유지하고 listener만 뗀다.
  - 인스턴스는 2개다. `RpcRequests`가 `maxConcurrentRpc`용, `Subscriptions`가 `maxSubscriptions`용을 각자 생성자 안에서 만든다. 생성자 시그니처와 `create-bridge-server.ts` 배선은 바꾸지 않는다.
  - RPC: `#cancelActive`는 `offRetire()`(slot 유지, ADR 0009 §10), `finally`는 `release()`. `running`·`#inFlight`·`#tryAcquire`·`#release`·`releaseRetire`를 삭제한다. id map과 중복 `requestId` 선취소는 남는다.
  - 구독: 1건 = lease 1개(pending 등록부터 consumer close까지). pending → consumer 이음에서 listener만 교체한다. 한도 판정식·`releaseRetire`·`#start`의 `window.closed` 뒤 해제를 삭제한다. watermark·id map·`#liveStates`·`#pruneIfEmpty`는 `queuedEvents`·`dispose` 순회용으로 남는다.
  - `SessionSlots`는 `type DocumentSession`만 import한다. 진단 sink·wire를 모른다. 한도 초과 진단과 응답은 호출자에 남는다(리뷰 04 후보 03 범위).
  - 관측 가능한 동작을 모두 보존한다: wire 메시지 순서·sequence·코드·문구, 진단 종류·순서, snapshot 값(동기 sink 안에서 읽은 값 포함), slot 반환 시점, retire listener 등록·호출 순서.
  - test 표면: RD-015 그릴링 결정 5의 예외로 `test/main/session-slots.test.ts`에서 직접 test한다(RD-034·RD-036과 같은 근거).

  **진행 순서:** 리뷰 04 후보 02(registration leaf reader, 재현된 설정 결함)를 먼저 한다. 가치 순서이며 기술 의존은 없다(파일 겹침 없음).

  **범위 밖:** 리뷰 04 후보 02~06. `#liveStates`·watermark·id map 제거. `create-bridge-server.ts`·`document-sessions.ts`·공개 API·wire·오류 코드·문구·진단 종류.

  **완료 기준:**
  - refactor 전에 server seam characterization test 5건(C1 중복 `requestId` 뒤 앞 slot 유지, C2 pending+active 혼합 한도와 pending → consumer 전환 뒤 slot 유지, C3 pending이 있는 `server.dispose()` 뒤 `subscriptions`·`queuedEvents` 0, C4 detach 시 pending slot 즉시 반환, C5 session-opened sink retire RPC 뒤 `rpcInFlight` 0)을 추가하고 수정 전 코드에서 GREEN을 확인한다.
  - `session-slots.test.ts`가 lease 규칙(한도·격리·retire 뒤 집계·release 멱등·즉시 호출·release 뒤 무시·이중 등록 throw·`offRetire`·listener 안 재진입·등록 순서)을 고정한다.
  - 기존 test 단언 변경은 0건이다. `git diff dev -- create-bridge-server.ts document-sessions.ts` 빈 출력이다.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`이 통과한다.
  - 새 ADR은 쓰지 않는다. ADR 0010 §10·0015·0023 "범위 밖"에 개정 note를 달고, `CONTEXT.md` "구독"·"RPC 요청"과 `docs/architecture.md` slot 서술에 1문장씩 반영하고, 리뷰 04.html 카드 01 완료를 표시한다.

  계획: `_works/20260925-18-session-slots/`. **결과:** 완료 조건 전부 충족, 편차 1건(계획 밖 승인 변경, 아래). DELTA-01(characterization test C1~C5 추가, 수정 전 코드에서 GREEN 확인 — 40 files/796 tests) → DELTA-02(`session-slots.ts`의 `SessionSlots`·`SlotLease` 신규, 직접 test 15건, 아직 배선 안 함 — 41 files/811 tests) → DELTA-03(`RpcRequests`를 `SessionSlots`로 전환, `running`·`#inFlight`·`#tryAcquire`·`#release`·`releaseRetire`·`session.onRetire` 문자열 0건) → **DELTA-02a(계획 밖, DELTA-04에서 발견)**: `Subscriptions.dispose()`가 아직 `#start()`에서 `lease.onRetire`를 등록하기 전인 consumer를 먼저 `#close()`로 닫아 lease를 release하는 경쟁에서, `SlotLease.onRetire`의 원래 계약("release된 lease는 등록·호출 모두 no-op")이 옛 `session.onRetire`(release 개념 없음, 세션이 이미 retire면 등록 즉시 무조건 호출)와 다른 결과(`test/main/subscriptions.test.ts:2049`의 `server.dispose()` 케이스에서 CANCELLED 통지 소실)를 냈다. 사용자 확인(2026-09-25) 뒤 계약을 개정했다: release된 lease라도 등록 시점에 세션이 이미 retire돼 있으면 listener를 즉시 1회 호출한다(등록은 남기지 않음). 세션이 아직 살아있으면 순수 no-op은 유지한다(기존 case 불변). `session-slots.test.ts`에 case 1개 추가(16 case) — 41 files/812 tests. → DELTA-04(`Subscriptions`를 `SessionSlots`로 전환, K2 불변식(`subscriptions.ts` pending→consumer 이음 구간에 외부 호출 없음) 코드로 재확인, grep 3종 0건) → DELTA-05(`CONTEXT.md`·`docs/architecture.md`·ADR 0010 §10·0015·0023 "범위 밖"에 개정 note) → DELTA-06(전체 검증) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest, Electron acceptance 포함) 41 files/812 tests 통과(기준 790+DELTA-01 6+DELTA-02 15+DELTA-02a 1), 1회 통과, TRP-001·TRP-004 미발동. 루트 `pnpm lint`·`pnpm format:check` 통과. demo `check-types`·`test:unit` 10 files/24 tests 통과. grep 3종·`git diff` 3종 전부 조건대로(본문 참고). `git diff dev --stat -- src`는 `session-slots.ts`(신규, +145)·`rpc-requests.ts`(+45/-57)·`subscriptions.ts`(+69/-52) 3파일뿐이다. `git diff dev -- test`의 삭제 열은 전부 0(기존 단언 변경 없음). **발견:** (1) C4(`test/main/subscription-limits.test.ts`의 "대기 중 세션 detach는 즉시 slot을 반환하고 CANCELLED를 통지한다")는 기존 `test/main/subscriptions.test.ts:1113`("detach during pending authorization silences a later authorize exception")와 조건은 겹치나 단언(authorize resolve 경로, detach 즉시 `subscriptions` 0)이 달라 별도로 추가했다(DELTA-01 "## 결정"). (2) C3의 Event 대기열 실측값은 `queuedEvents === 2`(capacity 2, overflow 없음). (3) 결정 10대로 `#liveStates`·`#pruneIfEmpty`는 `queuedEventsCount`·`dispose` 순회용으로 남아 리뷰 카드 "liveStates 흡수"는 일부만 달성했다. (4) DELTA-02a — 위 계획 밖 변경.

- [x] **RD-042 — stream test의 wire 조립을 test 전용 드라이버 `renderer-document.ts`로 옮긴다.** 동작 변경 없는 test 구조 이동이다. 2026-09-25 확인 기준(`dev` @ `4d03d0d`): test/main의 server 호출(`controlStream`·`dispatchRpc`·`handshake`·`cancel`)이 차지하는 줄은 전체 11,884줄 중 2,236줄이고, stream 파일에 몰려 있다(`subscription-watermark` 64%, `subscription-limits` 51%, `subscriptions` 31%, `diagnostics-lifecycle` 31%; RPC 파일은 7~12%). 파일마다 `command`·`ack`·`subscribeCommand` helper를 다시 만들고(빌더 23벌), clientId 기본값이 `client-1`·`document-1`·`doc-1`로 갈라졌으며, ack sequence와 watermark 규칙(`testSubscriptionId(n)` 증가)을 호출하는 test가 직접 안다. 이 4파일은 9/23 이후 27개 커밋에서 수정됐다. **구조:** 드라이버는 Renderer 문서 1개(sender + clientId)만 소유한다 — subscriptionId 발급(server 단위 증가), 구독별 수신 frame 기록, `ack(sequence?)`, `unsubscribe()`, 대기 구독용 `begin`. server·`FakeTarget`·수명 사건·`dispose`는 test에 남는다. escape hatch는 `subscribe(key, { id?, onFrame? })`뿐이고 비정상 envelope·잘못된 id·handshake·RPC는 raw 호출로 남긴다. **완료 기준:**
  - 4파일(`subscription-watermark`·`subscription-limits`·`diagnostics-lifecycle`·`subscriptions`)의 test 개수가 이관 전과 같고, `expect` 단언 대상·값 변경이 0건이다(파일별 mutation RED 확인).
  - 이관 전후 test별 server 호출 기록(method, sender, value)이 같다.
  - 4파일에 자체 wire helper가 없고, 영어 test 제목이 없으며, 파일 개요 주석이 있다(제목 정리는 이관과 별도 커밋).
  - `src/` 변경이 없다. 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`가 통과한다.
  - 리뷰 04.html 카드 04를 축소 채택으로 갱신한다(로컬). 나머지 test/main 파일과 RPC 쪽 이관은 범위 밖이다.

  계획: `_works/20260925-19-stream-test-driver/`. **결과:** 완료 조건 전부 충족, 편차 없음. DELTA-01(드라이버 `test/main/renderer-document.ts`와 직접 test 10개, mutation 3종 RED 확인. 이관 전 코드에서 test별 server 호출 기록을 떴다 — 임시 vitest setup이 `createBridgeServer`를 감싸 (method, sender, value)를 기록, 커밋하지 않음) → DELTA-02~05(파일별 이관 커밋 + 제목·주석 커밋 분리) → DELTA-06(검증·문서) 순으로 진행. 파일별 수치(test 수 전후 동일, `expect(` 개수 동일, 호출 기록 diff): `subscription-watermark` 7/7·11/11·diff 0, 271→130줄; `subscription-limits` 18/18·51/51·diff 0, 802→394줄; `diagnostics-lifecycle` 23/23·49/49·diff 0, 522→429줄; `subscriptions` 69/69·186/186·diff 0, 2,142→1,477줄(합계 3,737→2,430줄). 단언은 값 변경 0건이고 대상 변수만 handle의 `frames`·`types()`로 바뀌었다. 파일마다 단언 3~7개를 틀리게 바꿔 RED를 확인했다. 드라이버를 거치지 않는 raw 호출: 형식 오류 id 2건, `subscription-opened` 진단 안의 unsubscribe 1건(handle 반환 전 동기 진단), handshake·dispatchRpc 전부. 한글 없는 제목은 0건이다(`subscriptions` 65건·`diagnostics-lifecycle` 2건 번역). 검증: 패키지 `xvfb-run -a pnpm verify` 43 files/826 tests(기준 816 + 드라이버 10) 통과, 루트 `pnpm lint`·`pnpm format:check` 통과, demo `check-types` 통과, `git diff dev -- packages/rx-bridge-electron/src` 빈 출력. **발견:** 계획 시점의 "영어 제목 79건"은 첫 글자가 영문자인 제목을 센 값이었다(`maxSubscriptions`·`authorize`로 시작하는 한글 제목 포함). 한글이 없는 제목 기준으로 다시 세면 watermark 0, limits 0, lifecycle 1, subscriptions 60(+describe 5)이다. architecture·ADR에는 Main test seam 서술이 없어 문서 변경은 없다.

### RemoteState 프레임워크 연동 adapter (출처: 2026-09-25 그릴링 "실제 사용처를 위한 편의 함수 제공 검토")

- [x] **RD-043 — `/renderer`에 `RemoteState`를 외부 store 계약으로 옮기는 프레임워크 중립 adapter `snapshotStore`를 추가하고, React는 README 레시피로 안내한다.** 공개 export 추가이며 기존 호출 형태는 바뀌지 않는다(이전 작업 불필요). 2026-09-25 확인 기준(`dev` @ `50fb265`): React 사용처는 `apps/demo/src/renderer/use-remote-state.ts`(26줄, 사용 6곳: `App.tsx` 5·`RelayPanel.tsx` 1)를 직접 작성해야 하고, 패키지·README는 "Observable + `.snapshot`"만 안내한다. 직접 작성할 때 틀리기 쉬운 지점은 셋이다: (1) `error`·`complete`도 변경 알림으로 받지 않으면 `stale` 전이가 렌더되지 않고 rxjs가 미처리 error를 보고한다. (2) subscribe 함수 참조가 렌더마다 바뀌면 재구독으로 generation이 새로 열린다. (3) 원격 종료 뒤에는 재구독하지 않는다. `snapshot`은 `LocalGeneration#snapshot` 필드를 그대로 돌려주므로(`src/renderer/local-generation.ts:49`) 변경이 없으면 같은 참조다. **구조:**
  - `snapshotStore<T>(state: RemoteState<T>): RemoteStateStore<T>`. 반환 모양은 `{ subscribe(onChange: () => void): () => void; getSnapshot(): RemoteStateSnapshot<T> }`이다. `subscribe`는 `state`를 구독해 `next`·`error`·`complete`마다 `onChange`를 부르고, error는 알림으로만 쓰고 삼킨다. 반환 함수는 그 구독을 해제한다.
  - `WeakMap` 캐시로 같은 `state` 객체에는 같은 store(같은 `subscribe`·`getSnapshot` 참조)를 돌려준다. React는 `useCallback` 없이 `useSyncExternalStore(s.subscribe, s.getSnapshot)`로 쓴다.
  - 입력은 공개 인터페이스 `RemoteState<T>`다(`RemoteStateClient`에 묶지 않는다 — 사용자 fake도 받는다). `RemoteState`·`RemoteStateSnapshot` 타입은 바꾸지 않는다.
  - 원격 complete/error 뒤 재구독하지 않는다(`stale`/`uninitialized`에서 멈춤). 종료 원인(`RemoteError`)은 노출하지 않는다 — 필요하면 Observable을 직접 구독한다.
  - React 의존성·`/react` subpath·별도 패키지는 만들지 않는다. "현재 범위 밖의 확장"의 "React 전용 패키지" 제외를 유지하고, 중립 adapter와 레시피는 그 제외에 해당하지 않는다고 새 ADR 0024에 기록한다.
  - demo `use-remote-state.ts`는 유지하되 `snapshotStore` 위에서 다시 쓰고, `useSyncExternalStore`의 세 번째 인자(getServerSnapshot)를 뺀다(Electron renderer에 SSR 없음). README 레시피와 같은 코드다.

  **범위 밖:** Event·RPC 편의 기능, TanStack Query 연동 예제 문서화(후속 이슈 `.scratch/renderer-framework-integration/issues/`에 등록 — TanStack은 demo 의존성에 넣지 않고 문서 예제로만 둔다). 자동 재구독·`retry()`. `RemoteStateSnapshot`에 error 추가. Svelte·Vue 레시피. `src/renderer/local-generation.ts` 동작 변경.

  **완료 기준:**
  - `test/renderer/`의 `snapshotStore` 단위 test가 다음을 검증한다: `next`·`error`·`complete`마다 `onChange` 호출, 같은 state에 같은 store·함수 참조, error가 rxjs 미처리 오류로 보고되지 않음, 반환 함수 호출 시 구독 해제(generation 종료), `getSnapshot`이 `state.snapshot`과 같은 참조. RED 먼저 확인한다.
  - `test/renderer/create-renderer-api.test.ts`의 공개 런타임 export 봉인 test는 목록에 `snapshotStore`를 더하는 것만 바뀐다(의도한 단언 변경 1건).
  - demo `use-remote-state.ts`가 `snapshotStore` 기반으로 줄고, 기존 demo test 2건(rerender 뒤 upstream 구독 1회, current → stale 렌더)이 단언 변경 없이 통과한다.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit`이 통과한다.
  - 새 ADR 0024(중립 adapter 결정, 재구독·error 비노출, React 전용 제외 해석), README "프레임워크 연동" 절(`RemoteState` 설명 뒤, React 레시피 1개와 종료 후 멈춤·원인 확인 방법, 다른 프레임워크는 adapter 모양만 한 줄), `docs/architecture.md` 패키지 경계 표의 `/renderer` 행과 "RPC와 스트림 계약" State 항목 1문장, 후속 `.scratch` 이슈를 반영한다.

  계획: `_works/20260925-20-remote-state-store/`. **결과:** 완료 조건 전부 충족, 편차 없음. DELTA-01(`snapshotStore` 구현 — `RemoteStateStore` 인터페이스, `WeakMap` 캐시로 같은 state에 같은 `subscribe`·`getSnapshot` 참조, `this` 없는 closure, `Object.freeze`, `renderer/index.ts` export, 봉인 test 갱신, `test/renderer/snapshot-store.test.ts` 8 case 신규. RED: `snapshot-store.ts` 없는 상태에서 8 test 전부 `snapshotStore is not a function` 실패 → GREEN: 2 files/41 tests(신규 8+기존 33) 통과. mutation (a) `error`·`complete` handler 제거 → 3건 실패, mutation (b) `WeakMap` 캐시 제거 → case1 1건 실패, 둘 다 확인 뒤 원복. case7·8 실측: dispose 뒤 subscribe·사용자 fake 구독 모두 `onChange`가 동기 1회 호출됨을 확인) → DELTA-02(demo `use-remote-state.ts`를 `snapshotStore` 위로 재작성, `useCallback`·세 번째 인자 제거 — `check-types` 통과, `test:unit` 10 files/24 tests 통과, 호출부·test 파일 diff 0. 회귀 관찰: `WeakMap` 캐시를 일시 제거하고 재build한 결과 demo `use-remote-state.test.tsx` 2건과 무관 test 1건이 "Maximum update depth exceeded"로 연쇄 실패 — 캐시가 `useSyncExternalStore` 계약(subscribe 참조 안정성)에 필수임을 실측 확인 후 원복) → DELTA-03(ADR 0024, README "프레임워크 연동" 절, `docs/architecture.md` "패키지 경계"·"RPC와 스트림 계약" 2곳 반영 — `prettier --check` 대상 3파일 통과, README 레시피 코드와 demo hook 완전 일치 확인. 발견: 이 시점 루트 `pnpm format:check` 전체 실행은 DELTA-01에서 커밋된 `snapshot-store.ts`의 사전 존재 포맷 이슈로 실패했으나 이번 DELTA가 만든 3파일만은 통과 — 범위 밖으로 판단하고 넘김) → DELTA-04(후속 이슈 2건 `.scratch/renderer-framework-integration/issues/`에 등록 — Event·RPC 편의 기능, TanStack Query 문서 예제. `Status: open`·`## Comments` 절 포함, 루트 `pnpm format:check` 통과) → DELTA-05(전체 검증, ROADMAP 결과 기록) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify`(build+check-types+vitest) 44 files/840 tests 통과, 1회 통과. 루트 `pnpm lint`·`pnpm format:check` 통과(DELTA-03의 포맷 이슈는 이후 별도 커밋 `9a26bf1`로 해소됨을 재확인). demo `check-types` 통과, `test:unit` 10 files/24 tests 통과. `git diff dev -- packages/rx-bridge-electron/src/renderer/local-generation.ts packages/rx-bridge-electron/src/contract apps/demo/test` 빈 출력. `git diff dev --stat -- packages/rx-bridge-electron/src`는 `renderer/index.ts`(+1)·`renderer/snapshot-store.ts`(신규, +43) 2파일뿐. `git diff dev -- packages/rx-bridge-electron/test`의 기존 파일 삭제 줄은 봉인 test 제목·기대 배열 목록 각 1줄뿐. **발견:** 계획과의 차이 없음. **리뷰 수정(2026-09-25):** listener마다 `state`를 따로 구독하던 구현은 원격 종료 뒤 새 listener가 연 generation의 변경을 기존 listener에게 알리지 못했다(실측: 기존 listener 알림 0회, `getSnapshot`은 새 generation `current` 반환 — React에서 같은 state를 읽는 컴포넌트끼리 tearing). store가 listener 집합과 `state` 구독 하나를 공유하도록 바꿨다. 동기 첫 알림 중 중첩 구독이 `state`를 다시 구독하던 재귀도 `open` 플래그로 막았다. 알림 중 해제된 listener 호출도 막았다. test 4건 추가(RED 확인), 패키지 verify 44 files/844 tests, demo `check-types`·`test:unit` 10 files/24 tests, 루트 `lint`·`format:check` 통과. ADR 0024·README·`docs/architecture.md`에 공유 구독과 남은 한계(store 밖 직접 구독이 연 generation은 끝난 listener에 알리지 않음)를 반영했다.

### snapshotStore의 generation 교체 추적 (출처: 2026-09-25 그릴링 "ADR 0024 한계 3 해결")

- [x] **RD-044 — `snapshotStore`가 listener가 있는 동안 새로 열린 generation에 합류해, generation 교체 뒤에도 모든 listener가 snapshot 변경 알림을 받게 한다.** `snapshotStore`의 알림 결함 수정이며 공개 타입은 바뀌지 않는다(이전 작업 불필요). 2026-09-25 확인 기준(`dev` @ `d83671a`): store는 자기 `state` 구독의 `next`·`complete`·`error`로만 변경을 안다. 원격 종료로 그 구독이 끝나고 listener만 남은 동안 같은 `RemoteState`에서 새 generation이 열리면 `getSnapshot()`은 새 상태를 돌려주지만 listener는 알림을 받지 못한다(tearing). G1: store 자신이 다시 열면 `connecting` 전이가 기존 listener에게 가지 않는다(첫 값·종료까지 일시). G2: store 밖 직접 구독이 새 generation을 열면 이후 값이 전달되지 않는다(지속). G3: 그 직접 구독이 해제될 때의 `current → stale`도 전달되지 않는다. generation을 끝내는 원인은 source complete/error만이 아니다 — 출력 검증 실패·연결 실패(`INTERNAL`, `src/main/subscriptions.ts:500-505`·`:553`), `authorize` 거부(`FORBIDDEN`), 구독 한도(`RESOURCE_EXHAUSTED`, `:259`), 세션 종료. 한도·권한은 나중 재구독이 성공할 수 있다. ADR 0024 한계 3이 G2를 기록했다. **구조:**
  - `LocalGeneration`(`kind: "state"`)에 모듈 내부 "generation 열림" 신호를 둔다. 발사 지점은 `subscribe()`에서 구독자 등록·`multiplexer.open`이 끝난 뒤 1곳이고, 그 generation이 여전히 활성(`this.#generation === generation && !generation.closed`)일 때만 쏜다(`open` 중 동기 종료 — transport 전송 실패·진단 sink 재진입 — 뒤에는 쏘지 않는다). 신호 listener 예외는 `subscribe()` 밖으로 내보내지 않고 rxjs 미처리 오류 보고와 같은 규칙(`config.onUnhandledError`, 없으면 `setTimeout` throw)으로 넘긴다. 등록 함수는 `local-generation.ts`가 export하되 `renderer/index.ts`에서 재export하지 않는다. `RemoteStateClient`가 아닌 입력에는 `undefined`를 돌려준다.
  - `snapshotStore`: 첫 listener가 들어올 때 신호에 등록하고 마지막 listener가 나갈 때 해제한다. 신호를 받으면 먼저 합류(`open === false`이면 `state.subscribe`)하고 그다음 listener 전원에게 notify한다. store 자신이 연 generation이면 합류하지 않는다. listener가 있는 동안 남이 연 generation도 유지한다(원격 구독 연장). 스스로 새 generation을 열지 않는다 — 새 listener 진입 때만 연다(RD-043 "자동 재구독 없음" 유지).
  - 알림 불변식: `getSnapshot()` 결과가 바뀌면 listener는 반드시 알림을 받는다. 여분 알림은 허용한다(dispose 뒤 subscribe의 1회, 활성 generation 합류 시 동기 재생). 구독 중인 listener도 자기 구독이 연 generation의 `connecting` 알림을 받는다.
  - 사용자 fake(`RemoteStateClient` 아님)는 신호가 없으므로 RD-043 동작(이벤트 기반) 그대로다. `RemoteState`·`RemoteStateSnapshot`·`RemoteStateStore` 공개 타입은 바뀌지 않는다.
  - README에 Main State source 평탄화 지침을 둔다: source complete/error는 Renderer generation을 끝낸다. source 교체는 오래 사는 `BehaviorSubject` 안에서 `switchAll`/`switchMap`으로 평탄화하고 안쪽 error는 값으로 바꾼다. helper는 추가하지 않는다.

  **범위 밖:** `RemoteState` Observable이 generation을 넘어 구독자를 잇는 계약 변경. `LocalGeneration`의 `share` 전환(`.scratch/local-generation-share/issues/01-share-replaysubject-refactor.md`). 공개 snapshot 변경 신호. RD-043 결과 문구 점검.

  **완료 기준:**
  - 수정 전 RED를 확인한 재현 test: G1(기존 listener가 `connecting` 알림), G2(직접 구독이 연 generation의 값 알림과 store 합류), G3(직접 구독 해제 뒤 generation 유지·`unsubscribe` 명령 없음), 첫 값 전 error로 끝난 새 generation(`uninitialized` 알림). 재진입: 신호 안 직접 subscribe·unsubscribe, 신호 안 마지막 listener 해제(wire `subscribe` → `unsubscribe` 순서, 누수 없음), 신호 listener 예외 격리, `transport.control` 동기 실패 시 재합류 루프 없음.
  - `test/renderer/snapshot-store.test.ts`의 기존 단언 변경은 "구독 중 listener의 `connecting` 알림"에 따른 호출 횟수 +1 조정과 그 제목뿐이다(DELTA에 목록). 사용자 fake 2건·dispose 1건·demo test 2건은 변경 없이 통과한다.
  - `test/renderer/remote-state.test.ts`·`remote-event.test.ts`·`renderer-dispose.test.ts`·`renderer-diagnostics.test.ts` 단언 변경 0건.
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit` 통과.
  - ADR 0024 개정 note(합류 방식, 알림 불변식, "한계" 3번째 항목 삭제, 평탄화 지침 참조), README "프레임워크 연동" 절 갱신과 평탄화 지침 문단·예제, `docs/architecture.md` State 항목 1문장을 반영한다.

  계획: `_works/20260925-21-remote-state-store-join/`. **결과:** 완료 조건 전부 충족, 편차 없음. DELTA-01(재현·재진입 test N1~N8을 `test/renderer/snapshot-store.test.ts`에 추가 — 수정 전 `npx vitest run test/renderer/snapshot-store.test.ts` 20 test 중 13 passed/7 failed: N1~N7 RED(N1·N2·N4는 A 미알림, N3는 `unsubscribe` 명령 발생과 `stale` 되돌림, N5·N7은 `connecting` 시점 호출 없음, N6은 A가 알림 못 받아 이탈 안 함), N8은 기존 error 배선만으로 이미 GREEN(신호 무관 경로, mutation (c)로 가치 확인 예정). 기존 test 12건 GREEN 유지, src 변경 없음) → DELTA-02(`LocalGeneration`에 `Set<() => void>` 내부 신호와 `onGenerationOpened` 모듈 함수(`WeakMap<RemoteState, LocalGeneration>` 등록 방식) 추가, `snapshotStore`가 첫 listener 진입 시 등록·마지막 이탈 시 해제하고 신호 수신 시 합류 먼저·notify 나중(결정 10) — `npx vitest run test/renderer/`: 7 files/159 tests 전부 GREEN(N1~N8 포함). mutation (a) 합류 제거 → N2·N3·N4 RED, mutation (b) 발사 지점을 `connecting` 대입 직후로 이동 → N5·N6 RED, mutation (c) 활성 generation 조건 제거 → N8 RED(subscribe 명령 1 기대에 6 수신) — 3종 모두 원복 뒤 159 재확인. 기존 단언 조정 15줄(제목 1+호출 횟수 14)이 DELTA-02 표와 정확히 일치. `git diff` src는 `local-generation.ts`·`snapshot-store.ts` 2파일뿐, 보존 test 5파일+`apps/demo` diff 0. 패키지 `check-types`·루트 `lint`·build·demo `check-types`·`test:unit`(10 files/24 tests) 통과) → DELTA-03(ADR 0024 개정 note 추가·"한계" 3번째 항목 삭제, README "프레임워크 연동" 절 갱신과 Main State source 평탄화 지침 문단+예제 추가, `docs/architecture.md` State 항목 1구절 추가 — 코드 변경 없음. 평탄화 예제를 임시 `.ts`로 옮겨 `tsc --noEmit`로 검증 후 삭제, `pnpm format:check` 통과) → DELTA-04(전체 검증. 패키지 `xvfb-run -a pnpm verify` 44 files/852 tests 통과(RD-043 종료 시점 44 files/844 tests 기준 +8 = N1~N8과 정확히 일치), `test/renderer/` 단독 7 files/159 tests. 루트 `pnpm lint`·`pnpm format:check` 통과. demo `check-types` 통과, `test:unit` 10 files/24 tests 통과. `git diff dev --stat -- packages/rx-bridge-electron/src` 2파일(`local-generation.ts` +102/-2, `snapshot-store.ts` +35/-4)만 확인. 보존 대상 test 5파일+`apps/demo` diff 0 재확인. `snapshot-store.test.ts` 삭제 줄 15개가 DELTA-02 표와 일치(개요 주석 추가는 삭제 없음). `grep -n "onGenerationOpened|generationOpened" src/renderer/index.ts` 0건(재export 없음, exit 1). **발견:** 없음(`pending-traps/`·`pending-issues/` 둘 다 비어 있음, 승격 대상 없음). **리뷰 수정(2026-09-26):** 결함 2건을 test 먼저(수정 전 RED 확인)로 고쳤다. (1) store보다 먼저 subject에 붙은 직접 구독(`repeat`·`retry`)이 원격 종료 알림 안에서 동기로 재구독하면 신호가 store의 `open === true`일 때 와서 합류가 빠졌다 — 새 generation 값이 `getSnapshot()`에는 보이지만 listener는 알림을 받지 못했다(N9). store의 종료 처리(`finish`)가 활성 generation(`state.snapshot.active`)이 있으면 합류하게 했다. (2) transport가 `control` 안에서 값을 동기로 보내면 합류의 현재값 재생 알림 안에서 마지막 listener가 떠날 때 `upstream`이 아직 없어 합류 구독이 남았다(원격 구독 누수, N10) — 결정 14의 "합류는 즉시 반환" 전제가 동기 transport에서 깨진다. 합류 뒤 listener가 없거나 다른 합류가 끼어들었으면(N11) 그 구독을 바로 놓는다. mutation: 종료 처리 합류 제거 → N9 RED, 끼어든 합류 확인 제거 → N11 RED. ADR 0024 개정 note에 반영했다.

### 사용자 source teardown 예외 격리 (출처: [.scratch/upstream-teardown-throw/issues/01-upstream-teardown-throw.md](.scratch/upstream-teardown-throw/issues/01-upstream-teardown-throw.md))

- [x] **RD-045 — 사용자 State·Event source의 teardown 예외를 `Upstreams`가 격리하고 새 진단 이벤트 `upstream-teardown-failed`로 기록한다.** 결함 수정과 공개 타입 추가다(`BridgeDiagnostic`에 멤버 1개 추가, 외부 사용자가 없어 이전 안내는 두지 않는다). 2026-09-26 재현 기준(`dev` @ `907c0e9`, 재현 test `test/main/upstream-teardown-throw.test.ts` 10건 RED, teardown throw만 뺀 대조 실행 10건 GREEN): rxjs 7은 teardown throw를 `UnsubscriptionError`로 다시 던지고 `Upstreams.disconnect`(`src/main/upstreams.ts:101-118`)는 catch 없이 부른다. (1) 세션 retire(`main-frame-navigation`·`render-process-gone`·`destroyed`·`replaced`)와 `server.dispose()`는 abort listener 안에서 consumer를 닫으므로 Main `uncaughtException`이 된다. (2) 공유(State·broadcast Event) 갈래는 `unsubscribe()` 뒤에 `#shared.delete`를 하므로 throw하면 해지된 entry가 남아 같은 key의 재구독이 upstream을 다시 구독하지 않는다(broadcast는 이후 값 0건, State는 늦은 합류 값 1건 뒤 변경 없음). (3) 전송 실패 뒤 close에서 난 예외는 `new Subscriber(observer)`에 격리가 없어 사용자 producer 호출(`subject.next`)로 동기 전파되고, 같은 `Subject`의 뒤 구독자가 그 값을 받지 못한다. **구조:**
  - `Upstreams`가 upstream 해지 지점(`disconnect`의 scoped·shared 갈래, 첫 member가 subscribe 중 빠진 경우의 해지)을 한 private 함수로 모아 `unsubscribe()` 예외를 잡는다. 공유 갈래는 map에서 entry를 먼저 지우고 그다음 해지한다.
  - 해지 예외는 생성자로 받은 콜백 `onTeardownError(key)`로 알린다(생략 시 no-op). `Upstreams`는 진단 sink를 모르는 채로 남는다. `Subscriptions`가 이 콜백을 `recordDiagnostic`에 잇는다.
  - 동기 방출 중 이미 닫힌 upstream에 teardown이 붙으며 throw하는 경우(`source.subscribe` 밖으로 나오는 예외, upstream이 이미 closed)도 같은 진단으로 기록하고 삼킨다. upstream이 닫히지 않았으면 기존대로 다시 던진다.
  - 진단 모양은 `{ type: "upstream-teardown-failed"; key: string }`이다. `Error`·message를 싣지 않는다(ADR 0010 §3). 실패한 해지 1회에 1건이며, 공유 upstream은 마지막 구독의 `subscription-closed` 뒤에 기록될 수 있다(구독이 아니라 upstream 단위 진단).

  **범위 밖:** upstream observer 전체의 예외 격리(`SafeSubscriber` 전환) — 수정 뒤 observer 안에서 사용자 producer로 새는 경로가 남지 않는지 확인만 하고, 남으면 멈추고 보고한다. 사용자 abort listener(`BridgeContext.signal`) 예외. source `complete`/`error` 자체 처리 변경. RPC 경로.

  **완료 기준:**
  - 재현 test 10건이 수정 뒤 GREEN이고, 각 경로에서 `upstream-teardown-failed`가 key와 함께 기록됨을 단언한다. 동기 방출 중 닫힌 upstream의 teardown throw test를 추가한다(RED 또는 진단 미기록 확인 먼저).
  - mutation: (a) 공유 갈래 map 삭제를 해지 뒤로 되돌리면 ~~가설 2 test~~ teardown 안 동기 재구독 test RED(2026-09-26 사용자 확인으로 대상 교체), (b) 해지 catch 제거 시 가설 1·4 test RED.
  - 기존 test 단언 변경 0건(`upstreams.test.ts` 포함).
  - 패키지 `xvfb-run -a pnpm verify`, 루트 `pnpm lint`·`pnpm format:check`, demo `check-types`·`test:unit` 통과.
  - 새 ADR 0025(격리·정리 순서·진단 결정), ADR 0010 개정 note(이벤트 1종 추가), `docs/architecture.md` 진단 문단, 패키지 README 진단 문단, `.scratch` 이슈 Status 갱신.

  계획: `_works/20260926-01-upstream-teardown-isolation/`. **결과:** 완료 조건 충족. 편차 1건: mutation (a)의 RED 대상 test를 교체했고 사용자 확인을 받았다(아래 발견). DELTA-01(재현 test 10건에 진단 단언을 더하고, 동기 방출 경로 2건(구독 중 동기 complete 뒤 teardown throw — scoped·broadcast)을 추가. 수정 전 12/12 RED: retire·dispose·replaced·scoped retire는 `uncaughtException`, 공유 정리 2건은 재구독 값 누락, 전송 실패 2건은 `subject.next`의 동기 `UnsubscriptionError`, 동기 방출 2건은 진단 미기록) → DELTA-02(`Upstreams`에 `#release`(해지 catch → `onTeardownError(key)`)·`#subscribe`(구독 호출이 던졌을 때 upstream이 이미 closed면 teardown 예외로 처리) 추가, 공유 갈래를 map 삭제 → 해지 순서로 변경, scoped entry에 key 보관, `Subscriptions`가 콜백을 `recordDiagnostic`에 연결, `BridgeDiagnostic`에 `upstream-teardown-failed` 추가. `test/main/` 29 files/536 tests 통과, 기존 단언 변경 0건. mutation: (a) 순서 되돌림 → 1 failed, (b) `#release` catch 제거 → 11 failed, (c) `#subscribe` catch 제거 → 2 failed, 모두 원복. 남은 동기 전파 경로 점검: observer 경로의 예외 지점(출력 검증·창 콜백·진단 기록·`send`·`disconnect`)이 모두 catch됨. `controller.abort()`의 사용자 abort listener 예외만 `uncaughtException`으로 남는다(범위 밖)) → DELTA-03(ADR 0025 신규, ADR 0010 상단 note·§4 개정 표시, `docs/architecture.md` 진단 문단, README 진단 문단, `.scratch` 이슈 Status "승격") → DELTA-04(전체 검증) 순으로 진행. 검증 수치: 패키지 `xvfb-run -a pnpm verify` 45 files/868 tests 통과, 루트 `pnpm lint`·`pnpm format:check` 통과, demo `check-types` 통과, `test:unit` 10 files/24 tests 통과. `git diff dev --stat -- packages/rx-bridge-electron/src`는 `main/subscriptions.ts`·`main/types.ts`·`main/upstreams.ts` 3파일, test는 새 파일 1개뿐. **발견:** 공유 갈래의 옛 순서(해지 → map 삭제)는 teardown throw와 무관하게 결함이었다. teardown 안에서 같은 key를 동기로 다시 구독하면 새 구독이 해지 중인 entry에 붙은 뒤 고아가 됐다. test로 고정했다. 해지 예외를 catch한 뒤에는 가설 2 test가 순서와 무관하게 통과하므로, mutation (a)의 대상을 이 test로 바꿨다. **리뷰 수정(2026-09-26):** (1) RD-045 항목 삽입이 RD-036 결과 문단 안의 `## 현재 범위 밖의 확장` 문자열에 걸려 그 문단을 자르고 가짜 제목을 만들었다. RD-036 문단을 원문대로 되돌리고 RD-045 절을 RD-044 뒤로 옮겼다. (2) `docs/architecture.md`·`BridgeDiagnostic` 주석의 순서 서술을 고쳤다 — upstream terminal·overflow 경로는 해지가 `subscription-closed` 앞이라 "항상 뒤"가 아니다. scoped upstream도 close 경로에서는 뒤에 온다. (3) operator를 거친 source(`pipe(map(...))`)의 동기 방출 경로는 rxjs `operate`가 teardown 예외를 닫힌 upstream의 `error`로 보내 버려 진단이 남지 않는다(예외는 새지 않음). ADR 0025 한계·module 주석에 적고 test 1건으로 고정했다. 패키지 `xvfb-run -a pnpm verify` 45 files/869 tests, 루트 `pnpm lint`·`pnpm format:check` 통과.

### 기능별 설계 문서 (출처: 사용자 요청 2026-09-26, [.scratch/readme-restructure/issues/01-readme-user-guide-restructure.md](.scratch/readme-restructure/issues/01-readme-user-guide-restructure.md) 검토 질문 1·2)

- [x] **RD-046 — `docs/design/`에 기능별 설계 문서를 작성하고 패키지 README를 한글 단일로 바꾼다.** ADR은 결정 단위·시간순이라 대체·개정 note가 쌓였고, architecture.md는 개요와 세부가 한 문단에 섞였다. 주제별로 현재 모델·불변식·흐름·설계 이유·기각한 대안·한계를 한 곳에 모은다. 소스 라인번호와 작업 이력(RD·DELTA·커밋)을 쓰지 않는다. 코드 변경은 없다.
  - `docs/design/README.md`(문서 역할·주제 색인·작성 규칙)와 주제 문서 11개: 계약과 등록, Renderer API, Transport와 배선, 문서 세션, RPC, Main 스트림 전달, Renderer 스트림과 State, Payload와 오류 모델, 세션 자원 한도, 종료, 진단.
  - 패키지 README의 영어 문장을 없앤다(설치 절 병기, 절 이름 `Hello world`·`Testing`).
  - `docs/README.md`·`docs/agents/domain.md`에서 설계 문서를 찾게 한다.

  **범위 밖:** README 재구성(이슈 `readme-restructure/01`, 이번 결정으로 세부 이관처가 `docs/design/`으로 정해진다), architecture.md 축약.

  **완료 기준:** 주제 문서 11개와 색인이 있다. 라인번호·작업 이력 grep 0건. 각 문서의 규칙을 코드·test와 대조한 검토 결과가 있고 발견을 반영했다. 문서 간 같은 규칙의 서술이 일치한다. 패키지 README 코드 블록 밖 영어 문장 0건. 루트 `pnpm format:check` 통과.

  계획: `_works/20260926-03-design-docs/`. **결과:** 완료 조건 충족, 편차 없음. 주제 문서 11개를 주제별 작성 에이전트 6개가 소스·test와 대조해 작성했고(문서당 규칙 25~60건 확인), 별도 교차 검토에서 모순 5건(06 capacity 한 칸 차이, 06 bind `dispose()` retire 사유, stream `authorize` 거부 중 sink 재진입 retire가 `CANCELLED`인 규칙 누락, 같은 문서 두 transport `replaced` 무통지 한계 누락, 11 RPC 진단 순서의 취소 위치)과 소유 중복 4건을 코드 기준으로 고쳤다. 라인번호·작업 이력 grep 0건, `pnpm format:check` 통과. 작성 중 발견한 architecture.md 불일치(`EventSource` re-export 서술, `./protocol` 진입점 누락, bind `dispose()` retire 행, RPC slot 반환 서술, 구독 판정 순서의 "RPC와 같은 순서", RD 표기)와 README 사실 오류 3건(adapter envelope 검사, stream의 `CANCELLED` 우선, `signal`을 무시하는 handler의 slot 점유)을 바로잡았다. 후속 이슈: [ADR·코드 주석 불일치](.scratch/adr-code-drift/issues/01-adr-and-comment-drift.md), [`BridgeOperation.domain` 미분해](.scratch/namespace-key-slash-domain/issues/01-bridge-operation-domain-split.md), [`EventSource` 타입 미공개](.scratch/event-source-type-export/issues/01-event-source-types-not-exported.md). README 재구성 이슈에 검토 질문 결정을 기록했다.

## 현재 범위 밖의 확장

Binary/MessagePort 전송, 지속적인 초고속 Event, 원격 콘텐츠·플러그인 권한, 범용 `global/session/webContents` 스트림 scope, React 전용 패키지는 지금의 RD에 포함하지 않는다. 타입에서 스키마 자동 생성(typia, ts-to-zod), 타입 수준 RPC 에러 코드, RPC 다중 인자도 경량 계약 RD 범위 밖이다. 실제 사용 사례가 생기면 성능·신뢰 모델과 공개 인터페이스를 별도로 설계한 뒤 다음 RD 번호로 추가한다. 기존 `rx-bridge-electron`의 RPC·State·Event 인터페이스를 통해 해결 가능한지 먼저 확인한다.
